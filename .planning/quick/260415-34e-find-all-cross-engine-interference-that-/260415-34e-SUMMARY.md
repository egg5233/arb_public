---
phase: quick
plan: 260415-34e
type: audit
tags: [cross-engine, interference, audit, read-only]
completed: 2026-04-15
---

# Quick Task 260415-34e: Cross-Engine Interference Audit — Summary

Read-only static analysis of every exchange-level query in `internal/engine/`, `internal/spotengine/`, and `internal/risk/` that could see the other engine's positions or balances. Catalogs 21 new findings (HIGH=7, MEDIUM=9, LOW=5) beyond the two fixes landed in v0.32.13.

## Deliverable

- `260415-34e-CROSS-ENGINE-AUDIT.md` — full structured report with priority matrix and recommended fix strategy

## Key takeaways

- **7 HIGH-severity sites** are direct twins or close cousins of the v0.32.13 bugs. The most urgent is `exit.go:1893-1894` (closePositionWithMode post-close verify) — identical pattern to the already-fixed rotation bug at `exit.go:2786`.
- **Two bilateral bugs:** finding 7 (PP entry fallback absorbs SF size) and finding 16 (SF pending-entry absorbs PP size) — same pattern on both engine sides.
- **Unified-account blast radius:** on Bybit UTA, OKX, Gate.io, `GetFuturesBalance.Available` and `.MarginRatio` are combined-engine metrics. PP's L5 safety transfer (finding 13) and SF's margin_health_exit trigger (finding 15) are the highest-impact unified-pool consequences.
- **WebSocket isolation is weak but not broken today:** SF fills route through PP's callback and are filtered only by the same `getExchangePositionSize` verify path that carries the HIGH findings. Fixing the verify path correctly requires either per-site `sfSizeOffset` subtraction or a shared `ownOrders` map.

## Recommended fix sequencing

1. **v0.32.14 (immediate):** `sfSizeOffset` subtraction on the 7 HIGH sites — mechanical, mirrors v0.32.13
2. **v0.33.0 (short-term):** Introduce `getEnginePositionSize` and `getEngineFuturesBalance` wrappers, migrate all P0+P1 sites
3. **Later:** Promote `ownOrders` to shared state, convert adapter callback to fan-out dispatcher

## No source files modified

This task was audit-only per the plan's READ-ONLY constraint.
