---
phase: 09-price-gap-dashboard-paper-live-operations
plan: 08
type: execute
wave: 5
depends_on: [03, 04, 05, 06, 07]
files_modified:
  - internal/pricegaptrader/paper_to_live_test.go
  - internal/pricegaptrader/e2e_regression_test.go
  - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md
  - CHANGELOG.md
  - VERSION
autonomous: false
requirements: [PG-OPS-01, PG-OPS-02, PG-OPS-03, PG-OPS-04, PG-OPS-05, PG-VAL-01, PG-VAL-02]
must_haves:
  truths:
    - "A paper-mode run produces a closed position with Mode='paper' and non-zero RealizedSlipBps (Pitfall 7 proof)"
    - "Flipping PriceGapPaperMode true→false mid-life preserves existing paper positions' Mode='paper' through their close (Pitfall 2 proof)"
    - "A live run after the flip produces a closed position with Mode='live' in the same pg:history list"
    - "Closed Log rows include correct Modeled/Realized/Delta for both paper and live trades"
    - "Every PG-OPS-0X and PG-VAL-0X requirement has at least one automated test AND one UAT checkbox"
    - "CHANGELOG.md + VERSION bumped (project rule: every commit updates both)"
  artifacts:
    - path: internal/pricegaptrader/paper_to_live_test.go
      provides: "End-to-end paper→live cutover test"
      contains: "TestPaperToLiveCutover"
    - path: .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md
      provides: "Manual verification checklist for human-visible behaviors"
    - path: CHANGELOG.md
      provides: "Phase 9 release notes"
    - path: VERSION
      provides: "Bumped version"
  key_links:
    - from: "Closed log history entries"
      to: "PriceGapPosition.Mode field (paper/live)"
      via: "immutable entry-time stamp"
      pattern: 'Mode:\\s*"(paper|live)"'
---

<objective>
End-to-end validation of Phase 9. This plan does four things:
1. Exercises the paper→live cutover as an automated integration test (PG-VAL-01, PG-VAL-02).
2. Writes a UAT checklist covering every browser-visible behavior that `go test` cannot prove.
3. Runs a full regression sweep across the codebase to prove perp-perp and spot-futures paths are untouched.
4. Bumps VERSION + CHANGELOG per the project commit rule.

Purpose: Phase 9 ships live-trading-adjacent code. The gate to green-light is a proven paper→live transition AND a proven zero-regression in existing engines. This plan is the final checkpoint.

Output: One automated integration test, one UAT checklist, a regression sweep result, a version bump. One human checkpoint at the end.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-CONTEXT.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-RESEARCH.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-UI-SPEC.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-VALIDATION.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-03-PLAN.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-04-PLAN.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-06-PLAN.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-07-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Paper→Live cutover integration test + full regression sweep</name>
  <files>internal/pricegaptrader/paper_to_live_test.go, internal/pricegaptrader/e2e_regression_test.go</files>
  <read_first>
    - internal/pricegaptrader/execution_test.go, monitor_test.go (existing stub exchange + DB harness)
    - internal/pricegaptrader/paper_mode_test.go (Plan 03 paper tests)
    - internal/pricegaptrader/tracker_broadcast_test.go (Plan 06 mock Broadcaster/Notifier)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-VALIDATION.md § "Per-Task Verification Map" row for TBD-val-01
  </read_first>
  <behavior>
    - Test TestPaperToLiveCutover: with PriceGapPaperMode=true, open paper position pA; flip flag to false; open live position pB; close both; pg:history contains 2 entries with pA.Mode="paper" and pB.Mode="live"; pA.RealizedSlipBps > 0 (Pitfall 7 proof); pB has non-zero realized slippage too; stub exchange PlaceOrder counter == 2 (once per leg on pB, zero on pA).
    - Test TestPaperToLiveCutover_NoOrphans: after the flip, pA's monitor goroutine continues to manage pA and eventually closes it without attempting a live close; paper close synth fires not the stub exchange.
    - Test TestPaperToLiveCutover_TelegramContinuity: mockNotifier records 2 entry calls and 2 exit calls; pA's Exit message has 📝 PAPER prefix, pB's does not (D-22 verification).
    - Test TestPaperToLiveCutover_ClosedLogShape: GET /api/pricegap/closed returns both pA and pB with correct Modeled/Realized/Delta fields, newest first.
    - Test TestE2ERegression_PerpPerpEngineUntouched: start internal/engine (stubbed) in a fresh test, run a canned funding-rate-arb scenario, assert final state matches pre-Phase-9 golden snapshot (use reflect.DeepEqual or field-by-field compare). If such a regression harness does not exist, this test becomes an expectation `t.Skip` with a note that the guardrail is Plan 04's Telegram regression test + `go test ./internal/engine/... -count=1 -race` passing unchanged.
    - Test TestE2ERegression_SpotFuturesEngineUntouched: same pattern for internal/spotengine. `t.Skip` acceptable if no harness exists; the guardrail is `go test ./internal/spotengine/... -count=1 -race` passing unchanged.
  </behavior>
  <action>
    1. Create `internal/pricegaptrader/paper_to_live_test.go` with `TestPaperToLiveCutover` and its 3 siblings. Use the existing stub exchange + in-memory/miniredis DB harness. Drive the test by:
       - Construct tracker with stub exchange + miniredis-backed DB + mock Broadcaster + mock Notifier.
       - Set `cfg.PriceGapPaperMode = true`.
       - Call `t.openPair(...)` for candidate A; assert position persisted with Mode="paper".
       - Set `cfg.PriceGapPaperMode = false` (simulating /api/config flip).
       - Call `t.openPair(...)` for candidate B; assert Mode="live" and stub PlaceOrder called once per leg.
       - Trigger close of A; assert synth path (stub PlaceOrder counter does NOT increase).
       - Trigger close of B; assert real path (counter increases).
       - Read `GetPriceGapHistory(0, 10)`; assert 2 entries with correct Mode values.
    2. Create `internal/pricegaptrader/e2e_regression_test.go` with the two regression tests. If harness missing, use `t.Skip("guardrail: go test ./internal/engine/... and ./internal/spotengine/... must stay green — run full suite after this plan lands")` — do NOT fabricate a regression that cannot actually assert.
    3. Run full suite: `go test ./... -count=1 -race`. All green.
    4. Run full frontend build: `(cd web && npm run build)`. Green.
    5. Run i18n sync gate: `bash scripts/check-i18n-sync.sh`. Green.
  </action>
  <verify>
    <automated>go test ./... -count=1 -race -short && bash scripts/check-i18n-sync.sh && (cd web && npm run build)</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/pricegaptrader/paper_to_live_test.go`
    - `grep -q "TestPaperToLiveCutover" internal/pricegaptrader/paper_to_live_test.go`
    - `grep -q "TestPaperToLiveCutover_NoOrphans" internal/pricegaptrader/paper_to_live_test.go`
    - `grep -q "TestPaperToLiveCutover_TelegramContinuity" internal/pricegaptrader/paper_to_live_test.go`
    - `grep -q "TestPaperToLiveCutover_ClosedLogShape" internal/pricegaptrader/paper_to_live_test.go`
    - `go test ./... -count=1 -race -short` exits 0
    - `go test ./internal/engine/... -count=1 -race` exits 0 (perp-perp regression)
    - `go test ./internal/spotengine/... -count=1 -race` exits 0 (spot-futures regression)
    - `(cd web && npm run build)` exits 0
    - `bash scripts/check-i18n-sync.sh` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>
    Paper→live cutover proven atomically. Existing engines demonstrably untouched. Full test + build suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: UAT checklist + CHANGELOG + VERSION bump</name>
  <files>.planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md, CHANGELOG.md, VERSION</files>
  <read_first>
    - VERSION (current string)
    - CHANGELOG.md (latest entry format — match style precisely)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-VALIDATION.md § "Manual-Only Verifications"
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UI-SPEC.md (the SPEC sections a human must visually verify)
  </read_first>
  <action>
    1. Create `.planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md` with the following sections:
       ```markdown
       # Phase 9 — User Acceptance Testing Checklist

       Human operator runs through these after `make build` and local launch. Each row maps to a requirement and a 09-UI-SPEC section.

       ## Requirement Coverage

       | Req | Behavior | UI-SPEC ref | Checked |
       |-----|----------|-------------|---------|
       | PG-OPS-01 | Price-Gap tab appears between spot-positions and history in nav | Nav Integration | [ ] |
       | PG-OPS-01 | Candidates table lists every entry from cfg.PriceGapCandidates | Layout Contract §2 | [ ] |
       | PG-OPS-01 | Disable button on an enabled candidate opens modal with reason field; typing reason + confirm writes pg:candidate:disabled:<symbol> (verify via `redis-cli GET pg:candidate:disabled:<SYM>`) | Interaction Contracts | [ ] |
       | PG-OPS-01 | Re-enable button on a disabled candidate opens modal showing reason + age; confirm clears the key | Interaction Contracts | [ ] |
       | PG-OPS-01 | Disabling via pg-admin CLI then refreshing dashboard shows same state (single-writer parity) | Canonical refs | [ ] |
       | PG-OPS-02 | Live Positions table populates after a paper entry, showing Entry/Current/Hold/PnL columns | Live Positions table | [ ] |
       | PG-OPS-02 | Current spread updates via WS (watch DevTools → WS frames for pg_positions) | Interaction Contracts | [ ] |
       | PG-OPS-02 | PAPER badge violet, LIVE badge green; row tint on paper rows is subtle violet | Color | [ ] |
       | PG-OPS-03 | Closed Log shows Modeled, Realized, Delta columns with correct sign color (red when realized > modeled) | Closed Log table | [ ] |
       | PG-OPS-03 | Load 100 more button appears when >100 rows in history | Pagination | [ ] |
       | PG-OPS-04 | Paper toggle in header flips between violet PAPER pill and green LIVE pill; POST /api/config persists to config.json | Header toggles | [ ] |
       | PG-OPS-04 | Flipping toggle mid-life does NOT relabel existing open paper positions | State Machine | [ ] |
       | PG-OPS-04 | Default boot with no price_gap.paper_mode in config → PAPER pill shown (default TRUE per D-12) | Pitfall 1 | [ ] |
       | PG-OPS-05 | Telegram entry alert arrives on paper entry with 📝 PAPER prefix | Copywriting Contract | [ ] |
       | PG-OPS-05 | Telegram exit alert arrives with reason, PnL, realized slippage, hold duration | D-20 | [ ] |
       | PG-OPS-05 | Telegram risk-block alert arrives once; subsequent alerts same (gate, symbol) within 1h suppressed | D-21 | [ ] |
       | PG-OPS-05 | No regression: perp-perp entry still fires NotifyAutoEntry | Plan 04 regression | [ ] |
       | PG-VAL-01 | Closed Log row for a paper trade shows Realized > 0 bps (Pitfall 7 proof) | D-04 | [ ] |
       | PG-VAL-02 | Rolling Metrics section populates with 24h/7d/30d bps/day per candidate | D-25 | [ ] |
       | PG-VAL-02 | Default sort is 30d bps/day descending | UI-SPEC Data Persistence | [ ] |
       | i18n | Switch locale to zh-TW; every Price-Gap surface renders Chinese copy, no English fallback | Copywriting Contract | [ ] |
       | Accessibility | Modals respond to Esc; role="dialog" present (check DevTools) | Accessibility Contract | [ ] |

       ## Runbook

       1. `make build` (frontend first, then Go)
       2. Launch with `./arb` (or systemd if configured)
       3. Open dashboard, navigate to Price-Gap tab
       4. Walk every row above, checking boxes as behaviors confirm
       5. Flip a paper→live cutover manually (only in dev/staging — never prod without approval)
       6. Review Telegram channel for expected messages
       7. File any failures as GitHub issues tagged phase-9 / bug

       **Exit criteria:** every row checked. Any unchecked row blocks Phase 9 sign-off.
       ```
    2. Bump `VERSION`: read current value (likely `0.33.x` per memory; Phase 8 closed at v0.33.0). Phase 9 ships as a minor. Bump to `0.34.0`.
    3. Prepend to `CHANGELOG.md` a Phase 9 entry. Match the existing CHANGELOG style — if entries are `## [VERSION] - DATE` then brief bullets, use that format. Bullets:
       ```
       ## [0.34.0] - 2026-04-22
       ### Added
       - Price-Gap dashboard tab (PG-OPS-01): candidate list with Disable/Re-enable confirmations, Live Positions, Closed Log with Modeled/Realized/Delta, Rolling Metrics (24h/7d/30d bps/day).
       - Paper-mode toggle (PG-OPS-04) persisted to config.json, default TRUE for new deployments (safety before live capital).
       - Telegram alerts (PG-OPS-05): NotifyPriceGapEntry / Exit / RiskBlock with cooldown per (gate, symbol); 📝 PAPER prefix in paper mode.
       - Realized-vs-modeled slippage logging (PG-VAL-01) surfaced in Closed Log.
       - On-demand rolling metrics (PG-VAL-02) from pg:history — no new write paths.
       - WebSocket topics: pg_positions, pg_event, pg_candidate_update.
       - REST: /api/pricegap/{state,candidates,positions,closed,metrics,candidate/{sym}/disable,candidate/{sym}/enable}.
       - i18n sync gate script: scripts/check-i18n-sync.sh.
       ### Changed
       - PriceGapPosition gained Mode field ("paper" | "live"), stamped at entry and immutable.
       - SetCandidateDisabled now persists JSON {reason, disabled_at} — backward-compatible reader handles legacy plain-string values.
       ### Unchanged (regression-pinned)
       - Perp-perp Telegram messages (golden-string regression tests added).
       - Spot-futures notification paths.
       - Phase 8 tracker core behaviour when PriceGapPaperMode=false.
       ```
    4. Do NOT commit. Planner does not commit; /gsd-execute-phase commits per-task.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md && grep -q "PG-OPS-01" .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md && grep -q "PG-VAL-02" .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md && grep -q "0.34.0" VERSION && grep -q "0.34.0" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md`
    - UAT covers all 7 Phase 9 requirements: PG-OPS-01, PG-OPS-02, PG-OPS-03, PG-OPS-04, PG-OPS-05, PG-VAL-01, PG-VAL-02
    - UAT has at least 20 checkbox rows
    - `cat VERSION` returns `0.34.0` (or whatever next minor fits current scheme — planner picks based on current VERSION content read at task time)
    - `head -30 CHANGELOG.md | grep -q "0.34.0"`
    - CHANGELOG entry format matches existing entries (verified by eyeballing `head -80 CHANGELOG.md` shape)
  </acceptance_criteria>
  <done>
    UAT checklist ready for human walk-through. Version + changelog bumped.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT walkthrough + Telegram visual check + paper→live dry run</name>
  <what-built>
    Everything from Plans 01–07 plus Task 1 and Task 2 of this plan. The full Phase 9 surface: dashboard tab, REST+WS backend, paper mode, Telegram alerts, rolling metrics, UAT checklist, version+changelog.
  </what-built>
  <how-to-verify>
    1. Run `make build` (frontend first, then Go binary). Confirm both stages exit 0.
    2. Start the bot locally (dev/staging — NEVER prod without explicit user approval): `./arb`.
    3. Open dashboard, log in with bearer token, navigate to Price-Gap tab.
    4. Walk every checkbox row in `.planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md`. Tick boxes as behaviors confirm. File any uncheckable row as a bug issue.
    5. Verify paper-mode Telegram pipeline: force a paper entry by enabling one candidate with a threshold low enough to trigger; observe the 📝 PAPER entry + exit alerts in Telegram.
    6. Verify risk-block Telegram: trigger a gate (e.g., disable a candidate then attempt entry) and confirm cooldown — second attempt within 1h should NOT alert.
    7. Verify paper→live cutover in a controlled run: flip toggle, observe existing paper position keeps PAPER badge through its close; new entries get LIVE badge.
    8. Verify zh-TW locale: switch language, confirm every Price-Gap surface renders Chinese.
    9. Verify no regression on perp-perp / spot-futures: let the bot run 5+ minutes with both engines active, confirm existing positions unaffected and existing Telegram alerts still fire.
    10. Sign UAT.md.

    Runtime config reminder: Claude MUST NOT modify `config.json`. If the operator needs paper mode on, they either flip the dashboard toggle or manually edit config.json themselves.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 9 complete, or describe specific UAT rows that failed so a gap-closure plan can be drafted.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full-suite regression | Existing perp-perp and spot-futures engines must be byte-for-byte untouched in behavior |
| Paper→live cutover | Mid-life mode flip must preserve per-position Mode immutability |
| Live Telegram channel | Real alerts fire during UAT — operator must confirm no spam and no missing alerts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-35 | Elevation | Phase 9 ships with an unnoticed regression in perp-perp alerts | high | mitigate | Task 1 regression tests + Plan 04 golden-string tests + UAT row "No regression: perp-perp entry still fires NotifyAutoEntry" |
| T-09-36 | Tampering | Paper→live flip corrupts an open paper position | high | mitigate | Task 1 TestPaperToLiveCutover_NoOrphans + UAT row for mid-life flip |
| T-09-37 | Elevation | Operator accidentally ships live with paper off and no UAT sign-off | high | mitigate | Task 3 checkpoint is blocking — no code merges past this without "approved" resume-signal |
| T-09-38 | Repudiation | CHANGELOG or VERSION missed bumping, break downstream tooling | medium | mitigate | Acceptance criteria asserts both files bumped to the new version |
</threat_model>

<verification>
- Task 1: `go test ./... -count=1 -race -short` green; frontend build green
- Task 2: UAT.md + VERSION + CHANGELOG in place
- Task 3: human sign-off recorded
- Every PG-OPS-0X and PG-VAL-0X has both automated test and UAT checkbox
</verification>

<success_criteria>
Phase 9 is demonstrably complete: operator has walked every UAT row, paper→live cutover proven live and in tests, regressions pinned, version bumped. Green light to commit + enable on a live account when the operator chooses.
</success_criteria>

<output>
After completion, create `.planning/phases/09-price-gap-dashboard-paper-live-operations/09-08-SUMMARY.md` listing: regression test count, UAT sign-off date, VERSION bump, any rows the operator flagged as failing (becomes input for a gap-closure plan if nonzero).
</output>
