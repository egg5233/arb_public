---
phase: 09-price-gap-dashboard-paper-live-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/pricegap_position.go
  - internal/config/config.go
  - internal/database/pricegap_state.go
  - internal/pricegaptrader/tracker.go
  - internal/pricegaptrader/notify.go
  - internal/pricegaptrader/types.go
  - internal/pricegaptrader/monitor.go
  - internal/pricegaptrader/slippage.go
  - internal/pricegap/slippage_write_path_test.go
  - scripts/check-i18n-sync.sh
autonomous: true
requirements: [PG-OPS-02, PG-OPS-03, PG-OPS-04, PG-OPS-05, PG-VAL-01, PG-VAL-02]
must_haves:
  truths:
    - "PriceGapPosition struct carries a Mode field persisted to Redis"
    - "PriceGapPosition struct carries ModeledSlipBps and RealizedSlipBps float64 fields persisted to Redis (D-23)"
    - "closePair writes BOTH ModeledSlipBps AND RealizedSlipBps onto pos before LPUSH to pg:history (D-23)"
    - "PriceGapPaperMode bool config field exists with default TRUE (D-12 exception) and round-trips via /api/config"
    - "GetPriceGapHistory(offset, limit) reader method exists on the store and returns newest-first positions"
    - "Tracker holds a Broadcaster interface (DI) that internal/api/*.go can implement without breaking module boundary"
    - "scripts/check-i18n-sync.sh exits 0 when en.ts and zh-TW.ts key sets match, nonzero otherwise"
    - "With D-23 landed, realized-vs-modeled comparison is computable from pg:history alone — Plan 05 metrics do NOT need to read pg:slippage:* (D-24 simplification)"
  artifacts:
    - path: internal/models/pricegap_position.go
      provides: "Mode string field on PriceGapPosition, default 'live' on unmarshal"
      contains: "Mode string"
    - path: internal/pricegaptrader/types.go
      provides: "ModeledSlipBps + RealizedSlipBps float64 fields on PriceGapPosition (D-23)"
      contains: "ModeledSlipBps"
    - path: internal/config/config.go
      provides: "PriceGapPaperMode config field + Defaults + JSON round-trip"
      contains: "PriceGapPaperMode"
    - path: internal/database/pricegap_state.go
      provides: "GetPriceGapHistory reader"
      contains: "func (c *Client) GetPriceGapHistory"
    - path: internal/pricegaptrader/notify.go
      provides: "Broadcaster interface + PriceGapEvent + PriceGapCandidateUpdate types"
      contains: "type Broadcaster interface"
    - path: scripts/check-i18n-sync.sh
      provides: "i18n lockstep gate used by every Wave 2+ plan"
  key_links:
    - from: "internal/pricegaptrader/tracker.go"
      to: "internal/pricegaptrader/notify.go Broadcaster"
      via: "tracker struct field"
      pattern: "broadcaster\\s+Broadcaster"
    - from: "internal/database/pricegap_state.go AddPriceGapHistory"
      to: "internal/database/pricegap_state.go GetPriceGapHistory"
      via: "LRANGE on pg:history LIST key"
      pattern: "LRange\\(.*pg:history"
    - from: "internal/pricegaptrader/monitor.go closePair"
      to: "internal/pricegaptrader/types.go PriceGapPosition.{Modeled,Realized}SlipBps"
      via: "direct field assignment before LPUSH to pg:history"
      pattern: "pos\\.(Modeled|Realized)SlipBps\\s*="
---

<objective>
Phase 8 → Phase 9 bridge. Land the schema and DI extensions every downstream Phase 9 plan depends on, so Wave 2 plans can compile and execute independently.

Purpose: Phase 8 shipped without (a) a `Mode` field on `PriceGapPosition`, (b) the `PriceGapPaperMode` config flag, (c) a `GetPriceGapHistory` reader, (d) a `Broadcaster` DI seam the tracker can publish WS events through without importing `internal/api`, and (e) per-trade `ModeledSlipBps` + `RealizedSlipBps` persisted onto the position record at close (D-23 — Phase 8 only persisted the aggregated ratio used by the auto-disable gate; per-trade realized-vs-modeled comparison data was missing). This plan closes all five gaps in one atomic bridge so Wave 2 plans (paper-mode, WS broadcast, metrics, Telegram) can run in parallel against a stable contract. It also writes `scripts/check-i18n-sync.sh`, the lockstep gate every UI-touching plan will invoke.

Output: Extended model + config + store + tracker DI + slippage write-path + i18n gate script, all green under `go test ./... -count=1 -race`.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-CONTEXT.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-RESEARCH.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-UI-SPEC.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-VALIDATION.md

<interfaces>
<!-- Contracts this plan establishes for all downstream Phase 9 plans. -->
<!-- Executor MUST NOT deviate from these signatures; Wave 2 plans compile against them. -->

From internal/models/pricegap_position.go (extended here):
```go
type PriceGapPosition struct {
    // ... all existing fields ...
    Mode string `json:"mode"` // "paper" | "live" — stamped at entry in openPair, immutable. Default "live" on unmarshal of pre-Phase-9 records.
}
```

From internal/pricegaptrader/types.go (D-23 — may already carry other slippage fields; add these two if missing):
```go
type PriceGapPosition struct {
    // ... all existing fields ...
    ModeledSlipBps  float64 `json:"modeled_slip_bps"`  // expected-at-entry slippage the model predicted (one value, set at entry)
    RealizedSlipBps float64 `json:"realized_slip_bps"` // measured slippage at close (one value, set in closePair before LPUSH)
}
```

From internal/config/config.go (extended here):
```go
type Config struct {
    // ... existing PriceGap* fields ...
    PriceGapPaperMode bool `json:"price_gap_paper_mode"` // default TRUE — D-12 exception to default-OFF convention
}
```

From internal/database/pricegap_state.go (added here):
```go
// GetPriceGapHistory returns up to `limit` closed positions, newest first,
// skipping the first `offset`. Backs pg:history LIST (capped 500 per Phase 8 D-14).
func (c *Client) GetPriceGapHistory(offset, limit int) ([]*models.PriceGapPosition, error)
```

From internal/pricegaptrader/notify.go (new file):
```go
type PriceGapEvent struct {
    Type     string                    `json:"type"`   // "entry" | "exit" | "auto_disable"
    Position *models.PriceGapPosition  `json:"position,omitempty"`
    Symbol   string                    `json:"symbol,omitempty"`
    Reason   string                    `json:"reason,omitempty"`
}

type PriceGapCandidateUpdate struct {
    Symbol     string `json:"symbol"`
    Disabled   bool   `json:"disabled"`
    Reason     string `json:"reason,omitempty"`
    DisabledAt int64  `json:"disabled_at,omitempty"` // unix seconds
}

type Broadcaster interface {
    BroadcastPriceGapPositions(positions []*models.PriceGapPosition)
    BroadcastPriceGapEvent(evt PriceGapEvent)
    BroadcastPriceGapCandidateUpdate(update PriceGapCandidateUpdate)
}

// NoopBroadcaster is used when tracker runs without WS hub (tests, CLI).
type NoopBroadcaster struct{}
func (NoopBroadcaster) BroadcastPriceGapPositions([]*models.PriceGapPosition) {}
func (NoopBroadcaster) BroadcastPriceGapEvent(PriceGapEvent) {}
func (NoopBroadcaster) BroadcastPriceGapCandidateUpdate(PriceGapCandidateUpdate) {}
```

Tracker struct gains (in internal/pricegaptrader/tracker.go):
```go
broadcaster Broadcaster  // set via option; defaults to NoopBroadcaster{}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Position.Mode + PriceGapPaperMode config + GetPriceGapHistory reader</name>
  <files>internal/models/pricegap_position.go, internal/config/config.go, internal/database/pricegap_state.go, internal/models/pricegap_position_test.go, internal/config/config_test.go, internal/database/pricegap_state_test.go</files>
  <read_first>
    - internal/models/pricegap_position.go (current schema — confirm Mode absent before adding)
    - internal/config/config.go (confirm PriceGapPaperMode absent; locate Defaults() and applyJSON for PriceGap block)
    - internal/database/pricegap_state.go (confirm AddPriceGapHistory exists; locate pg:history LIST key and cap-500 trim logic)
    - internal/database/pricegap_state_test.go (mirror existing test pattern for LRange-based reads)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-RESEARCH.md § "Phase 8 Dependencies" + § "Pitfall 1" (PaperMode default TRUE — D-12 exception)
  </read_first>
  <behavior>
    - Test: PriceGapPosition with Mode="paper" round-trips through JSON marshal/unmarshal
    - Test: PriceGapPosition unmarshaled from a pre-Phase-9 JSON blob (no "mode" key) defaults Mode to "live"
    - Test: config.Defaults() sets PriceGapPaperMode=true (D-12)
    - Test: applyJSON over {"price_gap_paper_mode": false} flips the field to false
    - Test: GetPriceGapHistory(0, 10) returns newest-first up to 10 closed positions after AddPriceGapHistory calls
    - Test: GetPriceGapHistory(5, 10) skips the 5 newest entries
    - Test: GetPriceGapHistory(0, 0) returns empty slice without error
  </behavior>
  <action>
    1. In `internal/models/pricegap_position.go`: add field `Mode string `json:"mode"`` to `PriceGapPosition`. Add `UnmarshalJSON` (or post-unmarshal hook invoked from store on read) that defaults `Mode` to `"live"` when empty — implement as a helper `normalizeMode(p *PriceGapPosition)` called in the store's decode path; do NOT add UnmarshalJSON to the struct itself (avoid breaking existing tests that pass literals). Export `normalizeMode` for reuse by rehydrate.
    2. In `internal/config/config.go`: add field `PriceGapPaperMode bool `json:"price_gap_paper_mode"`` to `Config`. In `Defaults()` set `c.PriceGapPaperMode = true` with comment `// D-12 exception: paper default TRUE until live validated`. In the `jsonPriceGap` nested struct (the JSON shim for /api/config) add a matching `PaperMode *bool `json:"paper_mode,omitempty"`` field and wire it through `toJSON()` / `applyJSON()` exactly mirroring existing flags (e.g., `PriceGapEnabled`).
    3. In `internal/database/pricegap_state.go`: add `func (c *Client) GetPriceGapHistory(offset, limit int) ([]*models.PriceGapPosition, error)` that runs `c.redis.LRange(ctx, priceGapHistoryKey, int64(offset), int64(offset+limit-1)).Result()`, unmarshal each into `*PriceGapPosition`, call `models.NormalizeMode(p)` on each, return. If `limit <= 0`, return `nil, nil`. Reuse the exact `priceGapHistoryKey` constant already defined (verify name via grep `pg:history` in the file — use whatever constant name is already there, do NOT redeclare).
    4. Write the unit tests listed under <behavior>. Tests in internal/database must use the existing miniredis or redis-mock harness already in use in that file; copy the setup boilerplate from an existing test above.
  </action>
  <verify>
    <automated>go test ./internal/models/... ./internal/config/... ./internal/database/... -count=1 -race -run 'TestPriceGapPosition_Mode|TestConfig_PaperMode|TestGetPriceGapHistory'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Mode string .*json:\"mode\"" internal/models/pricegap_position.go` — field present
    - `grep -q "PriceGapPaperMode bool" internal/config/config.go` — field present
    - `grep -q "c.PriceGapPaperMode = true" internal/config/config.go` — default TRUE set in Defaults()
    - `grep -q "paper_mode" internal/config/config.go` — JSON tag present for /api/config round-trip
    - `grep -q "func (c \*Client) GetPriceGapHistory" internal/database/pricegap_state.go` — reader exists
    - `go build ./...` exits 0
    - `go test ./internal/models/... ./internal/config/... ./internal/database/... -count=1 -race` exits 0
    - `git diff internal/config/config.go | grep -qE '^\+.*paper_mode'` — JSON round-trip wired
    - NO modification to `config.json` (verified: `git status config.json` shows unchanged — the file is .gitignored and must never be touched by Claude)
  </acceptance_criteria>
  <done>
    Position.Mode, PriceGapPaperMode, GetPriceGapHistory all land with unit tests green. Defaults() sets paper=true (D-12). /api/config JSON round-trip wired but not yet connected to handler (Plan 02 wires dashboard handler).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Broadcaster DI seam + i18n sync gate script</name>
  <files>internal/pricegaptrader/notify.go, internal/pricegaptrader/tracker.go, internal/pricegaptrader/notify_test.go, scripts/check-i18n-sync.sh</files>
  <read_first>
    - internal/pricegaptrader/tracker.go (find Tracker struct definition + NewTracker signature / options)
    - internal/api/server.go L216–258 (Broadcast* pattern this interface mirrors; do NOT import this package here — module boundary)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-RESEARCH.md § "Pattern 1: WS Broadcast Extension"
    - web/src/i18n/en.ts (current key set — confirm no pricegap.* keys yet)
    - web/src/i18n/zh-TW.ts (mirror of en.ts)
  </read_first>
  <behavior>
    - Test: NoopBroadcaster methods do not panic and return immediately
    - Test: Tracker constructed without WithBroadcaster option uses NoopBroadcaster{} (no panic when entry/exit fires in other tests)
    - Test: Tracker with mock Broadcaster records calls when we directly invoke a future hook (hook wiring lands in Plan 06; this test just verifies the field is reachable)
    - Shell test: `bash scripts/check-i18n-sync.sh` exits 0 on current main (no pricegap keys yet), exits 1 if we add a key to en.ts only (temp test fixture).
  </behavior>
  <action>
    1. Create `internal/pricegaptrader/notify.go` with the exact types in the `<interfaces>` block: `PriceGapEvent`, `PriceGapCandidateUpdate`, `Broadcaster` interface, `NoopBroadcaster` struct + its three methods. Package `pricegaptrader`. NO import of `internal/api` or `internal/engine` or `internal/spotengine`.
    2. In `internal/pricegaptrader/tracker.go`: add field `broadcaster Broadcaster` to the `Tracker` struct. If there is a `NewTracker(...)` or `New(...)` constructor, add an optional setter `func WithBroadcaster(b Broadcaster) TrackerOption` following whatever option pattern exists; if none exists, add `func (t *Tracker) SetBroadcaster(b Broadcaster)`. Default the field to `NoopBroadcaster{}` in the constructor so nil never reaches downstream code.
    3. Create `internal/pricegaptrader/notify_test.go` asserting: NoopBroadcaster methods work; a `&mockBroadcaster{}` set on a fresh Tracker is reachable via a future hook (use the existing test-only accessor or add `(t *Tracker) broadcasterForTest() Broadcaster` behind a build tag if needed — prefer the minimal change: make `broadcaster` package-private and test in the same package).
    4. Create `scripts/check-i18n-sync.sh` (chmod +x) with shell:
       ```bash
       #!/usr/bin/env bash
       set -euo pipefail
       EN=$(mktemp); ZH=$(mktemp)
       trap 'rm -f "$EN" "$ZH"' EXIT
       grep -oE "^\s*['\"][a-zA-Z0-9_.]+['\"]\s*:" web/src/i18n/en.ts | sed -E "s/[[:space:]:'\"\"]//g" | sort -u > "$EN"
       grep -oE "^\s*['\"][a-zA-Z0-9_.]+['\"]\s*:" web/src/i18n/zh-TW.ts | sed -E "s/[[:space:]:'\"\"]//g" | sort -u > "$ZH"
       DIFF=$(diff "$EN" "$ZH" || true)
       if [[ -n "$DIFF" ]]; then
         echo "i18n key drift detected (en vs zh-TW):" >&2
         echo "$DIFF" >&2
         exit 1
       fi
       echo "i18n keys in sync: $(wc -l < "$EN") keys"
       ```
       The extraction pattern must tolerate the existing locale file format — adjust the grep anchor after reading 10 lines of `web/src/i18n/en.ts` to confirm whether keys are quoted with `'` or `"` and whether they are bare identifiers (`priceGap: {`). If keys are nested TS-style, swap the extractor for: `node -e "const m=require('./web/src/i18n/en.ts'); ..."` — but prefer pure-shell if format allows.
    5. If the locale files use nested TS objects rather than flat keys, the shell extractor must recurse — easiest: write a tiny Node one-liner or jq invocation that imports the TS file via `tsx` if available, else fallback to a line-based key listing that captures every `identifier:` at column >= 2. Favor a robust grep: `grep -oE "[a-zA-Z][a-zA-Z0-9_]*\s*:" web/src/i18n/en.ts | sort -u`. Confirm the script returns 0 on the current (pre-Phase-9) tree before landing.
  </action>
  <verify>
    <automated>go test ./internal/pricegaptrader/... -count=1 -race -run 'TestBroadcaster|TestNoopBroadcaster' && bash scripts/check-i18n-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/pricegaptrader/notify.go` — file exists
    - `grep -q "type Broadcaster interface" internal/pricegaptrader/notify.go`
    - `grep -q "NoopBroadcaster" internal/pricegaptrader/notify.go`
    - `grep -q "broadcaster Broadcaster" internal/pricegaptrader/tracker.go`
    - `! grep -q "internal/api" internal/pricegaptrader/notify.go` — module boundary preserved
    - `! grep -q "internal/engine" internal/pricegaptrader/notify.go`
    - `! grep -q "internal/spotengine" internal/pricegaptrader/notify.go`
    - `test -x scripts/check-i18n-sync.sh` — executable
    - `bash scripts/check-i18n-sync.sh` exits 0 on current tree (baseline)
    - `go test ./internal/pricegaptrader/... -count=1 -race` exits 0
  </acceptance_criteria>
  <done>
    Tracker has a Broadcaster DI field defaulting to Noop. New WS topics in Plan 06 will plug in via this seam. i18n sync gate is live and every Wave 2+ plan will call it in its verify block.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-23 slippage write-path — persist ModeledSlipBps + RealizedSlipBps on PriceGapPosition at close</name>
  <files>internal/pricegaptrader/types.go, internal/pricegaptrader/monitor.go, internal/pricegaptrader/slippage.go, internal/pricegap/slippage_write_path_test.go</files>
  <read_first>
    - internal/pricegaptrader/types.go (where PriceGapPosition lives; confirm whether ModeledSlipBps and/or RealizedSlipBps already present — grep first, add only what's missing)
    - internal/pricegaptrader/monitor.go (locate closePair — research points to ~L212; follow the path from exit-trigger → realized PnL computation → pg:history LPUSH)
    - internal/pricegaptrader/slippage.go (existing slippage module: the modeled value is computed at entry; confirm where it gets stored today — likely `pg:slippage:<sym>` aggregate only per Phase 8 D-14. This task relocates per-trade values onto the position record.)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-CONTEXT.md § D-23 (verbatim warning) + § D-24 (metrics data-source simplification once D-23 lands)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-RESEARCH.md § "Pattern: Slippage accounting"
  </read_first>
  <behavior>
    - Test TestSlippageWritePath_FieldsExist: `reflect.TypeOf(PriceGapPosition{})` has fields `ModeledSlipBps` (float64) and `RealizedSlipBps` (float64) with expected JSON tags `modeled_slip_bps` and `realized_slip_bps`.
    - Test TestSlippageWritePath_WrittenBeforeLPUSH: construct a tracker with stub exchange + miniredis. Open a position (modeled slippage computed at entry → must land on pos.ModeledSlipBps immediately). Close the position via closePair. After close, `GetPriceGapHistory(0, 1)[0]` has BOTH pos.ModeledSlipBps > 0 AND pos.RealizedSlipBps != 0 (signed value; direction is measured vs. modeled).
    - Test TestSlippageWritePath_PaperMode: in paper mode (Plan 03 synthesizes fills at mid±(modeled/2)), the synthesized close produces RealizedSlipBps equal to ModeledSlipBps (by construction of the synth) — asserts that the paper branch still populates the field so Plan 05 metrics work identically for paper and live.
    - Test TestSlippageWritePath_NoDoubleWrite: closing a position only writes the realized value once; a second close call (idempotency guard) does not overwrite or append a second history entry.
  </behavior>
  <action>
    1. **Inspect first** (`grep -nE 'ModeledSlipBps|RealizedSlipBps' internal/pricegaptrader/types.go`). If BOTH fields already exist with correct types + JSON tags, skip to step 3. If one or both missing, add to the `PriceGapPosition` struct in `types.go`:
       ```go
       ModeledSlipBps  float64 `json:"modeled_slip_bps"`  // D-23: modeled slippage at entry (one value per position)
       RealizedSlipBps float64 `json:"realized_slip_bps"` // D-23: measured slippage at close (one value per position)
       ```
       Field order: adjacent to existing pnl/entry-spread fields for locality. Keep JSON tags snake_case to match the rest of the struct. If `internal/models/pricegap_position.go` is the canonical location instead of `internal/pricegaptrader/types.go`, add the fields there — the read_first inspection determines the owner.
    2. **Modeled write at entry** (`internal/pricegaptrader/execution.go` openPair path OR `slippage.go` if a pre-entry slippage computation already exists): at the point where the modeled slippage is computed (pre-entry gate — already happens per Phase 8 D-14 to feed the auto-disable ratio), ALSO assign it onto `pos.ModeledSlipBps` before the position is persisted. If the modeled value is currently only stashed into a local variable and pushed to `pg:slippage:<sym>`, add the one-line `pos.ModeledSlipBps = modeled` before `db.PutActivePriceGapPosition(pos)`.
    3. **Realized write at close** (`internal/pricegaptrader/monitor.go` closePair, per research ~L212): at the point where realized PnL + realized slippage are computed (after fills settle, before the `LPUSH pg:history`), add:
       ```go
       pos.RealizedSlipBps = realizedSlipBps  // whatever variable currently holds the measured-vs-expected delta
       ```
       If closePair does not currently compute realized slippage (Phase 8 may have computed it only for the aggregate ratio and discarded it), compute it here as: `realizedSlipBps = measured_effective_spread_bps - entry_spread_bps` (or equivalent formula from `slippage.go`). Reuse existing helpers in `slippage.go` rather than inlining math.
    4. **Paper-mode synth coupling** (cross-reference to Plan 03 Pitfall 7): when the close path is the synthesized paper close (fills at `mid ± modeled/2`), the realized value by construction equals the modeled value. Plan 03 implements the synth; this plan guarantees the schema + write-order so Plan 03 just assigns `pos.RealizedSlipBps = pos.ModeledSlipBps` at the synth point. Document this coupling in a comment above the realized-write line: `// Plan 03 paper synth assigns RealizedSlipBps := ModeledSlipBps here; live path assigns measured value.`
    5. **D-24 simplification note**: because per-trade modeled + realized now live on `PriceGapPosition` persisted via `pg:history`, Plan 05 metrics do NOT need to read `pg:slippage:*` — all required data is in the history list. Record this explicitly at the top of `types.go` as a comment block so Plan 05's executor sees it: `// D-23+D-24: Plan 05 rolling metrics compute realized-vs-modeled directly from pg:history via these fields. No Phase 8 pg:slippage:* reads needed.`
    6. **Idempotency**: closePair must not write the realized field twice. Inspect existing guards (likely a `pos.ClosedAt != time.Time{}` check or `exitActive[posID]` deletion before close). Add an early-return or an `if pos.RealizedSlipBps == 0` guard before the LPUSH section if the existing guard is insufficient.
    7. Write the test file `internal/pricegap/slippage_write_path_test.go` (new subpackage `internal/pricegap` for the integration test — keeps it out of the `pricegaptrader` package if that package has heavy test setup; or place it next to `monitor_test.go` if easier — the test path target is `go test ./internal/pricegap/... -run TestSlippageWritePath`). Use miniredis + stub exchange + mock broadcaster/notifier. Cover all 4 cases in <behavior>.
  </action>
  <verify>
    <automated>go test ./internal/pricegap/... ./internal/pricegaptrader/... -count=1 -race -run 'TestSlippageWritePath'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'ModeledSlipBps' internal/pricegaptrader/types.go` exits 0 (or the canonical types file identified in step 1) — field present
    - `grep -q 'RealizedSlipBps' internal/pricegaptrader/types.go` exits 0 — field present
    - `grep -qE 'pos\.(Modeled|Realized)SlipBps\s*=' internal/pricegaptrader/monitor.go` exits 0 — both fields written in closePair before LPUSH. (If ModeledSlipBps is written in execution.go at entry rather than monitor.go at close, the grep must match in execution.go instead — document which in the task summary.)
    - `grep -qE 'pos\.ModeledSlipBps\s*=' internal/pricegaptrader/execution.go` exits 0 (modeled written at entry) OR the same assignment appears in monitor.go pre-close recovery path
    - `go test ./internal/pricegap/... -run TestSlippageWritePath` exits 0
    - `go test ./internal/pricegaptrader/... -count=1 -race` exits 0 (no regression in existing tracker tests)
    - Write order verified by test: after closePair, `GetPriceGapHistory(0, 1)[0]` has both fields non-zero (modeled set at entry, realized set at close)
    - Idempotency verified: double-close does not produce a second history entry
  </acceptance_criteria>
  <done>
    D-23 warning resolved at the schema layer: per-trade modeled + realized slippage persisted on every closed position. D-24 simplification unlocked — Plan 05 metrics read only pg:history (no pg:slippage:* dependency). Plan 03's paper synth has a clear contract for how to populate RealizedSlipBps.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json writer | Only /api/config handler may write; plan surfaces the new field but does not expose it until Plan 02 |
| Redis pg:history reader | New read path; no write expansion in this plan |
| Module boundary | internal/pricegaptrader MUST NOT import internal/api, internal/engine, internal/spotengine |
| Position schema expansion (D-23) | Adding persisted fields is backward-compatible: pre-Phase-9 records unmarshal with zero values and normalize-on-read covers Mode; Modeled/Realized default to 0.0 and are ignored by Plan 05 metrics when zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | internal/config/config.go PriceGapPaperMode default | medium | mitigate | Default TRUE (D-12) prevents accidental live trading on first boot after Phase 9 deploy; acceptance criteria verifies `c.PriceGapPaperMode = true` in Defaults() |
| T-09-02 | Information Disclosure | GetPriceGapHistory serialization | low | mitigate | PriceGapPosition already omits credentials/keys; new Mode and SlipBps fields are public by design. Verified via grep that struct contains no `*Key`, `*Secret`, `*Token` fields. |
| T-09-03 | Spoofing | Broadcaster DI nil deref | medium | mitigate | NoopBroadcaster default prevents nil-panic in tests + CLI paths; acceptance criteria enforces NoopBroadcaster presence |
| T-09-04 | Tampering | Module boundary leak (internal/pricegaptrader → internal/api) | high | mitigate | Broadcaster defined as interface in pricegaptrader package; internal/api implements it, not vice versa. Acceptance criteria greps for forbidden imports. |
| T-09-05 | Elevation | Writing config.json directly | high | mitigate | This plan adds the FIELD; it does NOT write config.json. Acceptance criteria verifies `git status config.json` unchanged. Plan 02 adds the auth-gated handler. |
| T-09-06 | Repudiation | Missing per-trade slippage record prevents realized-vs-modeled comparison (D-23 warning) | high | mitigate | Task 3 persists both values onto PriceGapPosition at close; test TestSlippageWritePath asserts both fields on every history row; Plan 05 metrics read directly from this data |
</threat_model>

<verification>
- `go build ./...` — exits 0
- `go test ./... -count=1 -race -short` — exits 0
- `grep -q "Mode string" internal/models/pricegap_position.go`
- `grep -q "ModeledSlipBps" internal/pricegaptrader/types.go` (D-23 field present)
- `grep -q "RealizedSlipBps" internal/pricegaptrader/types.go` (D-23 field present)
- `grep -qE 'pos\.(Modeled|Realized)SlipBps\s*=' internal/pricegaptrader/monitor.go` (D-23 write-path)
- `grep -q "PriceGapPaperMode" internal/config/config.go`
- `grep -q "GetPriceGapHistory" internal/database/pricegap_state.go`
- `grep -q "type Broadcaster interface" internal/pricegaptrader/notify.go`
- `bash scripts/check-i18n-sync.sh` — exits 0 on current tree
- `! grep -qE "internal/(api|engine|spotengine)" internal/pricegaptrader/notify.go` — boundary preserved
- `git status config.json` — file unchanged
</verification>

<success_criteria>
Bridge complete: every downstream Phase 9 plan compiles against these interfaces without further coordination. All tests green. No behavior change at runtime — Tracker still runs identically because NoopBroadcaster swallows events and PaperMode default=true only affects Plan 03 branch (not yet wired). D-23 resolved: per-trade modeled + realized slippage persists on every closed position; Plan 05 metrics have all data they need from pg:history alone (D-24 simplification).
</success_criteria>

<output>
After completion, create `.planning/phases/09-price-gap-dashboard-paper-live-operations/09-01-SUMMARY.md` listing: field additions (Mode, ModeledSlipBps, RealizedSlipBps), new interfaces, new file paths, test counts, D-23 write-path locations (file:line for modeled-at-entry and realized-at-close), any deviations from the <interfaces> block.
</output>
