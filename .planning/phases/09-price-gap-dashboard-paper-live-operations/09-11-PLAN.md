---
phase: 09-price-gap-dashboard-paper-live-operations
plan: 11
type: execute
wave: 2
depends_on: [09, 10]
files_modified:
  - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT-REWALK.md
autonomous: false
gap_closure: true
requirements: [PG-OPS-01, PG-OPS-02, PG-OPS-03, PG-OPS-05, PG-VAL-01, PG-VAL-02]
must_haves:
  truths:
    - "Full 14-row UAT walk signed off on a live dev host with aggressive thresholds, after Gap #1 + Gap #2 fixes are deployed"
    - "PG-OPS-01 Disable/Re-enable modal Redis parity verified interactively (dashboard ↔ redis-cli ↔ pg-admin CLI all agree)"
    - "Accessibility: Esc closes both Disable and Re-enable modals without submit; role=dialog present on both"
  artifacts:
    - path: .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT-REWALK.md
      provides: "Walkthrough log with pass/fail per row, timestamps, screenshots where relevant, redis-cli output for PG-OPS-01 parity check, DevTools notes for a11y"
      min_lines: 60
  key_links:
    - from: "09-UAT.md 14 blocked/deferred rows"
      to: "09-UAT-REWALK.md pass-state after Gap #1 + #2 deploy"
      via: "live dev-host walkthrough at aggressive PriceGapSpreadThresholdBps=20"
      pattern: "PASS"
---

<objective>
Carry the three human-only verification items from 09-VERIFICATION.md human_verification block
through to a signed-off state now that Gap #1 (09-09) and Gap #2 (09-10) have landed. This plan
is a CHECKPOINT-only plan: no code changes, only a structured human walkthrough with operator
notes.

Purpose: Phase 9 cannot be marked verified until the 14 UAT rows blocked by the two gaps are
walked live. This plan is the explicit gate for that walkthrough.

Output: 09-UAT-REWALK.md with pass/fail per row, redis-cli parity evidence, DevTools a11y
confirmation.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-VERIFICATION.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-09-PLAN.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-10-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full 14-row UAT walkthrough on live dev host</name>
  <what-built>
    09-09 and 09-10 together unblock live-detection observability:
    - 09-09: PriceGapDebugLog flag ON surfaces non-fire reasons in logs, rate-limited
    - 09-10: Tracker.Start() subscribes every candidate on both legs; 15s BBO liveness assertion warns if any leg is silent
  </what-built>
  <how-to-verify>
    Deploy current build to a dev host:
      1. `make build` (frontend → go binary, per go:embed order)
      2. Set one candidate's spread threshold aggressively — suggested: `PriceGapSpreadThresholdBps=20` for a liquid candidate (SOON or VICUSDT)
      3. Set `PriceGapDebugLog=true` via dashboard toggle (validates 09-09 dashboard wiring)
      4. Set `PriceGapPaperMode=true` so no capital is at risk
      5. `./arb` — tail logs
      6. Within 30s, confirm:
         - `pricegap: subscribed <SYM>@<exch> ...` for each candidate × 2 legs (09-10 Task 1)
         - `pricegap: BBO live <SYM>@<exch> ...` for each candidate × 2 legs within 15s (09-10 Task 2)
         - `pricegap: no-fire <SYM> reason=...` lines appearing for non-firing candidates (09-09 Task 2)
      7. Wait for a detection to fire (may take 5-30 minutes at 20 bps threshold)
      8. Walk all 14 blocked/deferred rows from 09-UAT.md:
         - Live Positions table populates with Mode=paper row
         - Entry spread, current spread, hold time, PnL update via WebSocket
         - Closed Log (on position close) shows Modeled bps, Realized bps, Delta
         - Telegram channel receives `📝 PAPER` prefixed entry alert
         - Telegram receives exit alert on close
         - Rolling Metrics section populates with real bps/day row for the fired candidate
         - PG-OPS-04 paper-mode toggle in header flips pill violet/green and persists across refresh
         - PG-VAL-01: Realized > 0 on Closed Log row
         - PG-VAL-02: 24h/7d/30d columns populate for the active candidate; zero-padded rows visible for others
      9. Record each row as PASS/FAIL with timestamp + log snippet or screenshot reference
    Write results to `.planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT-REWALK.md`.
    Return after all 14 rows are either PASS or have a blocking reason documented.
  </how-to-verify>
  <resume-signal>Type "approved" if all 14 rows PASS; otherwise describe which rows failed and the observed log output so we can plan further gap-closure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PG-OPS-01 Disable/Re-enable modal Redis parity walkthrough</name>
  <what-built>
    Phase 9 shipped the Disable/Re-enable modals (Plan 07) backed by /api/pricegap/candidate/*
    endpoints (Plan 02), writing to Redis key pg:candidate:disabled:&lt;SYM&gt; with JSON payload
    {reason, disabled_at}. pg-admin CLI (Phase 8 Plan 08-08) is the alternate writer.
    This test verifies single-writer parity between the dashboard and the CLI.
  </what-built>
  <how-to-verify>
    On the live dev host:
      1. Open dashboard → Price-Gap tab
      2. Pick an ENABLED candidate, click "Disable" — enter reason "UAT parity test", confirm
      3. `redis-cli -n 2 GET pg:candidate:disabled:<SYM>` — expect JSON string containing "UAT parity test" + an ISO8601 disabled_at
      4. Click "Re-enable" on the same row, confirm
      5. `redis-cli -n 2 GET pg:candidate:disabled:<SYM>` — expect (nil)
      6. Run `./pg-admin disable <SYM> --reason "CLI test"` at the shell
      7. Refresh dashboard — expect the row to show Disabled state with reason "CLI test"
      8. Run `./pg-admin enable <SYM>` — refresh dashboard — expect Enabled state
    Append results + redis-cli output to `09-UAT-REWALK.md` under a "PG-OPS-01 Parity" section.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps produce the expected state transitions; otherwise describe the divergence.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Accessibility — Esc close + role=dialog on Disable and Re-enable modals</name>
  <what-built>Plan 07 shipped both modals reusing the Positions.tsx modal shell (L602 per UI-SPEC D-14).</what-built>
  <how-to-verify>
    In a browser with DevTools open:
      1. Price-Gap tab → click Disable on any candidate → modal appears
      2. In DevTools Elements panel, locate the modal root → confirm `role="dialog"` attribute is present
      3. Press Esc → modal must close without submitting (the row must remain Enabled in the table)
      4. Click Re-enable on a disabled candidate → repeat steps 2-3
      5. Reopen the Disable modal, click the Cancel button → confirm same no-submit behavior as Esc
    Append a short section to `09-UAT-REWALK.md` with: DevTools screenshot reference OR a one-line confirmation of role=dialog presence, and a pass/fail for Esc behavior on each modal.
  </how-to-verify>
  <resume-signal>Type "approved" if role=dialog is present on both modals and Esc closes both without submitting; otherwise describe what was missing.</resume-signal>
</task>

</tasks>

<verification>
- 09-UAT-REWALK.md exists with 14-row UAT table, PG-OPS-01 parity section, a11y section
- All rows marked PASS or with a documented blocking reason (which would trigger yet another gap-closure plan — NOT expected after Gap #1 + #2 land)
- Phase 9 re-verification (`/gsd-verify-phase 09`) should return `verified` once this plan completes
</verification>

<success_criteria>
- 14/14 UAT rows PASS on live dev host
- Dashboard ↔ redis-cli ↔ pg-admin parity confirmed
- role=dialog + Esc close confirmed on both modals
- 09-UAT-REWALK.md committed as evidence
</success_criteria>

<output>
After checkpoint completion, write `.planning/phases/09-price-gap-dashboard-paper-live-operations/09-UAT-REWALK.md`
with the walkthrough evidence.
</output>
