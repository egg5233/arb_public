---
phase: 09-price-gap-dashboard-paper-live-operations
plan: 05
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - internal/pricegaptrader/metrics.go
  - internal/pricegaptrader/metrics_test.go
  - internal/api/pricegap_handlers.go
  - internal/api/pricegap_handlers_test.go
autonomous: true
requirements: [PG-OPS-05, PG-VAL-01, PG-VAL-02]
must_haves:
  truths:
    - "ComputeCandidateMetrics is a pure function: input is []*PriceGapPosition (closed history) + now time, output is []CandidateMetrics"
    - "Metrics rows include Candidate, Symbol, TradesWindow, WinPct, AvgRealizedBps, Bps24hPerDay, Bps7dPerDay, Bps30dPerDay"
    - "Rolling windows computed on-demand from pg:history — no background rollup, no new Redis writes (D-24)"
    - "Required data (modeled + realized slippage, realized PnL, notional, closed_at) is ALL on PriceGapPosition after Plan 01 Task 3 lands D-23 — no pg:slippage:* read needed"
    - "bps/day = sum(pnl_bps within window) / window_days; zero-trade candidates emit a row with zero values rather than being omitted (UI-SPEC: '-' in empty cells but row still reads)"
    - "GET /api/pricegap/metrics returns metrics sorted 30d-bps-per-day descending"
    - "Candidate key format: '<symbol>_<long>_<short>' to disambiguate same-symbol pairs across exchange combinations"
  artifacts:
    - path: internal/pricegaptrader/metrics.go
      provides: "ComputeCandidateMetrics pure function + CandidateMetrics type"
      contains: "func ComputeCandidateMetrics"
    - path: internal/api/pricegap_handlers.go
      provides: "handlePriceGapMetrics (real impl replacing Plan 02 stub) + handlePriceGapState metrics wiring"
      contains: "ComputeCandidateMetrics"
  key_links:
    - from: "internal/api/pricegap_handlers.go handlePriceGapMetrics"
      to: "internal/pricegaptrader/metrics.go ComputeCandidateMetrics"
      via: "direct function call; pure, no state"
      pattern: "pricegaptrader\\.ComputeCandidateMetrics"
    - from: "internal/api/pricegap_handlers.go handlePriceGapState"
      to: "same ComputeCandidateMetrics"
      via: "metrics field in aggregate response"
      pattern: "\"metrics\"\\s*:"
---

<objective>
Implement PG-VAL-02: on-demand rolling metrics aggregator. Pure function reads `pg:history` (already populated by Phase 8 + Plan 03 paper mode) and computes per-candidate 24h / 7d / 30d bps/day plus win% and avg realized bps. No new Redis write paths (D-24).

Purpose: Operators decide promote/demote of candidates manually based on rolling performance. PG-VAL-01 data (realized vs modeled slippage) is persisted on `PriceGapPosition` by Plan 01 Task 3 (D-23 write-path fix); this plan turns it into a queryable view. After the bridge fix, `pg:history` contains every field the metrics need — no `pg:slippage:*` read required (D-24 simplification).

Output: Pure aggregator + handler wiring (replaces Plan 02 stub) + tests.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-CONTEXT.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-RESEARCH.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-01-PLAN.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-02-PLAN.md

<interfaces>
```go
// internal/pricegaptrader/metrics.go
package pricegaptrader

type CandidateMetrics struct {
    Candidate      string  `json:"candidate"`         // "<symbol>_<long>_<short>"
    Symbol         string  `json:"symbol"`
    LongExchange   string  `json:"long_exchange"`
    ShortExchange  string  `json:"short_exchange"`
    TradesWindow   int     `json:"trades_window"`     // count within widest window (30d)
    WinPct         float64 `json:"win_pct"`           // 0..100
    AvgRealizedBps float64 `json:"avg_realized_bps"`
    Bps24hPerDay   float64 `json:"bps_24h_per_day"`
    Bps7dPerDay    float64 `json:"bps_7d_per_day"`
    Bps30dPerDay   float64 `json:"bps_30d_per_day"`
}

// ComputeCandidateMetrics groups closed positions by (symbol, long, short),
// computes rolling bps/day windows using ClosedAt as the sort key.
// Pure: no Redis, no network, no time.Now() — `now` is a parameter for testability.
// closedPositions should be newest-first (output shape of GetPriceGapHistory).
func ComputeCandidateMetrics(closed []*models.PriceGapPosition, now time.Time) []CandidateMetrics
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComputeCandidateMetrics pure-function aggregator</name>
  <files>internal/pricegaptrader/metrics.go, internal/pricegaptrader/metrics_test.go</files>
  <read_first>
    - internal/models/pricegap_position.go (fields: Symbol, LongExchange, ShortExchange, NotionalUSDT, RealizedPnL, ClosedAt)
    - internal/pricegaptrader/types.go (fields added by Plan 01 Task 3: ModeledSlipBps, RealizedSlipBps — these are on every history entry after bridge lands)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-01-PLAN.md § Task 3 (D-23 write-path — confirms this plan reads from pg:history only, no pg:slippage:* dependency per D-24)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-RESEARCH.md § "Pattern 4: On-Demand Metrics Aggregation"
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UI-SPEC.md § "Rolling Metrics table (D-25)"
  </read_first>
  <behavior>
    - Test TestComputeCandidateMetrics_Empty: empty input → returns empty slice (not nil panic)
    - Test TestComputeCandidateMetrics_SingleCandidate: 5 trades on BTCUSDT (Binance/Bybit), varied closed_at, realized_bps=[10,20,-5,15,8] → one row with WinPct=80 (4/5 positive) and AvgRealizedBps=9.6
    - Test TestComputeCandidateMetrics_WindowBoundary_24h: trades at now-1h, now-12h, now-25h → only first two counted in 24h window
    - Test TestComputeCandidateMetrics_WindowBoundary_7d: trade at now-6d23h counts; trade at now-7d1h does not
    - Test TestComputeCandidateMetrics_WindowBoundary_30d: trade at now-29d counts; trade at now-31d does not
    - Test TestComputeCandidateMetrics_BpsPerDayFormula: total_bps within 7d = 140, expected Bps7dPerDay = 140/7 = 20.0
    - Test TestComputeCandidateMetrics_MultipleCandidates: BTC+ETH+SOL input → 3 rows
    - Test TestComputeCandidateMetrics_CandidateKey: same symbol on different exchange pairs produces distinct rows keyed by "<sym>_<long>_<short>"
    - Test TestComputeCandidateMetrics_NoNotionalNoPanic: NotionalUSDT=0 → skip bps computation for that trade, do not divide by zero
    - Test TestComputeCandidateMetrics_ZeroTradesRow: if caller wants per-candidate row even with zero trades (see handler), the PURE function does NOT emit those — the handler pads against the config candidate list (Task 2). This test asserts ComputeCandidateMetrics only emits rows for candidates that have at least one closed trade.
    - Test TestComputeCandidateMetrics_HistoryOnlyDataSource: fixture contains 5 closed positions each with non-zero ModeledSlipBps + RealizedSlipBps + RealizedPnL + ClosedAt + NotionalUSDT; assert resulting metrics include non-zero Bps7dPerDay computed from these fields ALONE (no other data source touched). Proves D-24 simplification: pg:history is sufficient.
  </behavior>
  <action>
    1. Create `internal/pricegaptrader/metrics.go` with `CandidateMetrics` struct and `ComputeCandidateMetrics` function per <interfaces>.
    2. Implementation:
       - Group `closed` by key `pos.Symbol + "_" + pos.LongExchange + "_" + pos.ShortExchange`.
       - For each group, iterate trades:
         - Skip if `pos.ClosedAt.IsZero()` or `pos.NotionalUSDT == 0`.
         - Compute `tradeBps = pos.RealizedPnL / pos.NotionalUSDT * 10000`.
         - Classify by window: `age := now.Sub(pos.ClosedAt)`. If `age <= 24h` add to 24h bucket; if `age <= 7d` add to 7d bucket; if `age <= 30d` add to 30d bucket. (Windows are cumulative: a 1h-old trade contributes to all three.)
         - Track total realized_bps for AvgRealizedBps; count wins (`pos.RealizedPnL > 0`).
       - After iteration, compute: `Bps24hPerDay = sum24h / 1.0; Bps7dPerDay = sum7d / 7.0; Bps30dPerDay = sum30d / 30.0`. WinPct = wins / total * 100.
       - Return slice sorted by `Bps30dPerDay` descending (matches UI-SPEC default sort).
       - DO NOT touch `pg:slippage:*` or any other Redis key. All fields used by this function (RealizedPnL, NotionalUSDT, ClosedAt, ModeledSlipBps, RealizedSlipBps if needed for future columns) are on PriceGapPosition after Plan 01 Task 3. This honors D-24.
    3. Create `internal/pricegaptrader/metrics_test.go`. Build `time.Time` fixtures relative to a fixed `now = time.Date(2026, 4, 22, 12, 0, 0, 0, time.UTC)` so tests are deterministic.
    4. NO Redis, NO time.Now(), NO I/O in the function. Pure. This keeps it trivially testable and safe to call from any handler.
  </action>
  <verify>
    <automated>go test ./internal/pricegaptrader/... -count=1 -race -run 'TestComputeCandidateMetrics'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/pricegaptrader/metrics.go`
    - `grep -q "func ComputeCandidateMetrics" internal/pricegaptrader/metrics.go`
    - `grep -q "CandidateMetrics struct" internal/pricegaptrader/metrics.go`
    - `! grep -q "time.Now()" internal/pricegaptrader/metrics.go` — pure function, now passed as parameter
    - `! grep -q "redis\." internal/pricegaptrader/metrics.go` — no Redis access
    - `! grep -q "pg:slippage" internal/pricegaptrader/metrics.go` — D-24 simplification: pg:history is the sole source after Plan 01 Task 3
    - `! grep -qE "internal/(api|engine|spotengine|database)" internal/pricegaptrader/metrics.go` — module boundary preserved
    - `go test ./internal/pricegaptrader/... -count=1 -race` exits 0
    - TestComputeCandidateMetrics_HistoryOnlyDataSource passes — proves non-zero Bps7dPerDay computable from pg:history PriceGapPosition fields alone (no pg:slippage:* read)
  </acceptance_criteria>
  <done>
    Pure rolling-metrics aggregator ready to call from any handler. Deterministic, testable, no side effects. D-24 honored: single data source (pg:history) because D-23 bridge landed the needed per-trade fields on PriceGapPosition.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ComputeCandidateMetrics into /api/pricegap/state + /api/pricegap/metrics</name>
  <files>internal/api/pricegap_handlers.go, internal/api/pricegap_handlers_test.go</files>
  <read_first>
    - internal/api/pricegap_handlers.go (Plan 02's handlePriceGapState and handlePriceGapMetrics stubs)
    - internal/pricegaptrader/metrics.go (Task 1 above)
    - internal/database/pricegap_state.go (GetPriceGapHistory signature from Plan 01)
  </read_first>
  <behavior>
    - Test TestHandlePriceGapMetrics_Empty: empty pg:history → 200 with Data=[]
    - Test TestHandlePriceGapMetrics_Sorted30dDesc: 3 candidates with Bps30dPerDay values [5, 20, 10] → response order [20, 10, 5]
    - Test TestHandlePriceGapMetrics_PaddedForConfigCandidates: cfg.PriceGapCandidates has BTCUSDT + ETHUSDT + SOLUSDT; pg:history has trades for BTCUSDT only → response includes all 3 candidates, ETHUSDT+SOLUSDT rows have TradesWindow=0 and zero bps values (UI-SPEC empty-cell rule)
    - Test TestHandlePriceGapState_IncludesMetrics: GET /api/pricegap/state → Data.metrics is populated (not empty stub)
  </behavior>
  <action>
    1. In `handlePriceGapMetrics` (currently stub from Plan 02), replace body:
       ```go
       history, err := s.db.GetPriceGapHistory(0, 500) // full window
       if err != nil { writeJSON(w, 500, Response{Error: err.Error()}); return }
       computed := pricegaptrader.ComputeCandidateMetrics(history, time.Now())
       // Pad with zero-trade rows for every configured candidate that has no history yet
       out := padWithConfigCandidates(computed, s.cfg.PriceGapCandidates)
       writeJSON(w, 200, Response{OK: true, Data: out})
       ```
    2. In `handlePriceGapState`, replace the `metrics: []CandidateMetrics{}` stub with the same computed+padded call. Share the logic via a private helper `(s *Server) computeMetricsForResponse() ([]CandidateMetrics, error)` so both handlers call the same code.
    3. Implement `padWithConfigCandidates(computed []CandidateMetrics, configured []config.PriceGapCandidate) []CandidateMetrics`: build a set of keys present in `computed`; for each configured candidate with key not in set, append a zero-filled `CandidateMetrics{Candidate: key, Symbol: c.Symbol, LongExchange: c.Long, ShortExchange: c.Short}`. Re-sort final slice by `Bps30dPerDay` desc so configured candidates with zero activity land at the bottom.
    4. Write the 4 tests. Reuse the handler test harness from Plan 02.
  </action>
  <verify>
    <automated>go test ./internal/api/... -count=1 -race -run 'TestHandlePriceGapMetrics|TestHandlePriceGapState_IncludesMetrics'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pricegaptrader.ComputeCandidateMetrics" internal/api/pricegap_handlers.go`
    - `grep -q "padWithConfigCandidates" internal/api/pricegap_handlers.go`
    - `grep -q "s.db.GetPriceGapHistory(0, 500)" internal/api/pricegap_handlers.go` OR equivalent full-range read
    - `! grep -q "pg:slippage" internal/api/pricegap_handlers.go` — handler reads pg:history only (D-24)
    - `go test ./internal/api/... -count=1 -race` exits 0
    - Regression: Plan 02's existing tests still green
  </acceptance_criteria>
  <done>
    Metrics endpoint returns full 30d-desc-sorted rows including zero-activity configured candidates. State aggregate returns same metrics in its `metrics` field. pg:history is the single source of truth (D-24 honored).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis history → computation | Untrusted volume; capped at 500 rows (Phase 8 D-14) so DoS via pg:history flood is bounded |
| Handler → client | Metrics payload contains no secrets; existing auth chain sufficient |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-22 | Denial of Service | Aggregator over unbounded pg:history | low | mitigate | pg:history LIST capped at 500 by Phase 8; handler reads at most 500 |
| T-09-23 | Information Disclosure | Metrics leak modeled-vs-realized that could inform external actors | low | accept | Dashboard is operator-only (bearer auth); metrics are business-internal. No PII. |
| T-09-24 | Tampering | Divide-by-zero on Notional=0 | low | mitigate | Pure function skips entries with NotionalUSDT == 0; test covers |
</threat_model>

<verification>
- `go test ./internal/pricegaptrader/... ./internal/api/... -count=1 -race` exits 0
- `go build ./...` exits 0
- Pure function has no time.Now() or Redis imports
- Pure function has no pg:slippage:* reference (D-24)
</verification>

<success_criteria>
Rolling metrics available via REST. Pure function is trivially testable. Zero new write paths — D-24 honored. Single data source — pg:history (enabled by Plan 01 Task 3 D-23 fix). Handlers pad with config candidates so the UI table has a row per configured pair.
</success_criteria>

<output>
After completion, create `.planning/phases/09-price-gap-dashboard-paper-live-operations/09-05-SUMMARY.md` listing: CandidateMetrics schema, window-boundary semantics, padding behavior, confirmation that pg:slippage:* was not read.
</output>
