---
phase: 09-price-gap-dashboard-paper-live-operations
plan: 07
type: execute
wave: 4
depends_on: [02, 05, 06]
files_modified:
  - web/src/App.tsx
  - web/src/pages/PriceGap.tsx
  - web/src/i18n/en.ts
  - web/src/i18n/zh-TW.ts
autonomous: true
requirements: [PG-OPS-01, PG-OPS-02, PG-OPS-03, PG-VAL-02]
must_haves:
  truths:
    - "A new 'Price-Gap' page renders between 'spot-positions' and 'history' in the nav"
    - "Page contains 5 sections in order: Header (toggles + stat strip), Candidates, Live Positions, Closed Log, Rolling Metrics (D-01)"
    - "Master enable + Paper toggle in header POST to /api/config and round-trip persist to config.json"
    - "Candidate rows render per D-13: disabled rows show yellow tint, reason + age, inline Re-enable; enabled rows show Disable button"
    - "Re-enable and Disable both go through a confirmation modal using the exact shell from Positions.tsx L602 (D-14)"
    - "Live Positions subscribes to WS pg_positions + pg_event topics; REST seeds on mount"
    - "Closed Log paginated 100 rows with 'Load 100 more'; cap 500"
    - "PAPER badge uses violet-400; soft violet-500/5 row tint applied to paper rows"
    - "Every new pricegap.* key present in BOTH web/src/i18n/en.ts AND web/src/i18n/zh-TW.ts"
    - "(cd web && npm run build) exits 0 (go:embed build order honored)"
  artifacts:
    - path: web/src/pages/PriceGap.tsx
      provides: "Price-Gap dashboard page — single file until >600 lines per UI-SPEC"
      min_lines: 300
    - path: web/src/App.tsx
      provides: "Page union + nav registration + renderPage case for 'price-gap'"
      contains: "'price-gap'"
    - path: web/src/i18n/en.ts
      provides: "All pricegap.* keys"
      contains: "pricegap.pageTitle"
    - path: web/src/i18n/zh-TW.ts
      provides: "Lockstep mirror of pricegap.* keys"
      contains: "pricegap.pageTitle"
  key_links:
    - from: "web/src/pages/PriceGap.tsx"
      to: "GET /api/pricegap/state"
      via: "useEffect fetch on mount"
      pattern: "/api/pricegap/state"
    - from: "web/src/pages/PriceGap.tsx"
      to: "WS topics pg_positions, pg_event, pg_candidate_update"
      via: "useWebSocket handler (reuse Positions.tsx pattern)"
      pattern: "pg_positions|pg_event|pg_candidate_update"
---

<objective>
Build the Price-Gap dashboard page per 09-UI-SPEC.md, verbatim. Translate the approved SPEC into React/Tailwind code without re-designing. Every token, class name, copy string, and section order comes from the SPEC. Every i18n key lands in both locale files in lockstep.

Purpose: This is the operator's entire window into the price-gap tracker. The SPEC is locked; this plan is translation, not design.

Output: web/src/pages/PriceGap.tsx (single file), App.tsx nav wiring, both i18n files extended, frontend build green.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-CONTEXT.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-UI-SPEC.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-02-PLAN.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-05-PLAN.md
@.planning/phases/09-price-gap-dashboard-paper-live-operations/09-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n lockstep — add all pricegap.* keys to en.ts AND zh-TW.ts</name>
  <files>web/src/i18n/en.ts, web/src/i18n/zh-TW.ts</files>
  <read_first>
    - web/src/i18n/en.ts (current structure — flat vs nested, quote style)
    - web/src/i18n/zh-TW.ts (mirror)
    - web/src/i18n/index.ts (TranslationKey derivation + fallback behavior)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UI-SPEC.md § "Copywriting Contract" (exhaustive key list — copy VERBATIM)
    - scripts/check-i18n-sync.sh (from Plan 01 — confirm current sync, then confirm after adds)
  </read_first>
  <action>
    1. Open 09-UI-SPEC.md Copywriting Contract section. Extract EVERY key-value pair listed there (Page chrome, Section titles, Column headers, CTAs+actions, Modal copy, Empty states, Error/warning states, Destructive confirmations). Plus `nav.pricegap` (EN: `Price-Gap`, zh-TW: `價差套利`).
    2. Add every key to `web/src/i18n/en.ts` with the EN copy exactly as specified in the SPEC. Match the existing file's structure (nested objects under `pricegap: { pageTitle: 'Price-Gap', ... }` OR flat keys `'pricegap.pageTitle': 'Price-Gap'` — choose whichever matches).
    3. Add the SAME keys in the SAME order to `web/src/i18n/zh-TW.ts` with the zh-TW copy from the SPEC. Every key that appears in en.ts must appear in zh-TW.ts.
    4. For any key listed in the SPEC with `{placeholder}` interpolations, preserve the placeholder syntax verbatim (the locale system uses the same replacement convention as existing keys — verify by reading an existing interpolated key).
    5. No other changes to the locale files. Do NOT rename or touch existing keys.
    6. Run `bash scripts/check-i18n-sync.sh` — must exit 0.
  </action>
  <verify>
    <automated>bash scripts/check-i18n-sync.sh && (cd web && npm run build)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pricegap.pageTitle\\|pageTitle" web/src/i18n/en.ts` — key present
    - `grep -q "pricegap.pageTitle\\|pageTitle" web/src/i18n/zh-TW.ts` — key present
    - `grep -q "Price-Gap" web/src/i18n/en.ts`
    - `grep -q "價差套利" web/src/i18n/zh-TW.ts`
    - `grep -q "模擬模式" web/src/i18n/zh-TW.ts` — Paper mode zh-TW
    - `grep -q "Paper mode" web/src/i18n/en.ts`
    - `grep -q "模擬" web/src/i18n/zh-TW.ts` — PAPER badge zh
    - `grep -q "📝 PAPER" web/src/i18n/` — NOT expected: SPEC says this is a Go literal (not i18n key). Assert it is NOT in either locale.
    - `! grep -q "📝 PAPER" web/src/i18n/en.ts` — Telegram prefix is Go-side per SPEC
    - `bash scripts/check-i18n-sync.sh` exits 0
    - `(cd web && npm run build)` exits 0 — TypeScript TranslationKey type satisfied
    - Key counts match: `count_en=$(grep -c "pricegap\\." web/src/i18n/en.ts); count_zh=$(grep -c "pricegap\\." web/src/i18n/zh-TW.ts); [ "$count_en" -eq "$count_zh" ]`
  </acceptance_criteria>
  <done>
    i18n lockstep achieved for every new key. Build green. No English fallbacks will render in zh-TW for Price-Gap surfaces.
  </done>
</task>

<task type="auto">
  <name>Task 2: App.tsx nav integration + PriceGap.tsx page</name>
  <files>web/src/App.tsx, web/src/pages/PriceGap.tsx</files>
  <read_first>
    - web/src/App.tsx (Page union type ~L21, nav items array, renderPage() switch ~L196)
    - web/src/pages/Positions.tsx (CLOSEST scaffold — copy this for PriceGap.tsx, then replace data shape per SPEC)
    - web/src/pages/Positions.tsx L602–630 (modal shell — reuse verbatim)
    - web/src/pages/Positions.tsx helpers (formatAge, formatDateTime, formatPrice, pnlColor — import or duplicate)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-UI-SPEC.md § "Layout Contract" + § "Table Specifications" + § "Interaction Contracts" + § "State Machine Contracts" + § "Consumer Guidance: For the executor"
    - internal/api/pricegap_handlers.go (Plan 02 — confirm response shapes)
  </read_first>
  <action>
    1. In `web/src/App.tsx`:
       - Add `'price-gap'` to the `Page` union type (search for the line defining `Page = 'overview' | ...`).
       - Register a nav item between `'spot-positions'` and `'history'` with `labelKey: 'nav.pricegap'` (mirror existing nav item shape).
       - Add a case to `renderPage()`: `case 'price-gap': return <PriceGap ... />;` passing whatever props existing pages receive (token, WS hook, etc. — match Positions.tsx signature).
    2. Create `web/src/pages/PriceGap.tsx`. Start by copying `Positions.tsx` wholesale, then:
       - Rename the default export to `PriceGap`.
       - Replace the data-shape types with:
         ```ts
         interface PriceGapCandidate { symbol: string; long_exchange: string; short_exchange: string; threshold_bps: number; max_notional: number; disabled: boolean; disabled_reason?: string; disabled_at?: number; }
         interface PriceGapPosition { id: string; symbol: string; long_exchange: string; short_exchange: string; long_size: number; short_size: number; notional_usdt: number; entry_spread_bps: number; current_spread_bps?: number; realized_pnl: number; opened_at: string; closed_at?: string; exit_reason?: string; realized_slip_bps?: number; modeled_slip_bps: number; mode: 'paper' | 'live'; }
         interface CandidateMetrics { candidate: string; symbol: string; long_exchange: string; short_exchange: string; trades_window: number; win_pct: number; avg_realized_bps: number; bps_24h_per_day: number; bps_7d_per_day: number; bps_30d_per_day: number; }
         ```
       - REST seed on mount: `fetch('/api/pricegap/state', { headers: { Authorization: 'Bearer ' + token } })`, populate 5 state slices (enabled, paperMode, candidates, activePositions, closedLog, metrics).
       - Subscribe to WS topics `pg_positions` (replace activePositions), `pg_event` (prepend to closedLog on `type==='exit'`, flash-highlight on `type==='entry'`, patch candidates on `type==='auto_disable'`), `pg_candidate_update` (patch by symbol). Reuse the existing WS hook Positions.tsx uses.
       - Render 5 sections in the exact order + class names from UI-SPEC § "Layout Contract":
         - Wrapper: `<div className="space-y-8">`
         - Section panels: `<div className="bg-gray-900 border border-gray-800 rounded-lg p-4 overflow-x-auto">`
         - Section titles: `<h3 className="text-sm font-bold text-gray-300 uppercase tracking-wide mb-3">{t(...)}</h3>`
       - Header (Section 1): page title `<h2 className="text-xl font-bold text-gray-100">` + stat strip (Open: N, Budget: $used / $total) + two toggles. Each toggle POSTs to `/api/config` with the existing config-update body shape. Optimistic update, rollback on `ok: false`.
       - Candidates table (Section 2) per UI-SPEC Candidates table. Disabled rows: `bg-yellow-500/5 opacity-60`. Action column: yellow `Re-enable` or muted `Disable`. Both open the modal.
       - Live Positions (Section 3) per UI-SPEC D-03 columns. Paper rows: `bg-violet-500/5`. PAPER/LIVE badge in Symbol column using inherited badge shell from Positions.tsx L374 (`text-violet-400 text-[10px] font-bold` for PAPER; `text-green-400 text-[10px] font-bold` for LIVE). `formatAge`, `pnlColor` helpers reused.
       - Closed Log (Section 4) per UI-SPEC D-04 columns. Pinned close-time-desc sort. 100-row pagination with `Load 100 more` button at bottom. Delta bps colored: `realized > modeled → text-red-400`, `realized <= modeled → text-green-400`.
       - Rolling Metrics (Section 5) per UI-SPEC D-25 columns. Default sort `30d bps/day` desc, every column click-sortable.
    3. Modals (shared):
       - Disable confirmation: reuse Positions.tsx L602–630 shell; body copy from i18n `pricegap.modal.disableBody`; reason input `<input type="text" placeholder={t('pricegap.modal.disableReasonPlaceholder')}>`; confirm POSTs `/api/pricegap/candidate/{symbol}/disable` with `{reason}`. Buttons: `pricegap.modal.disable.confirm` + `pricegap.modal.disable.keepActive`.
       - Re-enable confirmation: same shell; body from `pricegap.modal.reenableBody` with `{age}` + `{reason}` interpolated. If reason starts with `exec_quality`, append `pricegap.modal.reenableExecQuality`. Confirm POSTs `/api/pricegap/candidate/{symbol}/enable`. Buttons: `pricegap.modal.reEnable.confirm` + `pricegap.modal.reEnable.keepDisabled`.
       - Modal buttons: confirm=`bg-red-600`, dismiss=`bg-gray-700`. Esc closes. `role="dialog"` + `aria-labelledby` + `aria-describedby`.
    4. Loading skeleton: 3-row `<div className="h-4 bg-gray-800 animate-pulse rounded">` per `<tbody>`. No spinner.
    5. Error states: seed fail → `<div className="text-red-400 text-xs p-4">{t('pricegap.err.seedFailed', { section })}<button onClick={retry}>{t('pricegap.action.retry')}</button></div>`. WS disconnect → banner `bg-yellow-500/10 text-yellow-400 text-xs px-4 py-2` with `pricegap.warn.wsDisconnected`.
    6. Typography invariant: ONLY `font-bold` (700) and regular (400). NO `font-medium` or `font-semibold` anywhere in this file. Helper pattern: grep the final file for `font-semibold\|font-medium` — must return 0 matches.
    7. Color invariant: violet (`violet-400`, `violet-500/5`, `violet-500/15`) used ONLY for PAPER-mode surfaces. Yellow (`yellow-400`, `yellow-500/5`, `yellow-600/20`) used ONLY for disabled-candidate surfaces. No other violet/yellow usage.
    8. Run `(cd web && npm run build)` — must exit 0. React+TS+Vite will type-check against the TranslationKey union derived from en.ts, catching any missing keys.
    9. CRITICAL: after editing `web/src/**`, full build order is `cd web && npm run build && cd .. && go build ./...` — the Go binary embeds `web/dist/` via `go:embed`. Do NOT run `go build` before `npm run build` (would serve stale SPA).
  </action>
  <verify>
    <automated>bash scripts/check-i18n-sync.sh && (cd web && npm run build) && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `test -f web/src/pages/PriceGap.tsx`
    - `grep -q "'price-gap'" web/src/App.tsx`
    - `grep -q "case 'price-gap'" web/src/App.tsx` — renderPage case
    - `grep -q "nav.pricegap\\|pricegap.pageTitle" web/src/App.tsx` — nav label wired
    - `grep -q "/api/pricegap/state" web/src/pages/PriceGap.tsx` — REST seed
    - `grep -q "pg_positions" web/src/pages/PriceGap.tsx` — WS topic
    - `grep -q "pg_event" web/src/pages/PriceGap.tsx`
    - `grep -q "pg_candidate_update" web/src/pages/PriceGap.tsx`
    - `grep -q "violet-400\\|violet-500" web/src/pages/PriceGap.tsx` — paper accent
    - `grep -q "yellow-500/5\\|yellow-600/20" web/src/pages/PriceGap.tsx` — disabled accent
    - `grep -q "animate-pulse" web/src/pages/PriceGap.tsx` — loading skeleton
    - `grep -q "role=\"dialog\"" web/src/pages/PriceGap.tsx` — modal a11y
    - `! grep -qE "font-medium|font-semibold" web/src/pages/PriceGap.tsx` — typography invariant (only 400 + 700)
    - `(cd web && npm run build)` exits 0
    - `go build ./...` exits 0 (go:embed picks up fresh dist)
    - `bash scripts/check-i18n-sync.sh` exits 0 (unchanged from Task 1)
    - `grep -c "pricegap\\." web/src/pages/PriceGap.tsx` >= 20 — page uses many i18n keys
    - Line count sanity: `wc -l web/src/pages/PriceGap.tsx` < 700 (per UI-SPEC: stay single-file until ~600 lines)
    - NO new npm dependencies: `git diff web/package.json web/package-lock.json` is empty (npm lockdown per CLAUDE.local.md)
  </acceptance_criteria>
  <done>
    Price-Gap page renders per UI-SPEC. Nav wired. i18n lockstep. Frontend + backend both build. npm lockdown respected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server REST response → React render | Candidate symbols and reasons render in DOM — React auto-escapes; no dangerouslySetInnerHTML |
| Bearer token → localStorage | Existing pattern, not changed by this plan |
| User input (reason field) → POST body | Length cap enforced server-side (Plan 02); client cap for UX |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-30 | Tampering | XSS via candidate symbol or reason rendered in dashboard | high | mitigate | Use {value} interpolation only (React escapes); acceptance criteria greps for absence of `dangerouslySetInnerHTML` |
| T-09-31 | Elevation | Accidental npm install introduces supply-chain risk | high | mitigate | acceptance criteria asserts package.json + lockfile unchanged; CLAUDE.local.md ban on npm install |
| T-09-32 | Information Disclosure | Missing auth header on /api/pricegap/* fetches | medium | mitigate | Every fetch includes `Authorization: Bearer ${token}` header (Positions.tsx pattern copied) |
| T-09-33 | Spoofing | Paper mode flipped without confirmation causing operator confusion | medium | accept | Toggle flips directly per UI-SPEC (no modal); header pill + row tint provide strong visual signal; existing positions keep their mode per D-11 |
| T-09-34 | Repudiation | i18n key drift silently renders English in zh-TW | medium | mitigate | scripts/check-i18n-sync.sh gate + UI-SPEC `pricegap.modal.cancel` ban enforced by grep |
</threat_model>

<verification>
- `bash scripts/check-i18n-sync.sh` exits 0
- `(cd web && npm run build)` exits 0
- `go build ./...` exits 0
- UI-SPEC typography invariant (2 weights only) enforced
- UI-SPEC color invariants enforced (violet for paper only, yellow for disabled only)
- npm lockdown enforced (no lockfile diff)
</verification>

<success_criteria>
Operator opens dashboard, clicks Price-Gap, sees the page rendered per SPEC in both EN and zh-TW. Toggles round-trip. Candidates table drives disable/enable. Live Positions update via WS. Closed Log shows Modeled/Realized/Delta. Rolling Metrics sorts 30d-desc by default.
</success_criteria>

<output>
After completion, create `.planning/phases/09-price-gap-dashboard-paper-live-operations/09-07-SUMMARY.md` listing: line count of PriceGap.tsx, i18n key count added, any UI-SPEC deviations (should be zero), frontend build output size.
</output>
