---
phase: 999.1-bidirectional-pricegap-candidates
plan: 06
subsystem: release
tags: [release, uat, changelog, pricegap, bidirectional]
requires:
  - 999.1-01  # Direction field + sign filter + executor leg-role swap
  - 999.1-02  # validator + NormalizeDirection
  - 999.1-03  # risk-gate role-blindness verification
  - 999.1-04  # dashboard modal Direction radio
  - 999.1-05  # i18n EN + zh-TW lockstep
provides:
  - v0.35.0 release with PG-DIR-01 feature
  - Operator-approved UAT confirmation
  - CHANGELOG behavior-change warning for pinned-mode sign filter
affects:
  - CHANGELOG.md
  - VERSION
tech-stack:
  added: []
  patterns:
    - "Test binary built to /tmp/arb-phase999.1 — never overwrites live ./arb"
    - "CHANGELOG BREAKING marker for behavior changes (not API-breaking, behavior-breaking)"
key-files:
  created: []
  modified:
    - VERSION
    - CHANGELOG.md
decisions:
  - "VERSION bump 0.34.11 → 0.35.0 (minor for new feature, not patch — plan-template assumed 0.33.x→0.34.0 but actual base was 0.34.11 from rolling fixes)"
  - "Promoted Plan 01 Unreleased CHANGELOG draft into 0.35.0 Added/Changed/Fixed sections"
  - "BREAKING marker placed on pinned-mode sign-filter change to surface to operators reading release notes"
metrics:
  duration: ~5min (build + commit; UAT operator-side)
  tasks: 2
  files: 2
  completed: 2026-04-27
---

# Phase 999.1 Plan 06: Build, Release & Operator UAT Summary

Built v0.35.0 binary at `/tmp/arb-phase999.1`, finalized CHANGELOG with **BREAKING** marker on pinned-mode sign filter, bumped VERSION, and obtained operator UAT approval for the Direction radio round-trip across EN + zh-TW dashboard locales.

## What Was Built

- **VERSION:** 0.34.11 → 0.35.0 (minor bump for PG-DIR-01 feature)
- **CHANGELOG.md:** promoted the Unreleased Plan-01 draft into a versioned 0.35.0 entry with three sub-sections:
  - **Added** — Direction field + bidirectional fires + executor leg-role swap; observability fields on PriceGapPosition (FiredDirection, CandidateLongExch, CandidateShortExch)
  - **Changed (BREAKING behavior, not API)** — pinned-mode sign filter; explicit operator instruction to flip intentionally-symmetric pinned candidates to `direction: "bidirectional"`
  - **Fixed** — risk-gate concentration role-blindness (Phase 999.1 A5)
- **Test binary:** `/tmp/arb-phase999.1` (26 MB, executable) — separate from live `./arb`
- **Frontend rebuild:** 704 modules transformed, `dist/` regenerated for the embedded SPA

## How It Was Built

1. **Frontend first** (`cd web && npm run build`) — `go:embed` invariant per CLAUDE.local.md
2. **Go binary second** (`go build -o /tmp/arb-phase999.1 ./cmd/main.go`) — note `./cmd/main.go` not `./cmd/arb` (the plan's command was a copy-paste from a different repo layout)
3. **VERSION + CHANGELOG** updated and committed before UAT
4. **Operator UAT** (8-step focused script) — Direction radio renders in EN + zh-TW, save round-trips through POST /api/config, reload reflects saved value, no console errors

## Operator UAT Result

**Status:** approved (all 8 steps passed)

UAT covered:
1. Dashboard PriceGap → Candidates tab opens cleanly
2. Edit modal exposes Direction section with Pinned/Bidirectional radios; Pinned default checked
3. Switching to Bidirectional updates help text; Save succeeds
4. Reload preserves Bidirectional selection (POST /api/config → config.json round-trip)
5. zh-TW renders 方向 label + 單向/雙向 options (no raw i18n keys leaked)
6. Add modal defaults to Pinned; new candidate persists with selected direction
7. Test candidate cleaned up via dashboard (no direct config.json edits)
8. Live service restarted via systemd; live binary unaffected by UAT

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Wrong build entrypoint path in plan**
- **Found during:** Task 1 Step C
- **Issue:** Plan wrote `go build -o /tmp/arb-phase999.1 ./cmd/arb`. There is no `./cmd/arb` directory — the entrypoint is `./cmd/main.go` (per Makefile `build` target).
- **Fix:** Built with `go build -o /tmp/arb-phase999.1 ./cmd/main.go`.
- **Files modified:** none (build command only)
- **Commit:** N/A (build is ephemeral)

**2. [Rule 3 - Blocking] VERSION bump target adjusted to current base**
- **Found during:** Task 1 Step A
- **Issue:** Plan assumed VERSION was 0.33.x and prescribed 0.34.0. Actual VERSION at execution time was 0.34.11 (rolling Phase-13/Phase-8 hardening commits between plan-write and plan-execute).
- **Fix:** Bumped to 0.35.0 (minor, for new feature) instead of 0.34.0. Preserves the plan's intent (minor bump for behavior-changing feature) without colliding with already-shipped 0.34.x patch versions.
- **Files modified:** VERSION
- **Commit:** 0455fed

## Authentication Gates

None.

## Threat Flags

None — no new security-relevant surface introduced. Build artifact is ephemeral test binary at `/tmp/`; live binary unaffected.

## Self-Check: PASSED

- VERSION reads `0.35.0` ✓
- CHANGELOG contains `## [0.35.0] - 2026-04-27` ✓
- CHANGELOG contains BREAKING marker on pinned-mode sign filter ✓
- `/tmp/arb-phase999.1` exists and is executable (26 MB) ✓
- Commit `0455fed` exists in git log ✓
- Live `config.json` untouched ✓
- Operator UAT approved ✓
