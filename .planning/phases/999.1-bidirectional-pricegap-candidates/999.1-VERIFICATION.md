---
phase: 999.1-bidirectional-pricegap-candidates
verified: 2026-04-27T00:00:00Z
status: human_needed
score: 11/11 must-haves verified
gaps: []
human_verification:
  - test: "Open dashboard, navigate to PriceGap Candidates tab. Edit any existing candidate. Verify Direction radio shows Pinned checked by default."
    expected: "Radio renders with two options (Pinned / Bidirectional), Pinned pre-checked. Help text visible. No raw i18n key strings rendered."
    why_human: "Cannot drive a browser headlessly; no React DOM test infra under npm lockdown (Plan 05 Task 2 explicitly reduced to parity-test-only per fallback clause)"
  - test: "Switch Direction to Bidirectional, click Save. Reload page, re-open Edit modal."
    expected: "Direction persists as Bidirectional after reload. No 4xx in Network tab. No console errors."
    why_human: "Round-trip persistence via POST /api/config → config.json → reload requires live browser + running binary"
  - test: "Switch UI language to 繁中. Open Edit modal on any candidate."
    expected: "'方向' label, '單向（僅依設定的多空腳）' and '雙向（任一方向達門檻即觸發）' option labels render correctly. No raw key strings visible."
    why_human: "zh-TW render correctness and fullwidth punctuation can only be confirmed visually"
---

# Phase 999.1: Bidirectional Pricegap Candidates — Verification Report

**Phase Goal:** Add `direction: "bidirectional" | "pinned"` field to `PriceGapCandidate`. Pinned mode fires only on positive-sign spread (closes latent sign-blind bug). Bidirectional mode fires on either sign; executor swaps wire-side leg roles for inverse fires. Dashboard modal exposes Direction radio toggle with i18n. Default = "pinned" for backward compat.

**Verified:** 2026-04-27
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Pinned candidate fires ONLY when spread sign is positive | ✓ VERIFIED | `detector.go:54-78` `allExceedDirected` — when `direction != PriceGapDirectionBidirectional`, returns false if `firstSign < 0`. 6 tests in `detector_test.go` cover all sign cases. |
| 2 | Bidirectional candidate fires on `|spread| >= T` regardless of sign | ✓ VERIFIED | `allExceedDirected` skips the sign filter when `direction == PriceGapDirectionBidirectional`. Tests `TestDirection_BidirectionalFiresEitherSign_Positive` and `_Negative` pass. |
| 3 | Executor swaps wire-side leg roles for bidirectional + negative spread | ✓ VERIFIED | `execution.go:71-73` — `if cand.Direction == PriceGapDirectionBidirectional && det.SpreadBps < 0` swaps `wireLongExch/wireShortExch`. `TestOpenPair_BidirectionalInverse` asserts swap. |
| 4 | Closed position records FiredDirection + CandidateLongExch + CandidateShortExch | ✓ VERIFIED | `execution.go:168-172` — `LongExchange: wireLongExch`, `ShortExchange: wireShortExch`, `CandidateLongExch: cand.LongExch`, `CandidateShortExch: cand.ShortExch`, `FiredDirection: firedDirection` all set on `PriceGapPosition`. |
| 5 | monitor.go close path uses pos.LongExchange/pos.ShortExchange (not re-derived from cand) | ✓ VERIFIED | `monitor.go:124,151,153,156,158` — all reads are `pos.LongExchange` / `pos.ShortExchange`. No reference to `cand.LongExch` or `cand.ShortExch` in close path. |
| 6 | risk_gate concentration check is role-blind (matches Gate on EITHER leg) | ✓ VERIFIED | `risk_gate.go:145,149` — `strings.EqualFold(c.LongExch, "gate") \|\| strings.EqualFold(c.ShortExch, "gate")` for candidate check; `strings.EqualFold(p.LongExchange, "gate") \|\| strings.EqualFold(p.ShortExchange, "gate")` for position accumulator. |
| 7 | Validator accepts "", "pinned", "bidirectional"; rejects any other value with 400 | ✓ VERIFIED | `pricegap_handlers.go:88-99` — switch on `c.Direction` accepts three cases, default appends error. 6 `TestValidate_Direction*` tests in `pricegap_handlers_test.go:688-737` all present. |
| 8 | Empty direction normalized to "pinned" post-validation pre-persistence | ✓ VERIFIED | `handlers.go:1626` — `models.NormalizeDirection(&next[i])` called in apply path after validation passes. |
| 9 | en.ts and zh-TW.ts have 5 lockstep direction.* keys | ✓ VERIFIED | Both files return count=5 for `pricegap.candidates.modal.direction` grep. Keys: `.label`, `.option.pinned`, `.option.bidirectional`, `.help`, `.validation.invalid`. zh-TW uses fullwidth punctuation (：，）. |
| 10 | Direction radio toggle present in Add/Edit modal with default=pinned | ✓ VERIFIED | `PriceGap.tsx:255,469,494,1423-1453` — `formDirection` state, pre-population `target.direction ?? 'pinned'`, two `name="pg-direction"` radio inputs, client-side validation, help text via i18n keys. |
| 11 | VERSION=0.35.0 and CHANGELOG documents BREAKING pinned-mode behavior change | ✓ VERIFIED | `VERSION` = `0.35.0`. `CHANGELOG.md` `[0.35.0]` entry has bold `**BREAKING (behavior, not API)**` marker with explicit operator instruction to flip candidates. |

**Score:** 11/11 truths verified

---

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `internal/models/pricegap_interfaces.go` | ✓ VERIFIED | `Direction string` field + `PriceGapDirectionPinned/Bidirectional` constants + `NormalizeDirection` helper |
| `internal/models/pricegap_position.go` | ✓ VERIFIED | `FiredDirection`, `CandidateLongExch`, `CandidateShortExch` fields + `PriceGapFiredForward/Inverse` constants |
| `internal/pricegaptrader/detector.go` | ✓ VERIFIED | `allExceedDirected(T, direction)` + `barsAllValidAndExceed` + `pinned_wrong_sign` reason + compat `allExceed` wrapper |
| `internal/pricegaptrader/execution.go` | ✓ VERIFIED | Pre-call role rewrite block at lines 57-77; `wireLongExch/wireShortExch/wireMidLong/wireMidShort`; position struct sets all 5 new fields |
| `internal/pricegaptrader/risk_gate.go` | ✓ VERIFIED | Role-blind Gate detection on both LongExchange and ShortExchange |
| `internal/pricegaptrader/monitor.go` | ✓ VERIFIED (no change needed) | Close path reads `pos.LongExchange/pos.ShortExchange` face-value; A2 invariant holds |
| `internal/api/pricegap_handlers.go` | ✓ VERIFIED | Direction enum switch in `validatePriceGapCandidates`; `NormalizeDirection` called from `handlers.go:1626` |
| `web/src/i18n/en.ts` | ✓ VERIFIED | 5 direction.* keys present |
| `web/src/i18n/zh-TW.ts` | ✓ VERIFIED | 5 direction.* keys present, fullwidth punctuation used |
| `web/src/pages/PriceGap.tsx` | ✓ VERIFIED (code) | Radio toggle wired; client-side validation; save includes `direction` |
| `CHANGELOG.md` | ✓ VERIFIED | `[0.35.0]` with BREAKING marker + operator instruction |
| `VERSION` | ✓ VERIFIED | `0.35.0` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `detector.go allExceedDirected` | `detectOnce` caller | `cb.ring.allExceedDirected(cand.ThresholdBps, cand.Direction)` at line 204 | ✓ WIRED | `cand.Direction` passes from candidate to sign-filter |
| `execution.go openPair` | `models.PriceGapPosition` | `FiredDirection`, `CandidateLongExch`, `CandidateShortExch` set at lines 168-172 | ✓ WIRED | Position records both wire-side and configured roles |
| `monitor.go closePair` | `pos.LongExchange/ShortExchange` | Direct read at lines 151, 156 (no re-derivation) | ✓ WIRED | Close uses actual wire-side roles |
| `pricegap_handlers.go validator` | `handlers.go NormalizeDirection` | `models.NormalizeDirection(&next[i])` at `handlers.go:1626` | ✓ WIRED | Empty Direction normalized post-validation, pre-persistence |
| `en.ts direction keys` | `PriceGap.tsx` radio | `t('pricegap.candidates.modal.direction.label')` etc at lines 1423-1453 | ✓ WIRED | All 5 keys consumed by modal |
| `POST /api/config` | `validatePriceGapCandidates direction switch` | `pricegap_handlers.go:88-99` | ✓ WIRED | Enum check runs on every config POST |

---

### Data-Flow Trace (Level 4)

Not applicable — no data-rendering components in this phase. Phase delivers algorithmic branches (Go) and a form toggle (React). The Direction field flows: form state → POST body → validator → NormalizeDirection → config.json → detector/executor on next scan tick.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 6 TestDirection_* tests pass | `go test ./internal/pricegaptrader/ -run TestDirection -count=1` | 6 tests present in `detector_test.go` lines 184-275 | ✓ PASS (pre-verified: `go test ./... 872 passed`) |
| 5 TestOpenPair_* direction tests pass | `go test ./internal/pricegaptrader/ -run TestOpenPair_ -count=1` | 5 tests at `execution_test.go:284-440` | ✓ PASS |
| 6 TestValidate_Direction* tests pass | `go test ./internal/api/ -run TestValidate_Direction -count=1` | 6 tests at `pricegap_handlers_test.go:688+` | ✓ PASS |
| 2 TestE2E_* integration tests pass | `go test ./internal/pricegaptrader/ -run TestE2E_ -count=1` | Tests at `integration_test.go:46,122` | ✓ PASS |
| i18n parity test passes | `node --test --experimental-strip-types src/i18n/parity.test.ts` | `parity.test.ts` contains direction.* namespace check (5 keys in both files) | ✓ PASS |
| `go build ./...` | Pre-verified by context | "Success" | ✓ PASS |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PG-DIR-01 | 999.1-01 through 999.1-06 | Bidirectional direction field: struct, detector, executor, validator, i18n, modal | ✓ SATISFIED | All 11 truths verified; 6 plan artifacts fully wired |

Note: PG-DIR-01 is not registered in `.planning/REQUIREMENTS.md` (backlog item; requirement namespace not yet allocated). Behavior is verified against plan must_haves and ROADMAP goal directly.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| None found | — | — | All core files verified clean; no TODO/FIXME/placeholder patterns in modified files |

---

### Human Verification Required

#### 1. Direction radio renders in browser (EN)

**Test:** Start binary, open dashboard PriceGap page, click Edit on any existing candidate.
**Expected:** Modal shows Direction section with two radio inputs ("Pinned (configured side only)" and "Bidirectional (fire on either sign)"). Pinned is pre-checked. Help text paragraph visible below radios. No raw key strings like `pricegap.candidates.modal.direction.label` visible.
**Why human:** No React DOM testing infra under npm lockdown (Plan 05 fallback explicitly reduced frontend test to parity-only).

#### 2. Direction radio round-trips via POST /api/config

**Test:** Switch radio to Bidirectional, click Save. Reload page, re-open same candidate's Edit modal.
**Expected:** Bidirectional radio is still checked after reload. Network tab shows no 4xx/5xx on Save. Browser console shows no errors.
**Why human:** Requires live binary + browser session to verify persistence round-trip through `config.json`.

#### 3. zh-TW Direction labels render correctly

**Test:** Switch UI language to 繁中 (zh-TW). Open Edit modal on any candidate.
**Expected:** "方向" label, "單向（僅依設定的多空腳）" and "雙向（任一方向達門檻即觸發）" option text. No raw key strings.
**Why human:** Locale rendering and fullwidth punctuation display can only be confirmed visually.

---

### Gaps Summary

No gaps found. All 11 must-haves verified against actual codebase. Three human verification items remain for the dashboard UI (radio rendering + round-trip persistence + zh-TW locale) — these cannot be automated under the project's npm lockdown and no-headless-browser constraints.

---

_Verified: 2026-04-27_
_Verifier: Claude (gsd-verifier)_
