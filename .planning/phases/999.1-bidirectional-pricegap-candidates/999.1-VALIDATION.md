---
phase: 999.1
slug: bidirectional-pricegap-candidates
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-27
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract. Detail in `999.1-RESEARCH.md` §"Validation Architecture".

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (Go)** | `go test` (stdlib) |
| **Framework (TS)** | `node --test --experimental-strip-types` (no vitest per npm lockdown) |
| **Quick run command** | `go test ./internal/pricegaptrader/ ./internal/api/ -run "Direction\|Bidirectional\|Pinned" -count=1` |
| **Full suite command** | `go test ./... -count=1 && node --test --experimental-strip-types web/src/pages/PriceGap.candidates.test.ts` |
| **Estimated runtime** | ~30s Go + ~1s frontend |

## Sampling Rate

- After every task commit: quick run command
- After every plan wave: full suite
- Before verification: full suite green
- Max feedback latency: 60s

## Per-Task Verification Map

To be filled by planner from PLAN.md tasks.

## Wave 0 Requirements

- [ ] `internal/pricegaptrader/direction_test.go` (new) — pinned-sign-filter regression + bidirectional-fire on both signs
- [ ] `internal/api/pricegap_candidates_validate_test.go` (extend) — direction field default + invalid-value rejection
- [ ] `web/src/pages/PriceGap.candidates.test.ts` (extend) — modal radio includes direction; lockstep i18n parity for new keys

## Manual-Only Verifications

| Behavior | Why Manual |
|----------|------------|
| zh-TW visual layout of new direction radio | Chinese line-wrap not type-checkable |
| Live operator promotes existing pinned candidate to bidirectional and observes inverse-direction fire when a real edge appears | Requires live market data + extended observation window |

## Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
