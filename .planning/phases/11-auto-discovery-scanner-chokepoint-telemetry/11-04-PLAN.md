---
phase: 11
plan: 04
type: execute
wave: 4
depends_on: [11-02, 11-03]
files_modified:
  - internal/pricegaptrader/scanner.go
  - internal/pricegaptrader/scanner_test.go
  - internal/pricegaptrader/scanner_static_test.go
  - internal/pricegaptrader/telemetry.go
  - internal/pricegaptrader/telemetry_test.go
  - internal/pricegaptrader/tracker.go
  - internal/api/pricegap_discovery_handlers.go
  - internal/api/pricegap_discovery_handlers_test.go
  - internal/api/server.go
  - internal/pricegaptrader/validate.go
autonomous: true
requirements:
  - PG-DISC-01
  - PG-DISC-03
user_setup: []

must_haves:
  truths:
    - "Scanner runs at PriceGapDiscoveryIntervalSec (default 300s) when PriceGapDiscoveryEnabled=true; goroutine present-but-no-op when disabled."
    - "Scanner walks operator-curated universe times all-cross of 6 supported exchanges; ≤20 universe enforced upstream by Plan 01 validation."
    - "ALL gates pass before non-zero score: ≥4-bar persistence + BBO <90s + depth probe ≥ floor + not on denylist + cross-exchange pair exists."
    - "Score is integer 0-100, gate-then-magnitude shape; sub-scores persisted alongside top-line score per cycle (D-03)."
    - "Rejected candidates captured with score=0 + why_rejected reason code (D-04); every (symbol times cross-pair) writes a record."
    - "Scanner is read-only at compile time — accepts RegistryReader, never *Registry."
    - "Telemetry writes pg:scan:cycles, pg:scan:scores:{symbol}, pg:scan:rejections:{date}, pg:scan:metrics, pg:scan:enabled keys per Redis schema."
    - "REST endpoint GET /api/pg/discovery/state returns documented envelope; GET /api/pg/discovery/scores/{symbol} returns 7d ZSET dump."
    - "WS hub broadcasts pg_scan_cycle (per cycle), pg_scan_metrics (5s throttle), pg_scan_score (debounced)."
    - "Bybit blackout window :04-:05:30 produces why_rejected=bybit_blackout, never sample errors."
    - "Symbol normalization: cross-exchange BBO join works for canonical BTCUSDT input across 6 adapters."
    - "1-bar pump fixture is REJECTED (Pitfall 1 mitigation verified)."
  artifacts:
    - path: "internal/pricegaptrader/scanner.go"
      provides: "Read-only scanner: scanLoop goroutine, runScanCycle, gate evaluation (persistence/freshness/depth/denylist/cross-pair), 0-100 magnitude score, reason-code emission"
      contains: "type Scanner struct"
    - path: "internal/pricegaptrader/telemetry.go"
      provides: "pg:scan:* Redis writers + WS broadcaster for cycle/metrics/score events"
      contains: "type Telemetry struct"
    - path: "internal/api/pricegap_discovery_handlers.go"
      provides: "GET /api/pg/discovery/state and GET /api/pg/discovery/scores/{symbol} REST handlers"
      contains: "handlePgDiscoveryState"
    - path: "internal/pricegaptrader/scanner_static_test.go"
      provides: "Static analysis test: scanner.go does NOT reference *Registry mutator method names"
      contains: "TestScanner_NoRegistryMutators"
  key_links:
    - from: "internal/pricegaptrader/scanner.go"
      to: "internal/pricegaptrader/registry_reader.go"
      via: "Scanner constructor accepts RegistryReader (Plan 01 interface), never *Registry"
      pattern: "RegistryReader"
    - from: "internal/pricegaptrader/scanner.go"
      to: "pkg/exchange.Exchange.GetBBO + GetDepth"
      via: "Per-cycle BBO read for freshness gate; per-cycle GetDepth for depth probe gate"
      pattern: "GetBBO|GetDepth"
    - from: "internal/pricegaptrader/telemetry.go"
      to: "internal/database (Redis DB 2)"
      via: "RPush/LTrim/ZAdd/HSet on pg:scan:* keys"
      pattern: "pg:scan:"
    - from: "internal/pricegaptrader/tracker.go Start()"
      to: "internal/pricegaptrader/scanner.go scanLoop"
      via: "t.wg.Add(1); go t.scanLoop() launched alongside existing tickLoop"
      pattern: "scanLoop"
    - from: "internal/api/pricegap_discovery_handlers.go"
      to: "internal/pricegaptrader/telemetry.go (read path)"
      via: "Handlers query the same pg:scan:* keys via telemetry.GetState/GetScores helpers"
      pattern: "telemetry.Get"
---

<objective>
Build the read-only auto-discovery scanner (PG-DISC-01) and discovery telemetry (PG-DISC-03 backend half — REST + WS endpoints; Plan 05 builds the React UI). Scanner is launched as a `t.wg.Add(1); go t.scanLoop()` goroutine inside `Tracker.Start()`, mirroring the existing `tickLoop` lifecycle. Default OFF: with `PriceGapDiscoveryEnabled=false`, the goroutine ticks but writes only an "OFF" stamp so the dashboard banner has data.

Purpose: Implement decisions D-01 (gate-then-magnitude), D-02 (integer 0-100), D-03 (sub-scores persisted), D-04 (rejected captured with reason), D-08 (silent skip for singletons), D-13 (compile-time read-only), D-17 (REST seed + WS push). Honors Pitfall 1 (4-bar persistence + BBO <90s + depth probe + denylist) as the antidote to false-positive pump.
Output: Scanner goroutine + scoring engine + telemetry writes + REST endpoints + WS broadcasts + comprehensive unit tests. Plan 02's Registry is a prerequisite (this plan compiles against Registry's RegistryReader interface from Plan 01); Plan 03's hard-cut migration runs in parallel (disjoint files).
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-CONTEXT.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-RESEARCH.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-VALIDATION.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-01-PLAN.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-02-PLAN.md

<interfaces>
From Plan 01 (Wave 1):
- PriceGapDiscoveryEnabled bool, PriceGapDiscoveryUniverse []string, PriceGapDiscoveryDenylist []string,
  PriceGapDiscoveryIntervalSec int (default 300), PriceGapDiscoveryThresholdBps int (default 100),
  PriceGapDiscoveryMinDepthUSDT int (default 1000), PriceGapAutoPromoteScore int (default 60),
  PriceGapMaxCandidates int (default 12)
- RegistryReader interface { Get(idx) (PriceGapCandidate, bool); List() []PriceGapCandidate }

From Plan 02 (Wave 2):
- *Registry implements RegistryReader (Plan 04 takes the interface)

From existing codebase (verified during planning):
- Tracker.Start() at internal/pricegaptrader/tracker.go:283 launches goroutines via t.wg.Add(1)
- tickLoop pattern at lines ~355-397 with firstTick offset; mirror for scanLoop
- sampleLegs(t.exchanges, candidate) at detector.go:126-152 — reusable for cross-exchange BBO read
- barRing.allExceedDirected at detector.go:54-78 — reusable persistence gate (T threshold + same-sign + bidirectional support)
- Exchange.GetBBO(symbol) (BBO, ok) — verified in detector.go:139-146
- Exchange.GetDepth(symbol) (*Orderbook, bool) — verified at pkg/exchange/exchange.go:73 (interface-level, all 6 adapters)
- cfg.PriceGapKlineStalenessSec at config.go:324 (default 90) — reuse for BBO freshness gate
- WS broadcaster pattern: tracker.go:140-146 SetBroadcaster + Broadcast{Event,PriceGapPositions}
- Bybit blackout window :04-:05:30 — hardcoded in existing pricegaptrader code; check for blackout helper or build inline check
- models.PriceGapDirectionBidirectional constant — used by barRing.allExceedDirected when direction-agnostic
- internal/database.Client surface for ZAdd/Expire/HSet/RPush/LTrim — verify exact method names by reading the existing metrics.go writer pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Scanner with gates + 0-100 magnitude score + cycle output</name>
  <files>internal/pricegaptrader/scanner.go, internal/pricegaptrader/scanner_test.go, internal/pricegaptrader/scanner_static_test.go</files>
  <read_first>
    - internal/pricegaptrader/tracker.go (lines 1-50 docstring, lines 100-200 Tracker struct + constructor, lines 283-335 Start/Stop, lines 355-397 tickLoop pattern, lines 240-263 subscribeCandidates pattern)
    - internal/pricegaptrader/detector.go (full file — barRing struct + allExceedDirected at 54-78; sampleLegs at 126-152; staleness gate at 194-201; reason emission at 220-226)
    - internal/pricegaptrader/risk_gate.go (existing BBO freshness gate pattern — pre-entry risk gate uses cfg.PriceGapKlineStalenessSec)
    - internal/pricegaptrader/registry_reader.go (Plan 01 — RegistryReader interface)
    - pkg/exchange/exchange.go (lines 60-100 — Exchange interface; GetBBO + GetDepth signatures; Orderbook struct shape)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-CONTEXT.md (D-01 to D-08, D-13)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-RESEARCH.md (Scanner Architecture, Universe Selection, Persistence/Freshness/Depth Gates, Scoring, Common Pitfalls)
    - models definitions for PriceGapCandidate (Symbol, LongExch, ShortExch, Direction)
  </read_first>
  <behavior>
    - Test 1 (default OFF): With PriceGapDiscoveryEnabled=false, RunCycle writes only an OFF marker (pg:scan:enabled set to "0") and returns; no per-symbol records emitted.
    - Test 2 (universe walk): With universe=[BTCUSDT, ETHUSDT] and 3 supported exchanges, RunCycle emits up to 2 times (3 times 2)=12 cross-pair records; symbol-no-cross-pair counter incremented for symbols with <2 listings.
    - Test 3 (≥4-bar persistence): A spread that crosses threshold for ONLY 1 bar produces score=0 with why_rejected=insufficient_persistence. After 4 bars of same-sign crossing, the same pair scores >0.
    - Test 4 (BBO freshness): If lastSampleAt for a leg is >90s old (cfg.PriceGapKlineStalenessSec), the bar ring resets; gate fails with why_rejected=stale_bbo.
    - Test 5 (depth probe): GetDepth returns an Orderbook whose clearable USDT at threshold-bps spread is < cfg.PriceGapDiscoveryMinDepthUSDT (default 1000); gate fails with why_rejected=insufficient_depth. Above floor → gate passes; depth_score component populated.
    - Test 6 (denylist by symbol): Universe contains BTCUSDT; denylist contains "BTCUSDT" — every BTCUSDT cross-pair scores 0 with why_rejected=denylist.
    - Test 7 (denylist by symbol@exchange tuple): Denylist contains "BTCUSDT@bybit" — only the (long=any, short=bybit) and (long=bybit, short=any) pairs score 0 with why_rejected=denylist; other cross-pairs evaluate normally.
    - Test 8 (Bybit blackout): During the :04-:05:30 wall-clock window, Bybit-leg BBO reads are skipped; the affected cross-pair scores 0 with why_rejected=bybit_blackout. Other exchanges in the cross-product still scan.
    - Test 9 (singleton skip — D-08): Universe contains SOMETHINGUSDT listed only on Binance — no per-symbol record emitted; pg:scan:metrics symbol_no_cross_pair counter increments by 1.
    - Test 10 (gate-then-magnitude — D-01): A pair where ALL gates pass and threshold spread = 200bps with depth-clearable=$5000 (above floor $1000) → score is in [1,100] (non-zero) AND sub-scores spread_bps, persistence_bars, depth_score, freshness_age_s, funding_bps_h, gates_passed[], gates_failed[] are populated.
    - Test 11 (1-bar pump rejected — Pitfall 1): Synthetic 1-bar spread of 500bps on a thin SOON-style pair → score=0 with why_rejected=insufficient_persistence. (Anti-Pitfall-1 verification.)
    - Test 12 (canonical symbol join — A1 from RESEARCH.md): Test fixture with adapters that return BBOs for canonical "BTCUSDT" form; cross-exchange join produces non-empty result; assertion verifies normalization didn't drop the join.
    - Test 13 (compile-time read-only): Scanner constructor signature is NewScanner(reader RegistryReader, ...). Static test in scanner_static_test.go reads the source of scanner.go and asserts NO regex match for `\.Add\(|\.Update\(|\.Delete\(|\.Replace\(` AND no token `\*Registry` (only `RegistryReader`).
    - Test 14 (cycle error budget): Inject 5 consecutive ticker-read failures → pg:scan:metrics records cycle_failed=true and remaining cross-pairs in that cycle are skipped; counter resets on next successful read.
    - Test 15 (sub-score persistence — D-03): Cycle record for an accepted candidate contains spread_bps, persistence_bars, depth_score, freshness_age_s, funding_bps_h, gates_passed, gates_failed — at least these 7 keys present.
  </behavior>
  <action>
    Create internal/pricegaptrader/scanner.go with:

    Reason enum (D-04 reason codes — exactly these 8 string constants):
      ReasonInsufficientPersistence = "insufficient_persistence"
      ReasonStaleBBO                = "stale_bbo"
      ReasonInsufficientDepth       = "insufficient_depth"
      ReasonDenylist                = "denylist"
      ReasonBybitBlackout           = "bybit_blackout"
      ReasonSymbolNotListedLong     = "symbol_not_listed_long"
      ReasonSymbolNotListedShort    = "symbol_not_listed_short"
      ReasonSampleError             = "sample_error"
      ReasonAccepted                = "" (empty = passed all gates)

    CycleRecord struct fields (per record JSON envelope written to pg:scan:cycles):
      Symbol string, LongExch string, ShortExch string, Score int (0-100),
      SpreadBps float64, PersistenceBars int, DepthScore float64 (0..1),
      FreshnessAgeSec int, FundingBpsPerHr float64 (Loris ÷8 applied),
      GatesPassed []string, GatesFailed []string,
      WhyRejected ScanReason (omitempty), AlreadyPromoted bool, Timestamp int64

    CycleSummary struct:
      StartedAt int64, CompletedAt int64, DurationMs int64,
      CandidatesSeen int, Accepted int, Rejected int, Errors int,
      WhyRejected map[ScanReason]int, Records []CycleRecord

    Scanner struct fields:
      cfg *config.Config, registry RegistryReader, exchanges map[string]exchange.Exchange,
      telemetry TelemetryWriter, log *utils.Logger, nowFunc func() time.Time,
      mu sync.Mutex, bars map[string]*barRing (keyed by "{symbol}:{long}:{short}"),
      lastSample map[string]time.Time (keyed by "{symbol}:{exch}"), consecErr int

    TelemetryWriter interface (Task 2 implements):
      WriteCycle(ctx context.Context, summary CycleSummary) error
      WriteEnabledFlag(ctx context.Context, enabled bool) error
      WriteSymbolNoCrossPair(ctx context.Context) error
      WriteCycleFailed(ctx context.Context) error

    Constructor: NewScanner(cfg *config.Config, reader RegistryReader, exchanges map[string]exchange.Exchange, tele TelemetryWriter, log *utils.Logger) *Scanner

    Public method RunCycle(ctx context.Context, now time.Time):
      1. If cfg.PriceGapDiscoveryEnabled is false: telemetry.WriteEnabledFlag(ctx, false); return.
      2. telemetry.WriteEnabledFlag(ctx, true).
      3. Snapshot universe + denylist + threshold + depthFloor + stalenessSec under cfg.RLock().
      4. Snapshot promoted candidates as map[string]bool keyed by "sym:long:short:direction" (read-only via s.registry.List()).
      5. For each symbol in universe:
           a. listings := listingExchanges(sym) — exch IDs where Exchange.GetBBO(sym) returns ok=true.
           b. If len(listings) < 2: telemetry.WriteSymbolNoCrossPair(ctx); continue.
           c. For each (longExch, shortExch) ordered pair where longExch != shortExch:
                If consecErr ≥ 5: telemetry.WriteCycleFailed(ctx); summary.Errors++; goto persist.
                rec := scanPair(...); accumulate into summary.
      6. summary.CompletedAt = nowFunc().Unix(); telemetry.WriteCycle(ctx, summary).

    scanPair gate evaluation order (short-circuit cheapest first):
      Gate 1: Denylist match (sym OR sym@exch on either leg) → ReasonDenylist.
      Gate 2: Bybit blackout (longExch==bybit OR shortExch==bybit during :04-:05:30) → ReasonBybitBlackout.
      Gate 3: BBO sample both legs (sampleLeg helper). On error → ReasonSampleError; consecErr++.
      Gate 4: BBO freshness <90s on both legs (mirror detector.go:194-201) → ReasonStaleBBO.
      Gate 5: Depth probe ≥ floor (min of legL.depthClearable, legR.depthClearable) → ReasonInsufficientDepth.
      Gate 6: ≥4-bar persistence via barRing.allExceedDirected(threshold, models.PriceGapDirectionBidirectional) → ReasonInsufficientPersistence.
      All gates pass: compute magnitude = clamp(0.5 * spreadExcessNorm + 0.3 * depthScore + 0.2 * max(fundingAlignment, 0), 0, 1) * 100. Round to int. WhyRejected = ReasonAccepted.

    Helper functions in scanner.go:
      listingExchanges(sym) []string — returns exch IDs where s.exchanges[id].GetBBO(sym) returns ok=true.
      sampleLeg(ctx, sym, exchID, threshold, now) (legSample, error) — calls Exchange.GetBBO for bid/ask and Exchange.GetDepth for depthClearable; tracks s.lastSample[sym+":"+exchID] for age.
      inBybitBlackout(now time.Time) bool — minute % 60 in [4, 5] OR (minute==5 AND second<30). Hardcoded :04-:05:30 per CLAUDE.local.md scan schedule.
      matchesDenylist(sym, exchID string, denylist []string) bool — checks both sym and sym+"@"+exchID forms.
      fundingAlignment(sym, longExch, shortExch string) float64 — reads from existing funding-rate cache; divides by 8 (Loris ÷8). Returns 0 if data unavailable. Sign indicates whether funding favors trade direction.
      snapshotPromoted() map[string]bool — s.registry.List() then build set of "sym:long:short:direction" keys.
      computeSpreadBps(bidL, askL, bidR, askR float64) float64 — long-short midpoint diff in basis points.

    scanner_static_test.go contains TestScanner_NoRegistryMutators that reads scanner.go via os.ReadFile and asserts:
      - regexp.MustCompile(`\*Registry\b`).Match(src) returns false (only the interface RegistryReader allowed)
      - regexp.MustCompile(`registry\.(Add|Update|Delete|Replace)\(`).Match(src) returns false
    Test fails with descriptive message if either regex matches.

    scanner_test.go fixture pattern: fakeExchange map with stubbed GetBBO + GetDepth returning controlled values; fakeTelemetry capturing WriteCycle calls; fakeRegistryReader returns controlled []models.PriceGapCandidate; tests inject nowFunc for blackout-window determinism.
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && go test ./internal/pricegaptrader/ -run 'TestScanner_' -count=1 -timeout=30s &amp;&amp; go vet ./internal/pricegaptrader/...</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/pricegaptrader/scanner.go` returns 0
    - `test -f internal/pricegaptrader/scanner_test.go` returns 0
    - `test -f internal/pricegaptrader/scanner_static_test.go` returns 0
    - `grep -c 'type Scanner struct' internal/pricegaptrader/scanner.go` returns 1
    - `grep -c 'RegistryReader' internal/pricegaptrader/scanner.go` returns ≥1
    - `grep -E '\*Registry\b' internal/pricegaptrader/scanner.go | wc -l` returns 0 (only the interface, not the concrete type)
    - `grep -E 'registry\.(Add|Update|Delete|Replace)\(' internal/pricegaptrader/scanner.go | wc -l` returns 0
    - `grep -E 'internal/engine|internal/spotengine' internal/pricegaptrader/scanner.go | wc -l` returns 0 (module boundary preserved)
    - `grep -c 'ReasonInsufficientPersistence\|ReasonStaleBBO\|ReasonInsufficientDepth\|ReasonDenylist\|ReasonBybitBlackout\|ReasonSymbolNotListedLong\|ReasonSymbolNotListedShort\|ReasonSampleError' internal/pricegaptrader/scanner.go` returns 8
    - `go test ./internal/pricegaptrader/ -run 'TestScanner_' -count=1` exits 0 with all 15 sub-tests passing
    - Race detector clean: `go test -race ./internal/pricegaptrader/ -run TestScanner_ -count=1`
  </acceptance_criteria>
  <done>
    Scanner implements gate-then-magnitude scoring (D-01), 0-100 integer score (D-02), full sub-score persistence (D-03), reason-coded rejection (D-04), singleton silent-skip (D-08), compile-time read-only (D-13). All 15 unit tests pass including the 1-bar-pump rejection (Pitfall 1 antidote) and canonical-symbol-join (RESEARCH A1 verification).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Telemetry — Redis writes + WS broadcast for pg:scan:* + Tracker scanLoop integration</name>
  <files>internal/pricegaptrader/telemetry.go, internal/pricegaptrader/telemetry_test.go, internal/pricegaptrader/tracker.go</files>
  <read_first>
    - internal/pricegaptrader/metrics.go (existing pure-aggregator + Redis writer pattern; mirror envelope)
    - internal/pricegaptrader/tracker.go (lines 140-146 broadcaster wiring; lines 209-225 throttled WS broadcast pattern; 240-263 subscribeCandidates; 283-335 Start/Stop; 355-397 tickLoop pattern)
    - internal/database/client.go (Redis client surface — exact method signatures for ZAdd/Expire/HSet/RPush/LTrim/Set/HIncrBy)
    - internal/api/ws.go OR internal/api/broadcaster.go — wherever the existing Broadcaster lives; capture BroadcastEvent envelope shape `{type, ts, payload}`
    - internal/pricegaptrader/scanner.go (Task 1 — TelemetryWriter interface + CycleSummary/CycleRecord structs; this task implements)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-RESEARCH.md (Redis Schema table — exact key names + TTLs + WS channels)
  </read_first>
  <behavior>
    - Test 1 (WriteCycle): Calling WriteCycle with a CycleSummary writes to pg:scan:cycles (RPush + LTrim 1000), updates pg:scan:metrics HASH (last_run_at, candidates_seen, accepted, rejected, errors, next_run_in, duration_ms), and broadcasts a pg_scan_cycle WS event with the full summary.
    - Test 2 (per-symbol score ZSET): For each accepted record, ZAdd to pg:scan:scores:{symbol} with score=record.Score and member=JSON-encoded {ts, score, sub_scores}; EXPIRE 7d set on the key.
    - Test 3 (rejections by date): Each rejected record increments pg:scan:rejections:{utc_date} HASH for its reason code; EXPIRE 30d set on the key.
    - Test 4 (WS throttle on metrics): WriteCycle called twice in <5 seconds — pg_scan_metrics WS event broadcast only once (5-second throttle window).
    - Test 5 (WS debounce on score): Multiple WriteCycle calls with score updates for the same symbol within 1 second — pg_scan_score events coalesced into one final broadcast.
    - Test 6 (WriteEnabledFlag): Sets pg:scan:enabled to "0" or "1" string.
    - Test 7 (WriteSymbolNoCrossPair): HIncrBy pg:scan:metrics field symbol_no_cross_pair by 1.
    - Test 8 (WriteCycleFailed): HSet pg:scan:metrics field cycle_failed=1; cleared to 0 after next successful WriteCycle.
    - Test 9 (Tracker integration): Tracker.Start() launches scanLoop via t.wg.Add(1); go t.scanLoop() AFTER the existing assertBBOLiveness launch; scanLoop calls Scanner.RunCycle on PriceGapDiscoveryIntervalSec ticker; respects t.stopCh for graceful shutdown.
    - Test 10 (Tracker pre-warms universe subscriptions): Tracker.Start() after subscribeCandidates also calls subscribeUniverse to fan out Exchange.SubscribeSymbol for every (universe-symbol, exchange) pair (Pitfall 5 mitigation).
    - Test 11 (Tracker no-op when scanner is nil): Tracker constructed without a Scanner does not panic; scanLoop is not launched.
  </behavior>
  <action>
    Create internal/pricegaptrader/telemetry.go:

    Telemetry struct fields:
      db database.Client (exact concrete type; verify via reading metrics.go's usage pattern)
      broker Broadcaster (extracted narrow interface with BroadcastEvent(eventType string, payload interface{}))
      cfg *config.Config (read PriceGapDiscoveryIntervalSec for next_run_in computation)
      log *utils.Logger
      nowFunc func() time.Time
      mu sync.Mutex
      lastMetricsBroad time.Time (5s throttle for pg_scan_metrics)
      pendingScoreSyms map[string]CycleRecord (debounce buffer for pg_scan_score)
      debounceTimer *time.Timer

    Broadcaster interface (extracted from existing api package; narrow surface):
      BroadcastEvent(eventType string, payload interface{})

    Constructor: NewTelemetry(db database.Client, broker Broadcaster, cfg *config.Config, log *utils.Logger) *Telemetry

    WriteCycle(ctx, summary) error implementation:
      1. body, _ := json.Marshal(summary); db.RPush(ctx, "pg:scan:cycles", body); db.LTrim(ctx, "pg:scan:cycles", -1000, -1).
      2. nextRunIn := cfg.PriceGapDiscoveryIntervalSec; db.HSet(ctx, "pg:scan:metrics", map with keys: last_run_at, candidates_seen, accepted, rejected, errors, duration_ms, next_run_in, cycle_failed=0).
      3. For each accepted record: key := "pg:scan:scores:"+rec.Symbol; point := JSON-marshal {ts, score, sub_scores: {spread_bps, depth_score, freshness_age_s, funding_bps_h, persistence_bars}}; db.ZAdd(ctx, key, float64(rec.Score), point); db.Expire(ctx, key, 7*24*time.Hour); queueScoreBroadcast(rec).
      4. date := time.Unix(summary.CompletedAt, 0).UTC().Format("2006-01-02"); for reason, count in summary.WhyRejected: db.HIncrBy(ctx, "pg:scan:rejections:"+date, string(reason), int64(count)); db.Expire(ctx, "pg:scan:rejections:"+date, 30*24*time.Hour).
      5. broker.BroadcastEvent("pg_scan_cycle", summary). maybeBroadcastMetrics(summary).
      6. Return nil; Redis errors logged but non-fatal (audit semantics).

    maybeBroadcastMetrics: takes mu, checks nowFunc().Sub(lastMetricsBroad) >= 5s, updates lastMetricsBroad, broadcasts pg_scan_metrics with the metrics fields only (not the full summary).

    queueScoreBroadcast / flushPendingScores: 1-second debounce. queueScoreBroadcast adds rec to pendingScoreSyms keyed by Symbol; if debounceTimer is nil, time.AfterFunc(1*time.Second, flushPendingScores). flushPendingScores swaps out pendingScoreSyms under lock and broadcasts pg_scan_score per symbol.

    WriteEnabledFlag(ctx, enabled bool) error:
      flag := "0"; if enabled { flag = "1" }; db.Set(ctx, "pg:scan:enabled", flag, 0).

    WriteSymbolNoCrossPair(ctx) error:
      db.HIncrBy(ctx, "pg:scan:metrics", "symbol_no_cross_pair", 1).

    WriteCycleFailed(ctx) error:
      db.HSet(ctx, "pg:scan:metrics", map[string]interface{}{"cycle_failed": 1}).

    Read-side helpers (used by Task 3 REST endpoints):
      GetState(ctx) (StateResponse, error) — HGetAll pg:scan:metrics + LRange pg:scan:cycles -1 -1 (last cycle); compose response struct including enabled flag (Get pg:scan:enabled), last_run_at, next_run_in, candidates_seen, accepted, rejected, errors, duration_ms, why_rejected (today's pg:scan:rejections:{date} HGetAll), score_snapshot (last cycle's accepted records).
      GetScores(ctx, symbol) (ScoresResponse, error) — ZRange pg:scan:scores:{symbol} 0 -1 WITHSCORES; parse JSON members; threshold_band reads cfg.PriceGapAutoPromoteScore.

    StateResponse + ScoresResponse struct definitions match the response envelope documented in UI-SPEC.md "Interaction & Data Contract" section.

    Modify internal/pricegaptrader/tracker.go:

    Add fields to Tracker struct:
      scanner *Scanner (nil if discovery feature wired off)
      telemetry *Telemetry

    Add functional-option setters (or modify NewTracker constructor to accept):
      WithScanner(s *Scanner) Option
      WithTelemetry(t *Telemetry) Option

    In Tracker.Start() (after the existing assertBBOLiveness launch around line 290):
      Add call to t.subscribeUniverse() if t.scanner != nil.
      If t.scanner != nil:
        t.wg.Add(1)
        go t.scanLoop()

    Add scanLoop method (mirror tickLoop at lines 355-397):
      defer t.wg.Done()
      intervalSec := t.cfg.PriceGapDiscoveryIntervalSec; if intervalSec <= 0 { intervalSec = 300 }
      interval := time.Duration(intervalSec) * time.Second
      firstTick := time.After(13 * time.Second) (offset off Bybit blackout boundary; differs from tickLoop's 7s to avoid colliding)
      var ticker *time.Ticker
      Loop:
        select case <-t.stopCh: if ticker != nil { ticker.Stop() }; return
        case <-firstTick: ticker = time.NewTicker(interval); t.scanner.RunCycle(context.Background(), time.Now())
        case <-tickerC(ticker): t.scanner.RunCycle(context.Background(), time.Now())
      tickerC helper handles nil ticker case (returns nil channel that blocks forever).

    Add subscribeUniverse method (Pitfall 5 mitigation):
      For each sym in t.cfg.PriceGapDiscoveryUniverse:
        For each (exchID, ex) in t.exchanges:
          if err := ex.SubscribeSymbol(sym); err != nil { t.log.Warnf("scanner subscribe %s on %s: %v", sym, exchID, err) }

    telemetry_test.go uses fake Redis client capturing all method calls (with timestamps) and fake Broadcaster capturing events. tracker_subscribe_test.go (existing) extended with TestTracker_SubscribeUniverse + TestTracker_ScanLoop (drive a fake Scanner that records RunCycle invocations; assert ≥1 invocation after firstTick + interval).
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && go test ./internal/pricegaptrader/ -run 'TestTelemetry_|TestTracker_ScanLoop|TestTracker_SubscribeUniverse' -count=1 -timeout=30s &amp;&amp; go vet ./internal/pricegaptrader/...</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/pricegaptrader/telemetry.go` returns 0
    - `test -f internal/pricegaptrader/telemetry_test.go` returns 0
    - `grep -c 'type Telemetry struct' internal/pricegaptrader/telemetry.go` returns 1
    - `grep -c 'pg:scan:cycles\|pg:scan:scores\|pg:scan:rejections\|pg:scan:metrics\|pg:scan:enabled' internal/pricegaptrader/telemetry.go` returns ≥5 (each Redis key referenced)
    - `grep -c 'pg_scan_cycle\|pg_scan_metrics\|pg_scan_score' internal/pricegaptrader/telemetry.go` returns ≥3 (each WS event referenced)
    - `grep -c 'scanLoop\|subscribeUniverse' internal/pricegaptrader/tracker.go` returns ≥2 (both methods exist)
    - `grep -E 'internal/engine|internal/spotengine' internal/pricegaptrader/telemetry.go internal/pricegaptrader/tracker.go | wc -l` returns 0 (module boundary preserved)
    - `go test ./internal/pricegaptrader/ -run 'TestTelemetry_' -count=1` exits 0 with all 8 sub-tests passing
    - `go test ./internal/pricegaptrader/ -run 'TestTracker_(ScanLoop|SubscribeUniverse)' -count=1` exits 0 with all 3 sub-tests passing
    - Existing tracker tests still pass: `go test ./internal/pricegaptrader/ -run 'TestTracker_Subscribe$|TestTrackerHotReload' -count=1`
    - Race detector clean: `go test -race ./internal/pricegaptrader/ -run 'TestTelemetry_|TestTracker_ScanLoop' -count=1`
    - `gofmt -l internal/pricegaptrader/telemetry.go internal/pricegaptrader/telemetry_test.go internal/pricegaptrader/tracker.go` returns empty
  </acceptance_criteria>
  <done>
    Telemetry implements all 4 TelemetryWriter methods + 2 read-side helpers; Redis schema (5 keys) + WS broadcast (3 events) + 5s throttle + 1s debounce all working. Tracker.Start() launches scanLoop alongside existing tickLoop; subscribeUniverse pre-warms universe BBO subscriptions. Module boundary preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire REST handlers GET /api/pg/discovery/state and /api/pg/discovery/scores/{symbol}</name>
  <files>internal/api/pricegap_discovery_handlers.go, internal/api/pricegap_discovery_handlers_test.go, internal/api/server.go</files>
  <read_first>
    - internal/api/server.go (after Plan 03 Task 1 has touched it — Server now holds CandidateRegistry; this task adds telemetry field)
  - internal/pricegaptrader/validate.go
    - internal/api/pricegap_handlers.go (existing GET handler patterns + writeJSON envelope)
    - internal/api/handlers.go (Bearer token auth middleware wiring; route registration helper)
    - internal/pricegaptrader/telemetry.go (Task 2 — GetState, GetScores read-side helpers)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-UI-SPEC.md (Interaction & Data Contract — exact response envelopes the frontend in Plan 05 expects)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-CONTEXT.md (D-17 REST seed + WS push pattern)
  </read_first>
  <behavior>
    - Test 1 (handlePgDiscoveryState happy path): GET /api/pg/discovery/state returns 200 with body `{ok: true, data: {enabled, last_run_at, next_run_in, candidates_seen, accepted, rejected, errors, duration_ms, why_rejected: {reason: count}, score_snapshot: [{symbol, score, sub_scores, gates_passed, gates_failed}]}}`.
    - Test 2 (handlePgDiscoveryState empty state): When no cycle has ever run, returns 200 with enabled=false (or true if flag set), last_run_at=0 (or null), candidates_seen=0, why_rejected={}, score_snapshot=[]. UI handles "Never" rendering per UI-SPEC.
    - Test 3 (handlePgDiscoveryScores happy path): GET /api/pg/discovery/scores/BTCUSDT returns 200 with body `{ok: true, data: {symbol: "BTCUSDT", points: [{ts, score, sub_scores}], threshold_band: {auto_promote: 60}}}`.
    - Test 4 (handlePgDiscoveryScores unknown symbol): GET /api/pg/discovery/scores/UNKNOWNUSDT returns 200 with empty points array (NOT 404 — empty result is normal; UI shows "No score history yet" empty state).
    - Test 5 (handlePgDiscoveryScores invalid symbol): GET /api/pg/discovery/scores/INVALID-SYMBOL returns 400 with error mentioning "invalid symbol format" (regex `^[A-Z0-9]{2,20}USDT$`). Reuses Plan 03's pricegaptrader.ValidateCandidates? No — extract a new helper `pricegaptrader.ValidateSymbol(s string) error` since this is per-symbol not per-candidate.
    - Test 6 (auth middleware): GET /api/pg/discovery/state without Bearer token returns 401 (existing middleware applies).
    - Test 7 (route registration): Both routes registered under the existing pricegap route group; smoke test by hitting the routes via httptest.
    - Test 8 (Server holds telemetry field): Server.NewServer now accepts a *pricegaptrader.Telemetry parameter; missing-telemetry construction is a compile error.
  </behavior>
  <action>
    Create internal/api/pricegap_discovery_handlers.go:

    Handlers (mirror existing pricegap_handlers.go style — http.HandlerFunc returning Response envelope via writeJSON):

      func (s *Server) handlePgDiscoveryState(w http.ResponseWriter, r *http.Request):
        ctx := r.Context()
        state, err := s.telemetry.GetState(ctx)
        if err != nil { writeJSON(w, http.StatusInternalServerError, Response{Error: err.Error()}); return }
        writeJSON(w, http.StatusOK, Response{OK: true, Data: state})

      func (s *Server) handlePgDiscoveryScores(w http.ResponseWriter, r *http.Request):
        symbol := mux.Vars(r)["symbol"] // or whatever the existing router uses; verify via reading server.go
        if err := pricegaptrader.ValidateSymbol(symbol); err != nil {
          writeJSON(w, http.StatusBadRequest, Response{Error: "invalid symbol format: " + err.Error()})
          return
        }
        scores, err := s.telemetry.GetScores(r.Context(), symbol)
        if err != nil { writeJSON(w, http.StatusInternalServerError, Response{Error: err.Error()}); return }
        writeJSON(w, http.StatusOK, Response{OK: true, Data: scores})

    Add ValidateSymbol helper to internal/pricegaptrader/validate.go (extracted in Plan 03 Task 2):
      func ValidateSymbol(s string) error {
        if !regexp.MustCompile(`^[A-Z0-9]{2,20}USDT$`).MatchString(s) {
          return fmt.Errorf("symbol %q does not match canonical form", s)
        }
        return nil
      }
    NOTE: This file is shared with Plan 03 Task 2. If Plan 03 has already shipped, append ValidateSymbol to the existing validate.go. If Plan 03 ships in parallel (same wave), coordinate by having Plan 04 add the function to validate.go and Plan 03 import + use it where applicable. Both files declare validate.go in files_modified; orchestrator wave runner serializes file overlap. **Update Plan 04 files_modified to include internal/pricegaptrader/validate.go.**

    Modify internal/api/server.go:
      Add field: telemetry *pricegaptrader.Telemetry
      Update NewServer constructor signature to accept *pricegaptrader.Telemetry as a required parameter.
      Update cmd/arb/main.go (already touched in Plan 03 Task 1 for registry) to also pass telemetry.

    Modify route registration in server.go (find the existing pricegap route group, e.g. `r.HandleFunc("/api/pg/...", ...)`):
      r.HandleFunc("/api/pg/discovery/state", s.handlePgDiscoveryState).Methods("GET")
      r.HandleFunc("/api/pg/discovery/scores/{symbol}", s.handlePgDiscoveryScores).Methods("GET")
    Both inherit the existing Bearer-token auth middleware.

    pricegap_discovery_handlers_test.go uses httptest.NewRecorder + httptest.NewRequest pattern. Build a fake Telemetry implementing GetState + GetScores; spin up Server with the fake; assert response bodies via JSON unmarshal.
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && go test ./internal/api/ -run 'TestPgDiscovery' -count=1 -timeout=30s &amp;&amp; go vet ./internal/api/...</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/api/pricegap_discovery_handlers.go` returns 0
    - `test -f internal/api/pricegap_discovery_handlers_test.go` returns 0
    - `grep -c 'handlePgDiscoveryState\|handlePgDiscoveryScores' internal/api/pricegap_discovery_handlers.go` returns 2
    - `grep -c '/api/pg/discovery/state\|/api/pg/discovery/scores' internal/api/server.go` returns ≥2 (both routes registered)
    - `grep -c 'telemetry\s*\*pricegaptrader\.Telemetry' internal/api/server.go` returns ≥1
    - `grep -c 'pricegaptrader\.ValidateSymbol' internal/api/pricegap_discovery_handlers.go` returns ≥1
    - `grep -c 'func ValidateSymbol' internal/pricegaptrader/validate.go` returns 1 (added in this task or co-existing with Plan 03)
    - `go test ./internal/api/ -run TestPgDiscovery -count=1` exits 0 with all 8 sub-tests passing
    - `go build ./...` exits 0 (server bootstrap compiles end-to-end with telemetry wired)
    - Existing handler tests still pass: `go test ./internal/api/ -run 'TestPriceGap|TestConfig|TestPostConfigCandidates' -count=1`
    - `gofmt -l internal/api/pricegap_discovery_handlers.go internal/api/pricegap_discovery_handlers_test.go internal/api/server.go internal/pricegaptrader/validate.go` returns empty
  </acceptance_criteria>
  <done>
    Two REST endpoints (GET /api/pg/discovery/state and GET /api/pg/discovery/scores/{symbol}) ship with documented response envelopes matching UI-SPEC.md. Server bootstrap holds telemetry field. ValidateSymbol helper added. Plan 05 frontend can fetch from these endpoints directly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exchange WS cache → Scanner | Adapter-side data; trusted (already used by detector). |
| Scanner → Telemetry → Redis DB 2 | Internal write path; pg:scan:* keys are scanner-owned (no other writers). |
| HTTP client → /api/pg/discovery/* | Authenticated (Bearer token); existing dashboard auth surface. |
| Redis pg:scan:* → Telemetry.GetState/GetScores → REST → frontend | Read path; data shaped by Telemetry helpers, not raw Redis. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-26 | Tampering | Scanner casts RegistryReader to *Registry to call Add | mitigate | scanner_static_test.go fails if `\*Registry\b` or `registry\.(Add\|Update\|Delete\|Replace)\(` regex matches scanner.go. Acceptance criterion enforced. |
| T-11-27 | Tampering | Operator universe contains malformed symbol that exchange adapter accepts as ambiguous | mitigate | Plan 01 validation regex `^[A-Z0-9]{2,20}USDT$` enforced at config load; Scanner trusts validated input. |
| T-11-28 | DoS | Exchange WS reconnection storms cause every cycle to fail; consecErr stuck high | mitigate | consecErr resets on next successful sample; cycle-failed flag captures the run; LIMIT 5 consecutive failures before short-circuiting the cycle (RESEARCH §1 budget). |
| T-11-29 | DoS | Scanner cycle stalls on slow exchange GetDepth call | mitigate | sampleLeg uses context.WithTimeout(2*time.Second) for GetDepth (which does I/O); GetBBO is in-process cache (no I/O). Cycle wall-clock budget = 5s × len(universe). |
| T-11-30 | Information disclosure | Telegram alert leaks raw exchange API error string | mitigate | Scanner emits redacted reason codes only (ReasonSampleError); raw err strings stay in Go logs, never reach Telegram (Telegram alerts are Phase 12+ scope anyway). |
| T-11-31 | Information disclosure | pg:scan:* Redis keys leak strategy info if Redis is exposed | accept | Existing Redis posture: localhost-bound, password-protected; new keys inherit same posture. No new exposure surface. |
| T-11-32 | Tampering | Race between WriteCycle's HSet "cycle_failed=0" and a concurrent WriteCycleFailed setting it to 1 | accept | Last-writer-wins on Redis HSet is acceptable for a forensic flag; the next WriteCycle will reflect the most-recent state. |
| T-11-33 | Information disclosure | /api/pg/discovery/scores/{symbol} URL parameter unsanitized → Redis injection | mitigate | ValidateSymbol regex `^[A-Z0-9]{2,20}USDT$` rejects any character outside the alphabet; symbol becomes part of a Redis key but cannot break out of the namespace. |
| T-11-34 | DoS | Operator floods GET /api/pg/discovery/state every 100ms | accept | Existing dashboard auth + rate limit middleware applies (verify; if missing, that's an existing gap, not Phase 11 specific). |
| T-11-35 | DoS | Scanner pre-warms 20 symbols × 6 exchanges = 120 SubscribeSymbol calls; rate-limit storm at startup | mitigate | SubscribeSymbol is a WS subscription (not REST); existing adapter SubscribeSymbol is debounced via the WS connection itself. firstTick offset of 13s gives the WS time to settle before first cycle. |
| T-11-36 | Tampering | Operator changes universe at runtime via pg-admin; scanner sees stale universe | accept | Scanner re-snapshots cfg.PriceGapDiscoveryUniverse at the START of every cycle (under cfg.RLock); operator sees the change reflected within one tick (≤300s by default). Acceptable for a discovery scanner. |
</threat_model>

<verification>
- All Plan 04 tests pass: `go test ./internal/pricegaptrader/ ./internal/api/ -count=1 -timeout=60s` exits 0.
- Race detector clean: `go test -race ./internal/pricegaptrader/ -run 'TestScanner_|TestTelemetry_|TestTracker_ScanLoop' -count=1`.
- Existing pricegap tests still pass (no regression): `go test ./internal/pricegaptrader/ -run 'TestDetector|TestTracker_(Subscribe|HotReload)|TestRiskGate' -count=1`.
- `make build` succeeds (frontend embed unchanged).
- `gofmt -l internal/pricegaptrader/scanner.go internal/pricegaptrader/scanner_test.go internal/pricegaptrader/scanner_static_test.go internal/pricegaptrader/telemetry.go internal/pricegaptrader/telemetry_test.go internal/pricegaptrader/tracker.go internal/api/pricegap_discovery_handlers.go internal/api/pricegap_discovery_handlers_test.go internal/api/server.go` returns empty.
- Scanner-static enforcement: `go test ./internal/pricegaptrader/ -run TestScanner_NoRegistryMutators -count=1` exits 0.
</verification>

<success_criteria>
- Scanner runs at configured interval when enabled; goroutine present-but-no-op when disabled.
- All 5 hard gates evaluated (denylist, blackout, sample, freshness, depth, persistence) before non-zero score (D-01).
- Score is integer 0-100; sub-scores persisted alongside (D-02, D-03).
- Rejected candidates captured with reason code; every (sym × pair) writes a record (D-04).
- Scanner is read-only at compile time (D-13) — static test enforces.
- pg:scan:* Redis schema matches RESEARCH.md §"Redis Schema" table.
- WS broadcasts: pg_scan_cycle (no throttle), pg_scan_metrics (5s), pg_scan_score (1s debounce).
- Tracker.Start launches scanLoop and subscribeUniverse alongside existing flows; graceful shutdown via stopCh.
- REST endpoints GET /api/pg/discovery/state and /scores/{symbol} return UI-SPEC envelope.
- Plan 05 (UI) can fetch + render against these endpoints without backend changes.
</success_criteria>

<validation>
**Validation Coverage (per VALIDATION.md Dimension 8):**
- Unit tests for new functions: scoring math (15 sub-tests in scanner_test.go), persistence gate (Test 3), BBO freshness check (Test 4), depth probe (Test 5), denylist matching (Tests 6+7), blackout window (Test 8), singleton skip (Test 9), error budget (Test 14), sub-score persistence (Test 15).
- Read-only invariant test for scanner: TestScanner_NoRegistryMutators in scanner_static_test.go (regex grep on scanner.go source — no `\*Registry\b`, no `registry\.(Add|Update|Delete|Replace)\(`).
- Default-OFF verification: TestScanner_DefaultOff (Test 1) — RunCycle with PriceGapDiscoveryEnabled=false produces only enabled-flag write, no per-symbol records.
- Symbol normalization cross-exchange BBO join test: Test 12 (canonical BTCUSDT join across multiple fake adapters).
- Telemetry tests: 8 sub-tests covering all 4 TelemetryWriter methods + WS throttle + WS debounce + Redis schema.
- Tracker integration tests: 3 sub-tests covering scanLoop launch, subscribeUniverse pre-warm, no-op when scanner is nil.
- REST handler tests: 8 sub-tests covering happy paths, empty states, validation, auth, route registration.
- Wave 0 dependencies satisfied: this plan creates scanner_test.go, scanner_static_test.go, telemetry_test.go, pricegap_discovery_handlers_test.go (all listed in VALIDATION.md Wave 0).
- Sampling rate: combined Plan 04 tests run in <60s under -count=1; all individual files <30s.
</validation>

<output>
After completion, create `.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-04-SUMMARY.md` documenting:
- Scanner gate evaluation order (denylist → blackout → sample → freshness → depth → persistence)
- Magnitude weights chosen (0.5 spread / 0.3 depth / 0.2 funding)
- Reason code enumeration (8 values)
- Redis schema (5 keys with TTLs)
- WS event types (3, with throttle/debounce policies)
- Tracker scanLoop firstTick offset (13s) chosen to avoid colliding with tickLoop's 7s
- subscribeUniverse pre-warm count = len(universe) × len(supported_exchanges)
- REST endpoint response shapes matching UI-SPEC.md
- Test counts: 15 scanner unit + 8 telemetry + 3 tracker + 8 REST handler = 34 sub-tests
- Confirmation that scanner is read-only (compile-time enforced)
- Confirmation that default-OFF produces only an enabled-flag write
</output>
