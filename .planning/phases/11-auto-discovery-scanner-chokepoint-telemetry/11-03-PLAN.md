---
phase: 11
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - internal/api/handlers.go
  - internal/api/server.go
  - internal/api/pricegap_handlers.go
  - cmd/arb/main.go
  - cmd/pg-admin/main.go
  - internal/api/handlers_registry_migration_test.go
  - cmd/pg-admin/main_registry_test.go
autonomous: true
requirements:
  - PG-DISC-04
user_setup: []

must_haves:
  truths:
    - "After this plan, ZERO direct mutations of cfg.PriceGapCandidates remain anywhere outside internal/pricegaptrader/registry.go."
    - "Dashboard POST /api/config candidate path routes through *Registry.Replace via the chokepoint."
    - "pg-admin gains four new commands (candidates list / add / delete / replace) all routing through *Registry."
    - "Server bootstrap constructs exactly one *Registry instance and passes it to all writers + future scanner."
    - "Existing dashboard candidate CRUD behavior preserved end-to-end (same validation, same active-position guard, same response envelope)."
    - "Concurrent dashboard + pg-admin writes against the SAME running config produce zero lost mutations (success criterion #3)."
  artifacts:
    - path: "internal/api/handlers.go"
      provides: "POST /api/config candidate path migrated to s.registry.Replace(ctx, source, next); zero direct mutations remain"
      contains: "s.registry.Replace"
    - path: "internal/api/server.go"
      provides: "Server struct holds *pricegaptrader.Registry; NewServer constructor accepts it"
      contains: "registry *pricegaptrader.Registry"
    - path: "cmd/arb/main.go"
      provides: "Bootstrap constructs Registry once and passes the same instance to API server + Tracker"
      contains: "pricegaptrader.NewRegistry"
    - path: "cmd/pg-admin/main.go"
      provides: "Four new candidate subcommands (list/add/delete/replace) routing through Registry"
      contains: "registry.Add"
  key_links:
    - from: "internal/api/handlers.go (POST /api/config)"
      to: "internal/pricegaptrader/registry.go *Registry.Replace"
      via: "s.registry.Replace(ctx, \"dashboard-handler\", validatedNext)"
      pattern: "s\\.registry\\.Replace\\("
    - from: "cmd/pg-admin/main.go"
      to: "internal/pricegaptrader/registry.go"
      via: "in-process import — pg-admin links the registry package directly"
      pattern: "pricegaptrader\\.NewRegistry"
    - from: "cmd/arb/main.go"
      to: "internal/api/server.go AND internal/pricegaptrader/tracker.go"
      via: "single *Registry instance shared between dashboard server and tracker (Plan 04 scanner takes it as RegistryReader)"
      pattern: "pricegaptrader\\.NewRegistry"
---

<objective>
Hard-cut migration of all `cfg.PriceGapCandidates` writers onto the Registry chokepoint from Plan 02. Two writers exist today: (1) `internal/api/handlers.go:1700` POST /api/config path; (2) `cmd/pg-admin/main.go` (no candidate-mutation commands today — this plan ADDS them, routed through Registry from day one). Server bootstrap is updated to construct one Registry instance shared between API + Tracker.

Purpose: Close Pitfall 2 (three-writer race) before Phase 12 ships. Implements decision D-11 (hard-cut, no legacy `cfg.PriceGapCandidates = ...` paths remain). After this plan, scanner promotion (Phase 12) has only the Registry to write through — no alternative path exists.
Output: Migrated handlers.go (one-line semantic swap with full validation preserved), 4 new pg-admin subcommands, bootstrap wiring, and an end-to-end integration test exercising both writers concurrently.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-CONTEXT.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-RESEARCH.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-01-PLAN.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-02-PLAN.md

<interfaces>
<!-- From Plan 02: -->
<!-- - pricegaptrader.Registry struct with Add(ctx, source string, c models.PriceGapCandidate) error -->
<!-- - Update(ctx, source, idx, c) error / Delete(ctx, source, idx) error / Replace(ctx, source, []models.PriceGapCandidate) error -->
<!-- - Get(idx) (PriceGapCandidate, bool) / List() []PriceGapCandidate -->
<!-- - NewRegistry(cfg *config.Config, db RegistryAuditWriter, log *utils.Logger) *Registry -->

<!-- From existing codebase (verified during planning): -->
<!-- - handlers.go:1700 — `s.cfg.PriceGapCandidates = next` is the ONLY direct mutation site to migrate -->
<!-- - handlers.go:1665-1700 — validation/normalize/guardActivePositionRemoval helpers stay in handlers.go (Registry expects pre-validated input, per Plan 02 Task 2 Replace contract) -->
<!-- - cmd/pg-admin/main.go — has no candidate-mutation commands today; only Redis disable-flag writes -->
<!-- - server.go — Server struct holds *config.Config; constructor signature evolves to accept *Registry -->
<!-- - cmd/arb/main.go — main bootstrap; constructs config, db client, server, tracker -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire *Registry into Server bootstrap; migrate handlers.go POST /api/config</name>
  <files>internal/api/server.go, internal/api/handlers.go, internal/api/pricegap_handlers.go, cmd/arb/main.go, internal/api/handlers_registry_migration_test.go</files>
  <read_first>
    - internal/api/server.go (full file — Server struct definition, NewServer constructor signature, current dependencies on *config.Config + *database.Client + *pricegaptrader.Tracker)
    - internal/api/handlers.go (lines 1655-1710 — POST /api/config candidate path; full validate/normalize/guardActivePositionRemoval block including any helper function calls)
    - internal/api/pricegap_handlers.go (any candidate-mutation paths — verify NO additional mutation sites exist)
    - cmd/arb/main.go (full file — bootstrap order; where config is loaded, db client constructed, server built, tracker started; identify the right insertion point for `pricegaptrader.NewRegistry` between config-load and server-construction)
    - internal/pricegaptrader/registry.go (Plan 02 Task 2 — method signatures including the `source` parameter)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-CONTEXT.md (D-11 hard-cut migration; D-09 typed methods)
  </read_first>
  <behavior>
    - Test 1 (handlers.go end-to-end): POST /api/config with a candidate-mutation payload (existing fixture) returns 200 with the same response envelope as before; on-disk config.json contains the new candidate; cfg.PriceGapCandidates in memory matches.
    - Test 2 (validation preserved): POST /api/config with an INVALID candidate (e.g. malformed symbol or duplicate) returns the same 400 error response as before (validation runs in handlers.go BEFORE Registry call).
    - Test 3 (active-position guard preserved): POST /api/config attempting to remove a candidate that has an active position returns the existing guard error; Registry is NOT called.
    - Test 4 (audit source): Successful POST /api/config writes a `pg:registry:audit` row with `source="dashboard-handler"`.
    - Test 5 (no direct mutations remain): Static grep `s\.cfg\.PriceGapCandidates\s*=` in `internal/api/` returns zero matches after migration.
    - Test 6 (Server has registry): Server struct exposes registry field; NewServer constructor signature includes the registry parameter; missing-registry construction is a compile error.
  </behavior>
  <action>
    **Step 1 — Server struct evolution (`internal/api/server.go`):**

    Add `registry *pricegaptrader.Registry` field to the Server struct. Update `NewServer` constructor signature to accept `*pricegaptrader.Registry` as a required parameter (NOT optional — server cannot start without it once Plan 03 ships). Update every call site of `NewServer` in the codebase (likely just `cmd/arb/main.go`).

    **Step 2 — Migrate handlers.go:1700 (`internal/api/handlers.go`):**

    Locate the existing block at lines ~1665-1710 in `internal/api/handlers.go`:
    ```go
    if pg.Candidates != nil {
        next := make([]models.PriceGapCandidate, len(*pg.Candidates))
        copy(next, *pg.Candidates)
        // ... normalize, validate, guardActivePositionRemoval ...
        s.cfg.PriceGapCandidates = next   // <-- THIS LINE
    }
    ```

    Replace ONLY the assignment line with:
    ```go
    if err := s.registry.Replace(r.Context(), "dashboard-handler", next); err != nil {
        s.cfg.Unlock() // ensure lock is released before writing error response
        writeJSON(w, http.StatusInternalServerError, Response{Error: fmt.Sprintf("registry replace: %v", err)})
        return
    }
    ```

    **CRITICAL:** Verify the locking pattern around line 1700 — handlers.go currently holds `s.cfg.Lock()` before the assignment. Registry.Replace internally takes the lock. So the handler MUST RELEASE the lock before calling Registry.Replace, OR Registry.Replace must be lock-aware. **Pick:** handler releases lock before Registry call. Registry takes its own lock.

    Refactor sequence:
    1. Validate + normalize + guardActivePositionRemoval — these still run UNDER `s.cfg.Lock()` because they read other config state.
    2. After validation succeeds, capture `next` slice as a local variable.
    3. `s.cfg.Unlock()` — release before calling Registry.
    4. Call `s.registry.Replace(r.Context(), "dashboard-handler", next)`.
    5. Continue with the existing response-writing code.

    Keep the response envelope unchanged: `writeJSON(w, http.StatusOK, Response{OK: true})` (or whatever the existing path returns).

    DO NOT modify the validation/normalize/guardActivePositionRemoval helpers themselves. DO NOT change the request DTO. DO NOT change the response shape.

    Audit grep: after migration, `grep -nE 's\.cfg\.PriceGapCandidates\s*=' internal/api/handlers.go internal/api/pricegap_handlers.go` MUST return zero matches.

    **Step 3 — Bootstrap (`cmd/arb/main.go`):**

    After `cfg, err := config.LoadJSON(...)` and `db, err := database.New(...)` lines, BEFORE constructing the API server:

    ```go
    // Phase 11 (PG-DISC-04): single Registry instance shared between API server,
    // pg-admin (separate process), and Tracker scanner (Plan 04, RegistryReader view).
    registry := pricegaptrader.NewRegistry(cfg, db, logger)
    ```

    Pass `registry` to `NewServer` and to the Tracker constructor (Tracker integration in Plan 04). For now, the Tracker constructor still takes optional fields; document the wiring in a comment.

    **Step 4 — Migration test (`internal/api/handlers_registry_migration_test.go`):**

    NEW file — does NOT exist today. Create with these tests:
    - `TestPostConfigCandidates_RoutesThroughRegistry`: spin up a test Server with a fake Registry that captures Replace calls; POST a valid candidate-mutation; assert exactly one Replace call with `source="dashboard-handler"` and the expected slice.
    - `TestPostConfigCandidates_ValidationFails_RegistryNotCalled`: POST with malformed candidate; assert Registry.Replace was NOT called; response is 400.
    - `TestPostConfigCandidates_ActivePositionGuard_RegistryNotCalled`: seed an active position; POST removing that candidate; assert Registry.Replace was NOT called; response is the existing guard error.
    - `TestPostConfigCandidates_RegistryError_Returns500`: fake Registry returns error from Replace; assert response is 500 with the wrapped error.
    - `TestPostConfigCandidates_NoDirectMutationsInPackage`: read the source of `internal/api/handlers.go` and `internal/api/pricegap_handlers.go`; regex-assert zero matches of `s?\.cfg\.PriceGapCandidates\s*=`.

    Use a `fakeRegistry` type that satisfies the same method surface as `*pricegaptrader.Registry` — extract a small interface in `internal/api/server.go` if needed:
    ```go
    type CandidateRegistry interface {
        Replace(ctx context.Context, source string, next []models.PriceGapCandidate) error
        Add(ctx context.Context, source string, c models.PriceGapCandidate) error
        // (Get/List from RegistryReader if needed by handlers; verify by reading current handlers.go for any reads)
    }
    ```
    Then `Server.registry` is typed as `CandidateRegistry` (interface) so tests can swap a fake. Production wiring still passes `*pricegaptrader.Registry` (which satisfies the interface).
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && go test ./internal/api/ -run 'TestPostConfigCandidates' -count=1 -timeout=30s && go vet ./internal/api/... ./cmd/arb/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 's?\.cfg\.PriceGapCandidates\s*=' internal/api/handlers.go internal/api/pricegap_handlers.go | wc -l` returns 0
    - `grep -c 's\.registry\.Replace' internal/api/handlers.go` returns ≥1
    - `grep -c 'pricegaptrader\.NewRegistry' cmd/arb/main.go` returns exactly 1
    - `grep -c 'registry\s*\*pricegaptrader\.Registry\|registry\s*CandidateRegistry' internal/api/server.go` returns ≥1
    - `go test ./internal/api/ -run TestPostConfigCandidates -count=1` exits 0 with all 5 sub-tests passing
    - Existing handler tests still pass: `go test ./internal/api/ -run 'TestPriceGap|TestConfig' -count=1` exits 0
    - `go build ./...` exits 0 (server bootstrap compiles end-to-end)
    - `go vet ./...` exits 0
    - `gofmt -l internal/api/handlers.go internal/api/server.go cmd/arb/main.go internal/api/handlers_registry_migration_test.go` returns empty
  </acceptance_criteria>
  <done>
    Server holds a CandidateRegistry interface field; handlers.go POST /api/config routes the candidate slice through `s.registry.Replace(ctx, "dashboard-handler", next)`; validation + active-position guard + response envelope unchanged; cmd/arb/main.go constructs one Registry instance and passes it to NewServer; static grep proves zero direct mutations remain in internal/api/.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pg-admin candidates subcommands routed through *Registry</name>
  <files>cmd/pg-admin/main.go, cmd/pg-admin/main_registry_test.go</files>
  <read_first>
    - cmd/pg-admin/main.go (full file — current command surface, flag parsing pattern, config-loading helper, db-client construction)
    - internal/pricegaptrader/registry.go (Plan 02 — method signatures, ErrDuplicateCandidate / ErrCapExceeded / ErrIndexOutOfRange sentinels)
    - internal/api/handlers.go (validation helpers around line 1665 — pg-admin must run the same validation BEFORE calling Registry, since Registry expects pre-validated input per Plan 02 Replace contract)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-RESEARCH.md (§"pg-admin in-process vs RPC" — recommendation: in-process import; concurrent-with-bot is acceptable because Registry's reload-from-disk + atomic-rename handles it)
  </read_first>
  <behavior>
    - Test 1 (`pg-admin candidates list`): Lists all candidates from cfg.PriceGapCandidates as JSON or table; exits 0.
    - Test 2 (`pg-admin candidates add ...`): Adds a candidate via Registry.Add; exits 0 on success; exits non-zero with stderr message on duplicate or cap exceeded.
    - Test 3 (`pg-admin candidates delete --idx N`): Removes idx N via Registry.Delete; exits 0 on success; exits non-zero on out-of-range.
    - Test 4 (`pg-admin candidates replace --file <path>`): Reads JSON file containing array of candidates, validates them with the same helper as handlers.go, calls Registry.Replace; exits 0 on success.
    - Test 5 (audit source): All four subcommands write `pg:registry:audit` rows with `source="pg-admin"`.
    - Test 6 (running while bot is live): Multi-process test exercises pg-admin invocation while a separate Registry instance (simulating the bot daemon) is also calling Add — final state is correct (zero lost mutations) — REUSE the multi-process test from Plan 02 Task 3 if possible, or extend with a parallel scenario.
  </behavior>
  <action>
    **Step 1 — Subcommand surface (`cmd/pg-admin/main.go`):**

    Add a top-level `candidates` subcommand to the existing flag-based command dispatcher. Wire 4 sub-subcommands:

    ```
    pg-admin candidates list
        Output: JSON array of current candidates (read via Registry.List())

    pg-admin candidates add --symbol BTCUSDT --long binance --short bybit --direction bidirectional [--threshold-bps 100]
        Validates the candidate (same regex/range as handlers.go), then Registry.Add(ctx, "pg-admin", c).
        Exit codes: 0 success; 2 validation failure; 3 duplicate; 4 cap exceeded; 1 other.

    pg-admin candidates delete --idx N
        Registry.Delete(ctx, "pg-admin", N). Exit codes: 0 success; 5 idx out of range; 1 other.

    pg-admin candidates replace --file <path>
        Reads JSON file, validates each entry, Registry.Replace(ctx, "pg-admin", next).
        Exit codes: 0 success; 2 validation failure; 1 other.
    ```

    For each command, the bootstrap in main():
    1. Load config: `cfg, err := config.LoadJSON(...)` (existing pattern).
    2. Construct db client: `db, err := database.New(...)` (existing pattern).
    3. Construct Registry: `registry := pricegaptrader.NewRegistry(cfg, db, logger)`.
    4. Dispatch to subcommand handler.

    **Step 2 — Reuse validation:**

    The validation helper currently lives in `internal/api/handlers.go`. For pg-admin to run the same validation, EITHER:
    (a) Extract validation into a public package `internal/pricegaptrader/validate.go` with `func ValidateCandidate(c models.PriceGapCandidate, cfg *config.Config) error` and `func ValidateCandidates([]models.PriceGapCandidate, ...) error`. Both handlers.go AND pg-admin import it.
    (b) Duplicate validation into pg-admin (inferior — drift risk).

    **Pick (a).** Extract:
    - Read `internal/api/handlers.go` lines ~1665-1700 to identify the validation logic.
    - Move regex/length/duplicate-checking into a new file `internal/pricegaptrader/validate.go`.
    - Active-position-guard stays in `handlers.go` (it requires API server's active-position store, which pg-admin doesn't link to). pg-admin DOES NOT enforce active-position guard — operator running pg-admin is assumed to know what they're doing; Registry's reload-from-disk ensures the daemon's view is consistent.
    - Update `internal/api/handlers.go` to call the extracted `pricegaptrader.ValidateCandidates(next, cfg)`.
    - pg-admin's `add` and `replace` subcommands call the same `pricegaptrader.ValidateCandidates`.

    **Step 3 — Tests (`cmd/pg-admin/main_registry_test.go`):**

    Use Go's `testscript` pattern OR direct invocation of subcommand handlers (extract `runCandidatesAdd(args []string, registry CandidateRegistry) int` etc. so tests can call directly without forking). Simpler approach:

    Refactor `main()` to a thin wrapper around `Run(args []string, deps Dependencies) int`. Tests construct deps with a fake Registry and call `Run([]string{"candidates", "add", "--symbol", "BTCUSDT", ...}, deps)` — assert exit code + Registry side-effects.

    Tests:
    - `TestPgAdmin_CandidatesAdd_Success`: fake Registry returns nil from Add; exit 0; verify the captured `(source, candidate)` arguments.
    - `TestPgAdmin_CandidatesAdd_Duplicate`: fake Registry returns `pricegaptrader.ErrDuplicateCandidate`; exit 3; stderr contains "duplicate".
    - `TestPgAdmin_CandidatesAdd_ValidationFails`: pass an invalid `--symbol "btc-usdt"`; exit 2 BEFORE Registry is called; Registry.Add not invoked.
    - `TestPgAdmin_CandidatesDelete_OutOfRange`: fake Registry returns `pricegaptrader.ErrIndexOutOfRange`; exit 5.
    - `TestPgAdmin_CandidatesReplace_FromFile`: write a JSON fixture file with 3 valid candidates; invoke `replace --file <path>`; assert Registry.Replace called with that slice.
    - `TestPgAdmin_CandidatesList`: fake Registry returns 2 candidates; assert stdout JSON contains both.
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && go test ./cmd/pg-admin/... ./internal/pricegaptrader/ -run 'TestPgAdmin|TestValidateCandidate' -count=1 -timeout=30s</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'candidates' cmd/pg-admin/main.go` returns ≥4 (subcommand dispatching present)
    - `grep -c 'pricegaptrader\.NewRegistry\|registry\.Add\|registry\.Delete\|registry\.Replace' cmd/pg-admin/main.go` returns ≥4 (all four operations route through Registry)
    - `grep -c 'pricegaptrader\.ValidateCandidate' cmd/pg-admin/main.go` returns ≥1 (shared validation called)
    - `grep -c 'pricegaptrader\.ValidateCandidate' internal/api/handlers.go` returns ≥1 (handlers.go uses the same shared validation)
    - `test -f internal/pricegaptrader/validate.go` returns 0 (extracted validation file exists)
    - `go test ./cmd/pg-admin/... -run TestPgAdmin_Candidates -count=1` exits 0 with ≥6 sub-tests passing
    - `go test ./internal/pricegaptrader/ -run TestValidateCandidate -count=1` exits 0 (validation unit tests in extracted file)
    - `go build ./cmd/pg-admin/...` exits 0 (binary compiles)
    - `gofmt -l cmd/pg-admin/main.go cmd/pg-admin/main_registry_test.go internal/pricegaptrader/validate.go` returns empty
  </acceptance_criteria>
  <done>
    pg-admin gains 4 candidate subcommands routed through Registry; validation extracted to `pricegaptrader.ValidateCandidates` and shared between handlers.go + pg-admin; tests cover all happy paths + sentinel errors; in-process bootstrap matches handlers.go pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → handlers.go validation → Registry.Replace | Authenticated dashboard operator; full-trust after Bearer token auth. |
| pg-admin CLI args → validation → Registry.X | Machine-local operator (sudo/ssh access); same trust model. |
| handlers.go ↔ Registry ↔ pg-admin | Two writers against the same config.json; Registry's reload-from-disk + atomic rename serializes them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-18 | Tampering | A new file accidentally adds `cfg.PriceGapCandidates = ...` after this plan ships | mitigate | Acceptance grep at the package level (`grep -nE 's?\.cfg\.PriceGapCandidates\s*=' internal/api/ cmd/`) returns zero. CI can wire this as a test that scans the entire repo. |
| T-11-19 | Tampering | handlers.go validation drifts from pg-admin validation (different regexes) | mitigate | Both call the SAME shared `pricegaptrader.ValidateCandidates`. Drift requires changing the shared file; tests in both packages exercise the shared code. |
| T-11-20 | Tampering | pg-admin run while bot is live silently overwrites in-memory daemon state | mitigate | Registry's reload-from-disk on every mutate ensures both processes converge on the on-disk state. Audit trail captures both with `source="dashboard-handler"` vs `source="pg-admin"`. |
| T-11-21 | Repudiation | Operator-via-pg-admin claims they didn't delete a candidate | mitigate | Registry audit row with `source="pg-admin"`, op="delete", before/after counts; persisted to pg:registry:audit Redis list capped at 200. |
| T-11-22 | DoS | Operator pg-admin replace --file with 1000 entries DOSes the bot via CPU validation cost | accept | Validation is O(n²) for dedupe but n ≤ PriceGapMaxCandidates (default 12, max 50). Pre-validation cap rejects oversized payloads in microseconds. |
| T-11-23 | Information disclosure | Validation error messages echo back operator-supplied JSON values | accept | pg-admin operator owns the input; handlers.go operator owns the dashboard payload; no third-party reads errors. |
| T-11-24 | Tampering | Registry.Replace called from handlers.go while caller's lock is held → deadlock | mitigate | Caller (handlers.go) MUST release `s.cfg.Unlock()` before calling Registry.Replace. Acceptance criterion + integration test verify no deadlock under concurrent POST+GET. |
| T-11-25 | Elevation of privilege | Adding `Get/List` to handler context exposes config to dashboard read paths that previously couldn't | mitigate | List is a defensive copy (Plan 02 Task 2); Get is bounds-checked; both read existing public state already exposed via existing dashboard responses. No new information surface. |
</threat_model>

<verification>
- All Plan 03 tests pass: `go test ./internal/api/ ./cmd/pg-admin/... ./internal/pricegaptrader/ -count=1 -timeout=60s` exits 0.
- Existing tests still pass (no regression): `go test ./... -count=1 -short` exits 0.
- Race detector: `go test -race ./internal/api/ -run TestPostConfigCandidates -count=1`.
- `make build` succeeds (frontend embed unchanged; both binaries build).
- `gofmt -l internal/api/handlers.go internal/api/server.go internal/api/pricegap_handlers.go cmd/arb/main.go cmd/pg-admin/main.go internal/pricegaptrader/validate.go` returns empty.
- Static check across full repo: `grep -rnE 's?\.cfg\.PriceGapCandidates\s*=' internal/ cmd/ | grep -v '_test\.go' | grep -v 'internal/pricegaptrader/registry.go' | grep -v 'internal/config/config.go'` returns ZERO. (Allowed: registry.go itself; config.go's load helper at ~line 1439 which is single-threaded boot-time and not a chokepoint concern.)
</verification>

<success_criteria>
- Zero direct cfg.PriceGapCandidates mutations remain in dashboard or pg-admin paths.
- One Registry instance constructed in cmd/arb/main.go, shared between API server + (Plan 04) tracker.
- pg-admin gains 4 subcommands (list/add/delete/replace) all routing through Registry.
- Validation logic shared between handlers.go and pg-admin via internal/pricegaptrader/validate.go.
- Audit trail captures `source="dashboard-handler"` and `source="pg-admin"` for every mutation.
- Plan 04 (scanner) can take a `RegistryReader` view of the same Registry instance.
</success_criteria>

<validation>
**Validation Coverage (per VALIDATION.md Dimension 8):**
- Migration unit tests: 5 sub-tests in `handlers_registry_migration_test.go` covering happy path, validation, active-position guard, registry error, no-direct-mutations static check.
- pg-admin unit tests: 6 sub-tests in `cmd/pg-admin/main_registry_test.go` covering all 4 subcommands + error sentinels.
- Validation unit tests: in `internal/pricegaptrader/validate_test.go` — extracted helper covered by tests previously in handlers.
- Hard-cut verification: acceptance grep `s?\.cfg\.PriceGapCandidates\s*=` in `internal/api/` and `cmd/` returns zero (excluding test files and registry.go itself).
- Concurrent-writer verification: relies on Plan 02 Task 3 `TestRegistry_ConcurrentMultiProcess` — that test exercises the SAME pattern handlers.go + pg-admin will use. No additional integration test needed in Plan 03 because the multi-process test in Plan 02 Task 3 already covers the production scenario (two processes calling Registry.Add against the same config file).
- Sampling rate: combined Plan 03 tests run in <30s.
- Wave 0 dependencies satisfied: this plan creates `handlers_registry_migration_test.go` and `main_registry_test.go` from the VALIDATION.md Wave 0 list (the entry "concurrent dashboard + pg-admin + scanner-attempt writes through chokepoint").
</validation>

<output>
After completion, create `.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-03-SUMMARY.md` documenting:
- Files migrated (handlers.go, pg-admin/main.go, server.go, cmd/arb/main.go)
- Shared validation helper extracted to internal/pricegaptrader/validate.go
- pg-admin subcommand surface (4 commands)
- Audit source values used ("dashboard-handler", "pg-admin")
- Static-grep verification: zero direct mutations remain in internal/api/ + cmd/
- Confirmation that Phase 12 promotion has only Registry to write through
</output>
