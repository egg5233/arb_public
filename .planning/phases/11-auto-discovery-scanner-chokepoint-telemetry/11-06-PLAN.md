---
phase: 11
plan: 06
type: execute
wave: 6
depends_on: [11-05]
files_modified:
  - web/src/components/Discovery/DiscoverySection.tsx
  - web/src/components/Discovery/DiscoveryBanner.tsx
  - web/src/components/Discovery/CycleStatsCard.tsx
  - web/src/components/Discovery/ScoreHistoryCard.tsx
  - web/src/components/Discovery/WhyRejectedCard.tsx
  - web/src/components/Discovery/PromoteTimelinePlaceholder.tsx
  - web/src/components/Discovery/__tests__/DiscoverySection.test.tsx
  - web/src/hooks/usePgDiscovery.ts
  - web/src/i18n/en.ts
  - web/src/i18n/zh-TW.ts
  - web/src/i18n/__tests__/discovery_keys.test.ts
  - web/src/pages/PriceGap.tsx
autonomous: false
requirements:
  - PG-DISC-03
  - PG-DISC-01
user_setup: []

must_haves:
  truths:
    - "Operator opening the PriceGap dashboard tab sees a Discovery sub-section between Master-toggle and Candidates table."
    - "Discovery section renders three states distinctly: Scanner ON (green pill), Scanner OFF (gray pill + disabled banner), Scanner errored (red pill + error banner)."
    - "CycleStatsCard renders 7 tiles (last_run, next_run_in, candidates_seen, accepted, rejected, errors, duration) with mono tabular numerics."
    - "ScoreHistoryCard symbol-selector dropdown lists configured universe symbols; selecting one renders Recharts <LineChart> with 7d ZSET data + threshold-band <ReferenceArea> overlay reading PriceGapAutoPromoteScore."
    - "WhyRejectedCard renders a table mapping reason → count from the latest cycle; empty state shows the i18n empty key."
    - "PromoteTimelinePlaceholder renders a dashed-border card with the Phase 12 placeholder copy."
    - "REST seed via GET /api/pg/discovery/state on mount; WS subscriptions on pg_scan_cycle, pg_scan_metrics, pg_scan_score apply incremental updates."
    - "All new i18n keys exist in BOTH en.ts AND zh-TW.ts (lockstep) — TypeScript build fails without zh-TW entries via TranslationKey derivation."
    - "No new npm dependencies — verified by zero changes to package-lock.json; only Recharts subcomponents (already locked) imported."
    - "Live BBO join checkpoint validates Phase Success Criterion #5 under real exchange feeds — the runtime gate Plan 04's mocked Test 12 cannot prove."
  artifacts:
    - path: "web/src/components/Discovery/DiscoverySection.tsx"
      provides: "Container component handling enabled/errored/healthy states + REST seed + WS subscriptions"
      contains: "DiscoverySection"
    - path: "web/src/components/Discovery/CycleStatsCard.tsx"
      provides: "7-tile responsive stat grid with mono tabular numerics"
      contains: "CycleStatsCard"
    - path: "web/src/components/Discovery/ScoreHistoryCard.tsx"
      provides: "Symbol-selector dropdown + Recharts LineChart + threshold-band ReferenceArea overlay"
      contains: "ScoreHistoryCard"
    - path: "web/src/components/Discovery/WhyRejectedCard.tsx"
      provides: "Reason→count breakdown table with i18n reason labels"
      contains: "WhyRejectedCard"
    - path: "web/src/components/Discovery/PromoteTimelinePlaceholder.tsx"
      provides: "Dashed-border placeholder card for Phase 12"
      contains: "PromoteTimelinePlaceholder"
    - path: "web/src/hooks/usePgDiscovery.ts"
      provides: "REST seed + 3 WS channel subscriptions hook"
      contains: "usePgDiscovery"
    - path: "web/src/i18n/en.ts"
      provides: "All pricegap.discovery.* keys per UI-SPEC.md Copywriting Contract section"
      contains: "pricegap.discovery.title"
    - path: "web/src/i18n/zh-TW.ts"
      provides: "All pricegap.discovery.* keys mirroring en.ts (lockstep mandatory)"
      contains: "pricegap.discovery.title"
    - path: "web/src/pages/PriceGap.tsx"
      provides: "Insertion of <DiscoverySection /> between Master-toggle row and Candidates table"
      contains: "<DiscoverySection"
  key_links:
    - from: "web/src/components/Discovery/DiscoverySection.tsx"
      to: "web/src/hooks/usePgDiscovery.ts"
      via: "Hook call: const { state, scores, loadScoresFor, errored, enabled } = usePgDiscovery()"
      pattern: "usePgDiscovery"
    - from: "web/src/hooks/usePgDiscovery.ts"
      to: "GET /api/pg/discovery/state and /api/pg/discovery/scores/{symbol} (Plan 05 Task 2)"
      via: "fetch on mount + on symbol-selector change"
      pattern: "/api/pg/discovery/"
    - from: "web/src/hooks/usePgDiscovery.ts"
      to: "WS hub (existing usePriceGapWebSocket pattern)"
      via: "Subscribe to pg_scan_cycle, pg_scan_metrics, pg_scan_score event types"
      pattern: "pg_scan_cycle|pg_scan_metrics|pg_scan_score"
    - from: "web/src/i18n/en.ts AND zh-TW.ts"
      to: "TranslationKey type derivation"
      via: "All pricegap.discovery.* keys must appear in BOTH files (lockstep)"
      pattern: "pricegap\\.discovery\\."
    - from: "web/src/pages/PriceGap.tsx"
      to: "web/src/components/Discovery/DiscoverySection.tsx"
      via: "JSX import + render between Master-toggle and Candidates sections"
      pattern: "<DiscoverySection"
---

<objective>
Build the Discovery sub-section UI inside the existing PriceGap dashboard tab per UI-SPEC.md (PG-DISC-03 frontend half). React + Vite + Tailwind v4 + Recharts only — zero new npm dependencies (npm lockdown active). Three scanner visual states (ON/OFF/ERRORED) distinct via existing StatusBadge color palette. EN + zh-TW i18n in lockstep. Closes the phase with TWO checkpoints: visual UI verification + live 6-exchange BBO join verification.

Purpose: Implement decisions D-14 (sub-section inside existing PriceGap tab; Phase 16 migrates to top-level), D-15 (4 cards: cycle stats, score history, why-rejected, Phase 12 placeholder), D-16 (always visible with "Scanner OFF" banner when disabled), D-17 (REST seed + WS throttled push). Closes Phase Success Criterion #5 via the live BBO checkpoint.
Output: 6 new React components + 1 hook + i18n lockstep + integration into PriceGap.tsx + vitest tests + visual checkpoint + live BBO join runtime checkpoint.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-CONTEXT.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-RESEARCH.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-VALIDATION.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-UI-SPEC.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-04-PLAN.md
@.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-05-PLAN.md

<interfaces>
From Plan 05 (Wave 5):
- GET /api/pg/discovery/state response: `{ok, data: {enabled, last_run_at, next_run_in, candidates_seen, accepted, rejected, errors, duration_ms, why_rejected: {reason: count}, score_snapshot: [{symbol, score, sub_scores, gates_passed, gates_failed}]}}`
- GET /api/pg/discovery/scores/{symbol} response: `{ok, data: {symbol, points: [{ts, score, sub_scores}], threshold_band: {auto_promote: number}}}`
- WS event types: pg_scan_cycle (full summary), pg_scan_metrics (5s throttled), pg_scan_score (debounced per symbol)
- cmd/arb/main.go bootstrap wires Scanner+Telemetry into Tracker — runtime live-system path is now closed at Plan 05 boundary

From existing codebase (verified during planning):
- web/src/pages/PriceGap.tsx — host file at 63.3K (existing); Master-toggle row + Candidates table sections present
- web/src/components/PnLChart.tsx — Recharts <LineChart> + <ResponsiveContainer> + <CartesianGrid> + <Tooltip> pattern (3.3K)
- web/src/components/StatusBadge.tsx — three-state color pattern (active=green/closed=gray/error=red); colorMapNew at lines 9-19
- web/src/i18n/en.ts and zh-TW.ts — TranslationKey derivation; 'nav.pricegap' at line 13
- web/src/index.css — Binance dark theme tokens (data-theme="new"); accent #f0b90b; positive #0ecb81; destructive #f6465d
- existing usePriceGapWebSocket pattern at PriceGap.tsx:141-199 — newline-separated JSON parsing + msg.type switch
- panelCls in PriceGap.tsx — bg-gray-900 border border-gray-800 rounded-lg p-4 (existing card pattern; reused unchanged)
- replaceTokens helper at PriceGap.tsx:131-137 — {name} token substitution for i18n string interpolation
- formatAge / formatDuration helpers in PriceGap.tsx — reuse for stat tile formatting
- npm lockdown: zero new dependencies; only Recharts subcomponents (already imported via PnLChart.tsx) allowed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add i18n keys (en + zh-TW lockstep) and lockstep verification test</name>
  <files>web/src/i18n/en.ts, web/src/i18n/zh-TW.ts, web/src/i18n/__tests__/discovery_keys.test.ts</files>
  <read_first>
    - web/src/i18n/en.ts (full file — existing pricegap.* namespace; TranslationKey derivation at top; existing translation patterns)
    - web/src/i18n/zh-TW.ts (full file — must mirror en.ts key set exactly)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-UI-SPEC.md (Copywriting Contract section — lists EVERY required key with EN + zh-TW values)
  </read_first>
  <behavior>
    - Test 1 (key set lockstep): TestDiscoveryI18nLockstep reads both en.ts and zh-TW.ts, extracts the set of keys matching `pricegap.discovery.*`, asserts the two sets are identical (no key in EN missing from zh-TW; no key in zh-TW missing from EN).
    - Test 2 (no empty values): Every pricegap.discovery.* key in BOTH files has a non-empty string value (catches stub entries).
    - Test 3 (token consistency): Keys whose EN value contains `{n}` or `{exchanges}` placeholders also have those same placeholders in their zh-TW value (catches translation drift on token interpolation).
    - Test 4 (TypeScript compile): `cd web && npx tsc --noEmit` exits 0 — TranslationKey derivation succeeds with the new keys (proves lockstep at compile time, complements the runtime test).
  </behavior>
  <action>
    Add the following keys to BOTH `web/src/i18n/en.ts` AND `web/src/i18n/zh-TW.ts`. Copy values verbatim from UI-SPEC.md "Copywriting Contract" section (do NOT paraphrase — UI-SPEC was reviewed and approved with these exact strings).

    Section header keys:
      pricegap.discovery.title             EN: "Discovery Scanner"        zh-TW: "自動發現掃描器"
      pricegap.discovery.subtitle          EN: "Read-only scanner ranks Strategy 4 candidates by score across the configured universe."   zh-TW: "唯讀掃描器依設定的標的池對 Strategy 4 候選排序。"
      pricegap.discovery.scannerOn         EN: "Scanner ON"               zh-TW: "掃描中"
      pricegap.discovery.scannerOff        EN: "Scanner OFF"              zh-TW: "掃描器已停用"
      pricegap.discovery.scannerErrored    EN: "Scanner errored"          zh-TW: "掃描器發生錯誤"

    Disabled-state banner keys:
      pricegap.discovery.banner.disabled.heading    EN: "Discovery is disabled"   zh-TW: "自動發現已停用"
      pricegap.discovery.banner.disabled.body       EN: "Set `PriceGapDiscoveryEnabled = true` in `config.json` to start the scanner. The section will populate on the next cycle."   zh-TW: "在 `config.json` 中將 `PriceGapDiscoveryEnabled` 設為 `true` 即可啟動掃描器。下一個週期將自動填入資料。"

    Error-state banner keys:
      pricegap.discovery.banner.error.heading       EN: "Scanner reported errors in the last cycle"   zh-TW: "上一週期掃描器回報錯誤"
      pricegap.discovery.banner.error.body          EN: "{n} ticker reads failed across {exchanges}. Cycle output may be incomplete."   zh-TW: "{n} 個行情讀取於 {exchanges} 失敗。本次週期資料可能不完整。"

    Cycle stats card keys:
      pricegap.discovery.cycle.title           EN: "Cycle Stats"          zh-TW: "週期統計"
      pricegap.discovery.cycle.lastRun         EN: "Last run"             zh-TW: "上次執行"
      pricegap.discovery.cycle.nextRunIn       EN: "Next run in"          zh-TW: "下次執行於"
      pricegap.discovery.cycle.candidatesSeen  EN: "Candidates seen"      zh-TW: "已掃描"
      pricegap.discovery.cycle.accepted        EN: "Accepted"             zh-TW: "通過"
      pricegap.discovery.cycle.rejected        EN: "Rejected"             zh-TW: "拒絕"
      pricegap.discovery.cycle.errors          EN: "Errors"               zh-TW: "錯誤"
      pricegap.discovery.cycle.duration        EN: "Duration"             zh-TW: "耗時"
      pricegap.discovery.cycle.never           EN: "Never"                zh-TW: "從未"

    Score history card keys:
      pricegap.discovery.scoreHistory.title             EN: "Score History (7d)"   zh-TW: "分數歷史（7天）"
      pricegap.discovery.scoreHistory.symbolSelector    EN: "Symbol"               zh-TW: "標的"
      pricegap.discovery.scoreHistory.thresholdBand     EN: "Auto-promote threshold"   zh-TW: "自動升級門檻"
      pricegap.discovery.scoreHistory.empty             EN: "No score history yet — scanner has not produced cycles for this symbol."   zh-TW: "尚無分數歷史 — 掃描器尚未對此標的產生週期資料。"
      pricegap.discovery.scoreHistory.noUniverse        EN: "Configure `PriceGapDiscoveryUniverse` in `config.json` to populate the symbol list."   zh-TW: "請於 `config.json` 設定 `PriceGapDiscoveryUniverse` 以填入標的清單。"
      pricegap.discovery.scoreHistory.selectPrompt      EN: "Select a symbol"      zh-TW: "選擇標的"

    Why-rejected breakdown keys:
      pricegap.discovery.whyRejected.title              EN: "Why Rejected (last cycle)"   zh-TW: "拒絕原因（上一週期）"
      pricegap.discovery.whyRejected.empty              EN: "No rejections in the last cycle."   zh-TW: "上一週期沒有拒絕記錄。"
      pricegap.discovery.whyRejected.reason.insufficient_persistence    EN: "Persistence < 4 bars"   zh-TW: "持續性不足 4 根 K 棒"
      pricegap.discovery.whyRejected.reason.stale_bbo                   EN: "BBO stale (>90s)"   zh-TW: "BBO 過期（超過 90 秒）"
      pricegap.discovery.whyRejected.reason.insufficient_depth          EN: "Depth below floor"   zh-TW: "深度低於門檻"
      pricegap.discovery.whyRejected.reason.denylist                    EN: "Denylist match"   zh-TW: "黑名單比對"
      pricegap.discovery.whyRejected.reason.bybit_blackout              EN: "Bybit blackout window"   zh-TW: "Bybit 維護時段"
      pricegap.discovery.whyRejected.reason.symbol_not_listed_long      EN: "Not listed (long leg)"   zh-TW: "多單方向未上架"
      pricegap.discovery.whyRejected.reason.symbol_not_listed_short     EN: "Not listed (short leg)"   zh-TW: "空單方向未上架"
      pricegap.discovery.whyRejected.reason.sample_error                EN: "Sample error"   zh-TW: "取樣錯誤"

    Phase 12 timeline placeholder keys:
      pricegap.discovery.timeline.title          EN: "Promote / Demote Timeline"   zh-TW: "升級／降級時間線"
      pricegap.discovery.timeline.placeholder    EN: "Promote and demote events will appear here once Phase 12 (auto-promotion) ships."   zh-TW: "升級與降級事件將於 Phase 12（自動升級）上線後顯示於此。"

    Insert these keys into both files following the existing namespace structure (likely a flat key-value object with dotted keys, or a nested object — match what en.ts already uses; mirror exactly in zh-TW.ts).

    Create `web/src/i18n/__tests__/discovery_keys.test.ts`:
      - Import en + zh-TW translation objects.
      - Test 1: Extract all keys matching /^pricegap\.discovery\./ from both; assert the two key sets are equal (use Set + symmetric difference).
      - Test 2: For each pricegap.discovery.* key, assert the value is a non-empty string in both files.
      - Test 3: For each EN value containing `{n}` or `{exchanges}`, assert the corresponding zh-TW value contains the same tokens.
      - Test 4: Assert the count of pricegap.discovery.* keys matches expectation (≥30 keys based on UI-SPEC enumeration); guards against accidental key deletion.

    DO NOT modify any other namespace. DO NOT change existing pricegap.* keys. ONLY add the new pricegap.discovery.* keys.
  </action>
  <verify>
    <automated>cd /var/solana/data/arb/web &amp;&amp; npx vitest run --testNamePattern 'TestDiscoveryI18nLockstep|discovery_keys' &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pricegap\.discovery\.' web/src/i18n/en.ts` returns ≥30
    - `grep -c 'pricegap\.discovery\.' web/src/i18n/zh-TW.ts` returns ≥30
    - Key counts match: `[ "$(grep -c 'pricegap\.discovery\.' web/src/i18n/en.ts)" = "$(grep -c 'pricegap\.discovery\.' web/src/i18n/zh-TW.ts)" ]` exits 0
    - `test -f web/src/i18n/__tests__/discovery_keys.test.ts` returns 0
    - `cd web && npx vitest run --testNamePattern discovery_keys` exits 0 with all 4 sub-tests passing
    - `cd web && npx tsc --noEmit` exits 0 (TranslationKey type derivation succeeds; proves no orphan key references)
    - `git diff --stat web/package-lock.json` shows zero changes (npm lockdown enforced — no new dependencies)
  </acceptance_criteria>
  <done>
    All 30+ pricegap.discovery.* i18n keys added to BOTH en.ts and zh-TW.ts in lockstep. Lockstep test (4 sub-tests) verifies key sets match, values non-empty, token interpolation consistent. TypeScript compiles cleanly. Zero npm dependency changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build usePgDiscovery hook + 6 Discovery components + integrate into PriceGap.tsx</name>
  <files>web/src/hooks/usePgDiscovery.ts, web/src/components/Discovery/DiscoverySection.tsx, web/src/components/Discovery/DiscoveryBanner.tsx, web/src/components/Discovery/CycleStatsCard.tsx, web/src/components/Discovery/ScoreHistoryCard.tsx, web/src/components/Discovery/WhyRejectedCard.tsx, web/src/components/Discovery/PromoteTimelinePlaceholder.tsx, web/src/components/Discovery/__tests__/DiscoverySection.test.tsx, web/src/pages/PriceGap.tsx</files>
  <read_first>
    - web/src/pages/PriceGap.tsx (full file 63.3K — existing tab structure; locate Master-toggle row + Candidates table sections; identify usePriceGapWebSocket hook pattern at lines 141-199; replaceTokens helper at 131-137; panelCls + sectionTitle constants; formatAge / formatDuration helpers)
    - web/src/components/PnLChart.tsx (Recharts pattern — exact composition of <ResponsiveContainer><LineChart><CartesianGrid><XAxis><YAxis><Tooltip><Line/>; tooltip styling; ResponsiveContainer height)
    - web/src/components/StatusBadge.tsx (three-state color map at lines 9-19 — active=green/closed=gray/error=red; mirror for Scanner ON/OFF/ERRORED pill)
    - web/src/index.css (theme tokens — accent #f0b90b for threshold band; positive #0ecb81; destructive #f6465d)
    - web/src/i18n/en.ts (Task 1 — all keys to use)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-UI-SPEC.md (full file — Layout & Component Inventory section for hierarchy; Interaction & Data Contract for endpoint shapes; Three scanner visual states table; Responsive breakpoints)
    - .planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-05-PLAN.md (Task 2 — REST endpoint response shapes)
  </read_first>
  <behavior>
    - Test 1 (DiscoverySection renders 4 cards): Mount with mocked hook returning healthy state — assert presence of CycleStatsCard, ScoreHistoryCard, WhyRejectedCard, PromoteTimelinePlaceholder by data-testid.
    - Test 2 (Scanner OFF state): Hook returns enabled=false — assert "Scanner OFF" text present, gray pill in header, disabled banner visible, cycle stats render with "—" / "Never" placeholders.
    - Test 3 (Scanner errored state): Hook returns enabled=true with errors=2 — assert "Scanner errored" text, red pill, error banner with token-replaced "{n} ticker reads failed across {exchanges}" interpolation.
    - Test 4 (CycleStatsCard 7 tiles): Renders all 7 stat tiles with mono tabular numeric values; "Last run" shows formatted timestamp or "Never" via i18n key.
    - Test 5 (ScoreHistoryCard symbol-selector): Renders <select> populated with universe symbols; selecting a symbol triggers loadScoresFor + Recharts <LineChart> renders with 7d points + <ReferenceArea> threshold-band overlay.
    - Test 6 (ScoreHistoryCard empty state): Symbol selected but no points returned — empty state copy renders.
    - Test 7 (WhyRejectedCard): Reason→count map renders as table rows with i18n reason labels; empty map renders i18n empty key.
    - Test 8 (PromoteTimelinePlaceholder): Renders with dashed border + placeholder copy.
    - Test 9 (REST seed): Hook calls fetch on mount with /api/pg/discovery/state; populates state on resolve.
    - Test 10 (WS subscription): Hook subscribes to pg_scan_cycle / pg_scan_metrics / pg_scan_score events; incoming pg_scan_cycle event refreshes CycleStatsCard + WhyRejectedCard atomically.
    - Test 11 (PriceGap.tsx integration): Existing PriceGap page mounts <DiscoverySection /> between Master-toggle and Candidates sections without breaking existing tests.
    - Test 12 (no new npm deps): web/package-lock.json byte-identical to pre-Plan-06 state.
  </behavior>
  <action>
    Create `web/src/hooks/usePgDiscovery.ts`:

    Hook signature: `usePgDiscovery() : { state: DiscoveryState | null; scores: Record<string, ScoresResponse>; loadScoresFor: (symbol: string) => void; errored: boolean; enabled: boolean; lastRunAt: number | null; }`

    Behavior:
      - On mount: fetch GET /api/pg/discovery/state; set initial state.
      - WS subscription via existing hub pattern (mirror usePriceGapWebSocket at PriceGap.tsx:141-199 — newline-separated JSON, switch on msg.type):
        - msg.type === "pg_scan_cycle" → setState(merging summary fields, replacing why_rejected map, replacing score_snapshot)
        - msg.type === "pg_scan_metrics" → setState(merging metrics fields only — last_run_at, next_run_in, candidates_seen, accepted, rejected, errors, duration_ms)
        - msg.type === "pg_scan_score" → setScores(prev => updating points array for msg.symbol, append new {ts, score, sub_scores})
      - loadScoresFor(symbol): fetch GET /api/pg/discovery/scores/{symbol}; set scores[symbol].
      - errored: derived as `state?.errors > 0 || state?.cycle_failed === true`.
      - enabled: derived as `state?.enabled === true`.
      - lastRunAt: derived as `state?.last_run_at ?? null`.

    Create the 6 components per UI-SPEC.md "Component file layout":

    `DiscoverySection.tsx` (container):
      - Imports usePgDiscovery + 5 sub-components.
      - Renders section header: <h3> with i18n title + status pill (Scanner ON green / OFF gray / ERRORED red) on right + last-run timestamp.
      - Renders <DiscoveryBanner /> conditionally (disabled/errored/healthy variants).
      - Renders <CycleStatsCard state={state} /> full-width.
      - 2-column row (60% / 40% on lg+, stacked on smaller): <ScoreHistoryCard /> left, <WhyRejectedCard /> right.
      - <PromoteTimelinePlaceholder /> full-width below.
      - Outer rhythm: `space-y-6` between cards inside section; section itself uses `space-y-8` to align with PriceGap.tsx page rhythm.
      - Cards reuse panelCls (`bg-gray-900 border border-gray-800 rounded-lg p-4`).

    `DiscoveryBanner.tsx`:
      - Props: `{ variant: 'disabled' | 'errored' | null, errorContext?: { n: number, exchanges: string[] } }`.
      - variant='disabled': bg-gray-800 text-gray-400 border-gray-700; renders `pricegap.discovery.banner.disabled.heading` + `.body`. role="status".
      - variant='errored': bg-[#f6465d]/15 text-[#f6465d] border-[#f6465d]/40; renders heading + body with replaceTokens for `{n}` and `{exchanges}`. role="alert".
      - variant=null: returns null (renders nothing — healthy state has no banner).

    `CycleStatsCard.tsx`:
      - Props: `{ state: DiscoveryState | null }`.
      - Renders 7-tile grid: Last run, Next run in, Candidates seen, Accepted, Rejected, Errors, Duration.
      - Each tile: <div className="p-3"><div className="text-xs text-gray-400">{label}</div><div className="text-lg font-bold font-mono tabular-nums">{value}</div></div>.
      - Last run: formatTimestamp(last_run_at) OR i18n `pricegap.discovery.cycle.never` if null/0.
      - Next run in: formatDuration(next_run_in seconds) — reuse PriceGap.tsx formatDuration if available.
      - Numeric tiles (candidatesSeen/accepted/rejected/errors/duration): "—" if state is null; otherwise the integer value.
      - Responsive: `grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-7 gap-4`.

    `ScoreHistoryCard.tsx`:
      - Props: `{ state: DiscoveryState | null, scores: Record<string, ScoresResponse>, onSelectSymbol: (s: string) => void }`.
      - Renders <select> populated from `state?.score_snapshot.map(r => r.symbol)` deduped (or from a separate universe field if added). If empty, render i18n `pricegap.discovery.scoreHistory.noUniverse` empty state.
      - On select change: call onSelectSymbol(value).
      - Below selector: if scores[selected] exists, render <ResponsiveContainer height={300}><LineChart data={points}><CartesianGrid/><XAxis dataKey="ts" tickFormatter={formatTime}/><YAxis domain={[0,100]}/><Tooltip/><ReferenceArea y1={threshold_band.auto_promote} y2={100} fill="#f0b90b" fillOpacity={0.10} /><Line type="monotone" dataKey="score" stroke="#0ecb81"/></LineChart></ResponsiveContainer>.
      - Loading state: spinner with text-xs text-gray-500 "Loading…".
      - Empty state (selected symbol but zero points): i18n `pricegap.discovery.scoreHistory.empty`.

    `WhyRejectedCard.tsx`:
      - Props: `{ whyRejected: Record<string, number> }`.
      - Empty whyRejected → render i18n `pricegap.discovery.whyRejected.empty`.
      - Otherwise: render a table with columns {Reason, Count}; rows iterate over whyRejected entries sorted by count desc.
      - Reason label: lookup `pricegap.discovery.whyRejected.reason.{reason}` i18n key; if missing, fall back to the raw reason string.
      - Count: <span className="font-mono tabular-nums">.

    `PromoteTimelinePlaceholder.tsx`:
      - Renders panelCls with `border-dashed` override + `p-8` interior padding.
      - Centered: <h4>{i18n title}</h4> + <p className="text-gray-500">{i18n placeholder}</p>.

    Component file `__tests__/DiscoverySection.test.tsx`:
      - Use vitest + @testing-library/react (already in package-lock.json — verify; if not, fall back to react-test-renderer which IS bundled).
      - Mock usePgDiscovery hook in each test (vi.mock).
      - Cover all 12 behaviors above with separate it() blocks.

    PriceGap.tsx integration:
      - Import: `import { DiscoverySection } from '@/components/Discovery/DiscoverySection';`
      - Locate the existing JSX between the Master-toggle row and the Candidates table (read the file to find the exact insertion point — look for `// Master toggle` comment or similar; the Candidates table likely begins with `{candidates.map(...)}`).
      - Insert `<DiscoverySection />` between these two sections at the same JSX nesting level.
      - DO NOT modify any existing PriceGap.tsx logic; pure additive insertion.

    DO NOT add any npm packages. Verify by running `git diff web/package.json web/package-lock.json` after implementation — output MUST be empty.
  </action>
  <verify>
    <automated>cd /var/solana/data/arb/web &amp;&amp; npx vitest run --testNamePattern 'DiscoverySection|usePgDiscovery|CycleStatsCard|ScoreHistoryCard|WhyRejectedCard' &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -d web/src/components/Discovery` returns 0
    - 6 component files exist: `ls web/src/components/Discovery/*.tsx | wc -l` returns ≥6 (5 cards + container; tests dir excluded)
    - `test -f web/src/hooks/usePgDiscovery.ts` returns 0
    - `test -f web/src/components/Discovery/__tests__/DiscoverySection.test.tsx` returns 0
    - `grep -c '<DiscoverySection' web/src/pages/PriceGap.tsx` returns ≥1
    - `grep -c 'usePgDiscovery' web/src/hooks/usePgDiscovery.ts` returns ≥1
    - `grep -c '/api/pg/discovery/state\|/api/pg/discovery/scores' web/src/hooks/usePgDiscovery.ts` returns ≥2
    - `grep -c 'pg_scan_cycle\|pg_scan_metrics\|pg_scan_score' web/src/hooks/usePgDiscovery.ts` returns ≥3
    - `grep -c 'ResponsiveContainer\|LineChart\|ReferenceArea' web/src/components/Discovery/ScoreHistoryCard.tsx` returns ≥3
    - `grep -c 'tabular-nums\|font-mono' web/src/components/Discovery/CycleStatsCard.tsx` returns ≥1
    - `grep -c 'border-dashed' web/src/components/Discovery/PromoteTimelinePlaceholder.tsx` returns ≥1
    - `cd web && npx vitest run --testNamePattern 'DiscoverySection|usePgDiscovery'` exits 0 with all 12 sub-tests passing
    - `cd web && npx tsc --noEmit` exits 0 (TypeScript compiles)
    - `cd web && npm run build` exits 0 (Vite production build succeeds)
    - `git diff --exit-code web/package.json web/package-lock.json` exits 0 (zero npm dependency changes)
    - `gofmt -l ...` does not apply; `cd web && npx prettier --check 'src/components/Discovery/**/*.tsx' 'src/hooks/usePgDiscovery.ts' 'src/i18n/__tests__/discovery_keys.test.ts'` exits 0 (if prettier configured) OR ESLint clean
  </acceptance_criteria>
  <done>
    6 React components + 1 hook + DiscoverySection integration into PriceGap.tsx complete. All 12 vitest behaviors verified. Three scanner states (ON/OFF/ERRORED) render distinctly per UI-SPEC.md. Zero new npm dependencies. TypeScript + Vite build green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Discovery section in browser</name>
  <files>web/src/components/Discovery/DiscoverySection.tsx, web/src/pages/PriceGap.tsx (no edits — visual verification only)</files>
  <action>Run the verification steps in <how-to-verify> below. This is a checkpoint task — no code changes are made; the operator validates visual/functional correctness of the Discovery section across the three scanner states (OFF / ON / ERRORED) and locale switching (EN / zh-TW). All verification commands and expected outcomes are listed below.</action>
  <verify>
    <automated>echo "Manual verification — see how-to-verify steps below; operator runs verification and replies with resume-signal"</automated>
  </verify>
  <done>Operator has confirmed (via resume-signal) that all three scanner states render distinctly, locale switching works, and zero npm dependency changes occurred. Issues, if any, become Plan 07 fixes.</done>
  <what-built>
    Discovery sub-section inside the existing PriceGap dashboard tab with:
    - Section header showing "Discovery Scanner" title + Scanner ON/OFF/ERRORED pill + last-run timestamp.
    - Conditional banner (disabled-state + error-state variants).
    - CycleStatsCard with 7 stat tiles in a horizontal grid.
    - ScoreHistoryCard with symbol-selector dropdown + Recharts <LineChart> + threshold band overlay.
    - WhyRejectedCard with reason→count breakdown table.
    - PromoteTimelinePlaceholder with dashed-border placeholder card.
    - All copy in EN + zh-TW (locale toggle works).
    - Zero new npm dependencies.

    Backend endpoints (Plan 05 Task 2) seed initial state; WS push (Plan 05 Task 1) drives incremental updates.
  </what-built>
  <how-to-verify>
    1. Build: `cd /var/solana/data/arb/web && npm run build && cd .. && go build ./cmd/arb`.
    2. Verify default-OFF state:
       a. Confirm `PriceGapDiscoveryEnabled=false` in config.json (default; do not modify config.json).
       b. Start the bot: `./arb` (or via systemd: `sudo systemctl restart arb`).
       c. Open the dashboard at https://localhost:8443 (or configured host).
       d. Log in with the existing Bearer token.
       e. Navigate to the "Price-Gap" tab (existing).
       f. Scroll between the Master-toggle row and the Candidates table.
       g. EXPECT: Discovery sub-section visible with:
          - Title "Discovery Scanner" (EN) or "自動發現掃描器" (zh-TW; toggle locale).
          - Gray "Scanner OFF" pill on right.
          - Disabled banner visible (gray bg) with copy mentioning `PriceGapDiscoveryEnabled`.
          - 7 stat tiles rendered with "—" or "Never" values.
          - Symbol-selector dropdown empty (no universe configured).
          - Why-rejected card showing the empty state copy.
          - Phase 12 placeholder card with dashed border.
    3. Verify ON state (with synthetic data):
       a. Stop the bot.
       b. Pre-populate Redis DB 2 with a sample cycle: `redis-cli -n 2 RPUSH pg:scan:cycles '{"started_at":1714291200,"completed_at":1714291210,"duration_ms":10000,"candidates_seen":12,"accepted":3,"rejected":9,"errors":0,"why_rejected":{"insufficient_persistence":5,"stale_bbo":2,"insufficient_depth":2},"records":[{"symbol":"BTCUSDT","long_exch":"binance","short_exch":"bybit","score":78,"spread_bps":150,"persistence_bars":4,"depth_score":0.85,"freshness_age_s":15,"funding_bps_h":0.05,"gates_passed":["cross_pair","freshness","depth","persistence"],"gates_failed":[],"why_rejected":"","ts":1714291210}]}'`
       c. Set the metrics hash: `redis-cli -n 2 HSET pg:scan:metrics last_run_at 1714291210 candidates_seen 12 accepted 3 rejected 9 errors 0 duration_ms 10000 next_run_in 250 cycle_failed 0`
       d. Set enabled flag: `redis-cli -n 2 SET pg:scan:enabled 1`
       e. Restart the bot.
       f. EXPECT in the dashboard:
          - Green "Scanner ON" pill.
          - No banner (healthy state).
          - 7 stat tiles populated: Last run shows formatted time, Candidates seen=12, Accepted=3, Rejected=9, Errors=0, Duration=10s, Next run in=4m 10s.
          - Symbol-selector populated with BTCUSDT (or empty if no score history loaded yet).
          - Why-rejected breakdown table shows 3 rows: insufficient_persistence (5), stale_bbo (2), insufficient_depth (2).
          - Phase 12 placeholder still visible with dashed border.
    4. Verify ERRORED state:
       a. `redis-cli -n 2 HSET pg:scan:metrics errors 5 cycle_failed 1`
       b. EXPECT: Red "Scanner errored" pill + red error banner with token-replaced "5 ticker reads failed" copy.
    5. Verify zh-TW locale: Toggle locale; confirm all Discovery section copy switches to Traditional Chinese matching UI-SPEC.md zh-TW values.
    6. Reset Redis state: `redis-cli -n 2 DEL pg:scan:cycles pg:scan:metrics pg:scan:enabled`.

    REMINDER: Do NOT modify config.json during this verification (npm/config lockdown rules).
  </how-to-verify>
  <resume-signal>Reply "approved" if visual quality, three-state rendering, and locale switching all pass. Reply with specific issues otherwise (e.g., "score history chart x-axis truncates timestamp", "scanner-off pill is wrong color").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live BBO join verification — 6-exchange canonical symbol cross-pair under real feeds (Phase Success Criterion #5)</name>
  <files>(no files modified — runtime verification only)</files>
  <action>Run the verification steps in <how-to-verify> below. This is the live-runtime acceptance gate for Phase Success Criterion #5: cross-exchange BBO join works under real exchange feeds (not just fake fixtures). Plan 04 Test 12 covers the join logic with mocked adapters; this checkpoint confirms the SAME logic works against the SIX live adapters with their distinct symbol-format quirks (Gate.io BTC_USDT, OKX BTC-USDT-SWAP, etc.).</action>
  <verify>
    <automated>echo "Live BBO join verification — see how-to-verify steps below; operator runs verification against the running binary and replies with resume-signal"</automated>
  </verify>
  <done>Operator has confirmed (via resume-signal) that BTCUSDT cross-exchange BBO join produced records from all 6 supported exchanges in a live scan cycle, and that pg:scan:scores:BTCUSDT contains a non-zero score progression. /gsd-verify-work picks up this checkpoint as the runtime gate for PG-DISC-01 Success Criterion #5.</done>
  <what-built>
    End-to-end Phase 11 wiring under live exchange feeds:
    - Plan 04 scanner core + Plan 05 telemetry + Plan 05 main.go bootstrap → scanLoop fires under real config.
    - Plan 02 chokepoint + Plan 03 hard-cut migration → registry mutations route through *Registry.
    - Plan 06 UI renders the live state.

    This checkpoint exercises the HARDEST integration property: cross-exchange BBO join across all 6 adapters with their distinct symbol formats (BTCUSDT canonical → BTC_USDT, BTC-USDT-SWAP, etc. inside each adapter). Mock fixtures in Plan 04 Test 12 cannot prove this property because they don't exercise the per-adapter symbol-translation paths.
  </what-built>
  <how-to-verify>
    1. Pre-flight: ensure all Plan 04/05/06 unit + integration tests are green:
       `cd /var/solana/data/arb && go test ./internal/pricegaptrader/... ./internal/api/... -count=1 -timeout=120s`
       `cd web && npx vitest run --testNamePattern 'DiscoverySection|usePgDiscovery|discovery_keys'`
    2. Build: `make build` (frontend → Go binary).
    3. Tell the user (operator) to:
       a. Set `PriceGapDiscoveryEnabled=true` in `config.json` (operator-only — Claude must NOT modify config.json per CLAUDE.local.md).
       b. Set `PriceGapDiscoveryUniverse=["BTCUSDT"]` in `config.json` for a focused test.
       c. Set `PriceGapDiscoveryIntervalSec=60` for faster feedback (revert to 300 after verification).
       d. Restart the bot: `sudo systemctl restart arb` (or run `./arb` manually).
       e. Wait ≥75 seconds for firstTick offset (13s) + first cycle interval (60s).
    4. Verify scanLoop fired under live config:
       `journalctl -u arb -n 200 | grep -E "phase-11|scanLoop|RunCycle"`
       EXPECT log lines confirming the goroutine launched and at least one cycle completed.
    5. Verify Redis pg:scan:* keys populated:
       `redis-cli -n 2 GET pg:scan:enabled` → expect "1"
       `redis-cli -n 2 HGETALL pg:scan:metrics` → expect last_run_at within ~75s, candidates_seen ≥ 1
       `redis-cli -n 2 LRANGE pg:scan:cycles -1 -1` → expect a JSON cycle summary
       `redis-cli -n 2 ZRANGE pg:scan:scores:BTCUSDT 0 -1 WITHSCORES` → expect ≥1 score entry
    6. **Critical join check** — verify the cycle covered all 6 exchanges:
       `redis-cli -n 2 LRANGE pg:scan:cycles -1 -1 | jq '.records[] | select(.symbol=="BTCUSDT") | {long: .long_exch, short: .short_exch, score, why_rejected}'`
       EXPECT records covering all 30 ordered cross-pairs of {binance, bybit, gate, bitget, okx, bingx} (6×5=30); NO record should have `why_rejected="symbol_not_listed_long"` or `"symbol_not_listed_short"` for BTCUSDT (BTCUSDT is listed on all 6).
    7. Verify dashboard renders the live state:
       a. Open the dashboard, navigate to Price-Gap tab.
       b. Discovery sub-section shows green "Scanner ON" pill.
       c. CycleStatsCard shows real timestamps (not "Never").
       d. Symbol-selector contains BTCUSDT.
       e. Selecting BTCUSDT renders the Recharts <LineChart> with the live score point.
    8. Reset for production:
       a. Tell the user to revert `PriceGapDiscoveryIntervalSec` to 300 (or their preferred value) in config.json.
       b. Confirm `PriceGapDiscoveryEnabled` setting matches user intent (default OFF unless explicitly opted in).
       c. Restart the bot to pick up the reverted config.

    REMINDER: Claude must NOT modify config.json directly per CLAUDE.local.md. All config changes are operator actions; Claude provides the exact text to set and verifies the result via `redis-cli` + log inspection.
  </how-to-verify>
  <resume-signal>Reply "approved" if all 6 exchanges produced records for BTCUSDT and the dashboard renders the live cycle. Reply with specific issues otherwise (e.g., "Bitget records all show why_rejected=stale_bbo", "OKX missing from records — symbol translation drift suspected"). Issues become Plan 07 fixes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ /api/pg/discovery/* | Authenticated dashboard operator; existing Bearer token middleware applies. |
| WS hub → frontend | Existing WS auth posture; no new authentication surface. |
| Frontend → DOM rendering | React XSS-safe by default; URL params (symbol-selector) inserted via React text-node, not innerHTML. |
| Live exchange feeds → Scanner → Telemetry → Redis | Phase Success Criterion #5 verified under runtime via Task 4 checkpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-37 | Tampering | Operator selects an arbitrary symbol in the URL hash → /api/pg/discovery/scores/UNTRUSTED → backend Redis injection | mitigate | Plan 05 Task 2 ValidateSymbol regex `^[A-Z0-9]{2,20}USDT$` rejects malformed input at the API boundary BEFORE Redis read. Frontend symbol-selector only emits values from the universe (controlled set). |
| T-11-38 | Information disclosure | i18n key drift between en + zh-TW exposes English fallback to zh-TW users showing internal field names | mitigate | discovery_keys.test.ts asserts lockstep at build time; CI fails on drift. TranslationKey type derivation also fails compile if a key is referenced but absent from EN. |
| T-11-39 | Tampering | New npm dependency sneaks in via dev-dependencies bypassing package-lock.json review | mitigate | Acceptance criterion `git diff --exit-code web/package.json web/package-lock.json` exits 0; npm lockdown enforced at the file level. |
| T-11-40 | Information disclosure | Discovery section renders raw reason codes for non-translated reason values | accept | Reason codes are operator-language (snake_case English); no PII, no exchange API leakage; fallback to raw reason if i18n key missing is by design (defensive UX). |
| T-11-41 | DoS | Operator opens 10 dashboard tabs; each subscribes to WS pg_scan_score → broker overwhelmed | accept | Existing WS hub implements per-connection rate limit (verify; if missing, that's a pre-existing concern, not Phase 11 specific). pg_scan_score is debounced 1s server-side (Plan 05 Task 1), bounding broadcast frequency. |
| T-11-42 | Spoofing | WS message with msg.type="pg_scan_cycle" but spoofed payload via XSS | mitigate | WS connection authenticated via Bearer cookie (existing); React renders text-only via JSX (no dangerouslySetInnerHTML used in any Discovery component). |
| T-11-43 | Tampering | New Tailwind classes accidentally use a different design token, breaking dark theme | mitigate | UI-SPEC.md was approved with specific tokens; acceptance criteria grep for `font-mono tabular-nums` + `border-dashed` enforce key UI-SPEC contracts. /gsd-ui-review post-execution audit catches subjective drift. |
| T-11-45 | Information disclosure | Live BBO checkpoint exposes operator credentials/keys via logs | accept | Verification reads Redis pg:scan:* keys + journalctl logs; no exchange API keys are logged in the cycle path (Scanner emits redacted reason codes only). |
</threat_model>

<verification>
- All Plan 06 tests pass: `cd web && npx vitest run --testNamePattern 'DiscoverySection|usePgDiscovery|discovery_keys'` exits 0.
- TypeScript clean: `cd web && npx tsc --noEmit`.
- Vite build succeeds: `cd web && npm run build` (frontend embed-ready).
- Go binary builds with new frontend: `cd /var/solana/data/arb && go build ./cmd/arb`.
- npm lockdown: `git diff --exit-code web/package.json web/package-lock.json`.
- Existing PriceGap tests still pass: `cd web && npx vitest run --testNamePattern 'PriceGap.candidates'`.
- Visual verification (Task 3) passes user approval covering OFF / ON / ERRORED / zh-TW locale states.
- Live BBO join verification (Task 4) passes user approval covering all 6 exchanges producing BTCUSDT records under real feeds.
</verification>

<success_criteria>
- 30+ pricegap.discovery.* i18n keys ship in EN + zh-TW lockstep.
- 6 Discovery components + 1 usePgDiscovery hook + DiscoverySection integrated into PriceGap.tsx.
- Three scanner states (ON/OFF/ERRORED) visually distinct per UI-SPEC.md.
- REST seed via /api/pg/discovery/state on mount; WS push for pg_scan_cycle / pg_scan_metrics / pg_scan_score.
- Recharts <LineChart> with <ReferenceArea> threshold-band overlay; symbol-selector dropdown.
- Zero new npm dependencies (lockdown preserved).
- TypeScript + Vite build clean.
- User-approved visual verification covering all states + locale switching.
- User-approved live BBO join verification covering all 6 exchanges producing BTCUSDT records.
</success_criteria>

<validation>
**Validation Coverage (per VALIDATION.md Dimension 8):**
- Unit tests for hook: usePgDiscovery REST seed (Test 9) + WS subscription routing (Test 10).
- Unit tests for components: 12 sub-tests covering DiscoverySection (4 states), CycleStatsCard, ScoreHistoryCard (with Recharts mock), WhyRejectedCard, PromoteTimelinePlaceholder.
- i18n lockstep test: 4 sub-tests in discovery_keys.test.ts covering key-set equality, non-empty values, token-interpolation consistency, count guard.
- TypeScript build verification: `npx tsc --noEmit` is part of acceptance criteria — TranslationKey derivation enforces lockstep at compile time.
- Vite build verification: `npm run build` is part of acceptance criteria — production bundle must build.
- npm lockdown verification: `git diff --exit-code web/package.json web/package-lock.json` is part of acceptance criteria.
- Manual-only checkpoint #1 (visual UI quality): Task 3 covers three-state rendering + locale switching.
- Manual-only checkpoint #2 (live BBO join): Task 4 covers Phase Success Criterion #5 against real exchange feeds.
- Wave 0 dependencies satisfied: this plan creates DiscoverySection.test.tsx and discovery_keys.test.ts from VALIDATION.md Wave 0 list.
- Sampling rate: vitest suite runs in <15s per VALIDATION.md.
</validation>

<output>
After completion, create `.planning/phases/11-auto-discovery-scanner-chokepoint-telemetry/11-06-SUMMARY.md` documenting:
- Component file inventory (6 files under web/src/components/Discovery/)
- Hook file (web/src/hooks/usePgDiscovery.ts)
- i18n key count added (≥30 in each of en.ts + zh-TW.ts)
- Three scanner state visual signals (green/gray/red pills)
- Recharts subcomponents used (LineChart, Line, ReferenceArea, ResponsiveContainer, CartesianGrid, XAxis, YAxis, Tooltip — all already locked)
- Test counts: 4 i18n + 12 component = 16 vitest sub-tests
- Visual verification result (user approved / issues for Plan 07)
- Live BBO join checkpoint result: pass/fail per 6-exchange BTCUSDT cross-pair coverage; record exact `redis-cli LRANGE pg:scan:cycles -1 -1` output as evidence
- Confirmation that zero npm dependencies were added
- Phase 16 migration note: <DiscoverySection /> is self-contained inside web/src/components/Discovery/ — Phase 16 (PG-OPS-09) lifts it to the new top-level Price-Gap tab without restructuring.
</output>
</content>
</invoke>