---
phase: 10-dashboard-candidate-crud
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/i18n/en.ts
  - web/src/i18n/zh-TW.ts
autonomous: true
requirements: [PG-OPS-07]
must_haves:
  truths:
    - "Both en.ts and zh-TW.ts contain the same set of pricegap.candidates.* keys (zero drift)"
    - "Add modal labels render in EN and zh-TW"
    - "Edit modal title with {symbol} interpolation works in both locales"
    - "All field-level validation error strings have keys in both locales"
    - "Delete confirm dialog body with {symbol}/{long}/{short}/{bps} interpolation works in both locales"
    - "Active-position-blocks-delete error string has keys in both locales"
  artifacts:
    - path: web/src/i18n/en.ts
      provides: "EN translations for pricegap.candidates.modal.*, pricegap.candidates.errors.*, pricegap.candidates.confirmDelete.*, pricegap.candidates.add.button"
      contains: "pricegap.candidates.modal.add.title"
    - path: web/src/i18n/zh-TW.ts
      provides: "Same key set as en.ts with Traditional Chinese values"
      contains: "pricegap.candidates.modal.add.title"
  key_links:
    - from: web/src/i18n/en.ts
      to: web/src/i18n/zh-TW.ts
      via: lockstep additions in same commit
      pattern: "pricegap.candidates"
---

<objective>
Add ALL `pricegap.candidates.*` translation keys to both `web/src/i18n/en.ts` and `web/src/i18n/zh-TW.ts` in a single commit. Plan 03 (frontend modal) will reference these keys exclusively — never inline strings. This plan ships independently of Plan 01 (no Go dependency) and independently of Plan 03 (Plan 03 can reference keys before they exist; runtime/typecheck behavior depends on this plan landing in same wave).

Purpose: Per CLAUDE.local.md i18n rules + Phase 10 D-21, every key must exist in BOTH locales. The codebase uses flat dotted keys (verified at en.ts:766+ per RESEARCH.md Pitfall 5). Phase 9 already shipped 67 pricegap keys per locale (parity confirmed via grep — both files have 67 `'pricegap.` lines). This plan extends that namespace.
Output: Both i18n files have identical key sets under `pricegap.candidates.*`.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/var/solana/data/arb/.planning/phases/10-dashboard-candidate-crud/10-CONTEXT.md
@/var/solana/data/arb/.planning/phases/10-dashboard-candidate-crud/10-RESEARCH.md
@/var/solana/data/arb/CLAUDE.local.md

<interfaces>
<!-- Both locale files use a flat string-keyed `const en = { ... } as const` shape. -->
<!-- TranslationKey type is derived from `keyof typeof en` per RESEARCH.md Pitfall 5. -->
<!-- Phase 9 already added the `pricegap.*` namespace; locate the existing pricegap block in each file and append the new keys at the end of that block. -->

Reference key naming pattern (verbatim from RESEARCH.md §"i18n — flat-key additions" lines 497–522):
- `'pricegap.candidates.add.button'`
- `'pricegap.candidates.modal.add.title'`
- `'pricegap.candidates.modal.edit.title'` (uses {symbol} {long} {short} interpolation)
- `'pricegap.candidates.modal.symbol.label'`, `.placeholder`
- `'pricegap.candidates.modal.longExch.label'`, `'.shortExch.label'`
- `'pricegap.candidates.modal.thresholdBps.label'`, `.maxPositionUSDT.label`, `.modeledSlippageBps.label`
- `'pricegap.candidates.modal.save'`, `.cancel`
- `'pricegap.candidates.errors.symbolFormat'`, `.exchangesEqual`, `.thresholdRange`, `.maxPositionRange`, `.slippageRange`, `.tupleCollision`, `.activePositionBlocksDelete`
- `'pricegap.candidates.confirmDelete.title'`, `.body` ({symbol}/{long}/{short}/{bps}), `.confirm`, `.cancel`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pricegap.candidates.* keys to web/src/i18n/en.ts</name>
  <files>web/src/i18n/en.ts</files>

  <read_first>
    - web/src/i18n/en.ts (locate the existing `'pricegap.*'` block — RESEARCH.md says it starts around line 766; read the entire pricegap section to understand existing key style and grouping)
    - .planning/phases/10-dashboard-candidate-crud/10-RESEARCH.md (§"i18n — flat-key additions" lines 497–522 for the verbatim key list with English values)
    - .planning/phases/10-dashboard-candidate-crud/10-CONTEXT.md (D-21 i18n lockstep, D-15 single-step delete confirm copy)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-CONTEXT.md (existing pricegap.candidates.disable.* key style — mirror that)
  </read_first>

  <action>
1. Open web/src/i18n/en.ts. Locate the existing `pricegap.*` block (search for `'pricegap.candidates.disable'` or `'pricegap.tab.title'` to find the namespace).

2. Append the following keys at the END of the pricegap.* block (right before the next non-pricegap namespace begins). Use the EXACT key strings and values from RESEARCH.md §"i18n — flat-key additions" lines 497–522:

```ts
'pricegap.candidates.add.button': 'Add candidate',
'pricegap.candidates.modal.add.title': 'Add candidate',
'pricegap.candidates.modal.edit.title': 'Edit {symbol} ({long}/{short})',
'pricegap.candidates.modal.symbol.label': 'Symbol',
'pricegap.candidates.modal.symbol.placeholder': 'BTCUSDT',
'pricegap.candidates.modal.longExch.label': 'Long exchange',
'pricegap.candidates.modal.shortExch.label': 'Short exchange',
'pricegap.candidates.modal.thresholdBps.label': 'Threshold (bps)',
'pricegap.candidates.modal.maxPositionUSDT.label': 'Max position (USDT)',
'pricegap.candidates.modal.modeledSlippageBps.label': 'Modeled slippage (bps)',
'pricegap.candidates.modal.save': 'Save',
'pricegap.candidates.modal.cancel': 'Cancel',
'pricegap.candidates.errors.symbolFormat': 'Symbol must be uppercase, end with USDT',
'pricegap.candidates.errors.exchangesEqual': 'Long and short exchanges must differ',
'pricegap.candidates.errors.thresholdRange': 'Threshold must be 50–1000 bps',
'pricegap.candidates.errors.maxPositionRange': 'Max position must be 100–50000 USDT',
'pricegap.candidates.errors.slippageRange': 'Modeled slippage must be 0–100 bps',
'pricegap.candidates.errors.tupleCollision': 'Candidate with this symbol+exchanges already exists',
'pricegap.candidates.errors.activePositionBlocksDelete': 'Cannot delete: candidate has an active position. Close it first.',
'pricegap.candidates.confirmDelete.title': 'Delete candidate?',
'pricegap.candidates.confirmDelete.body': '{symbol}: {long} ↔ {short}, threshold {bps} bps. This cannot be undone.',
'pricegap.candidates.confirmDelete.confirm': 'Delete',
'pricegap.candidates.confirmDelete.cancel': 'Cancel',
```

3. Run typecheck to confirm the file compiles: `cd /var/solana/data/arb/web && npx tsc -b --noEmit 2>&1 | head -20`

4. Verify the count: `grep -c "pricegap.candidates" /var/solana/data/arb/web/src/i18n/en.ts` — should return 23 (one per key).
  </action>

  <verify>
    <automated>cd /var/solana/data/arb/web && npx tsc -b --noEmit 2>&1 | tee /tmp/p10-02-en-tsc.log; test ${PIPESTATUS[0]} -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "pricegap.candidates" /var/solana/data/arb/web/src/i18n/en.ts` returns 23
    - `grep -c "pricegap.candidates.modal.add.title" /var/solana/data/arb/web/src/i18n/en.ts` returns 1
    - `grep -c "pricegap.candidates.errors.activePositionBlocksDelete" /var/solana/data/arb/web/src/i18n/en.ts` returns 1
    - `grep -c "pricegap.candidates.confirmDelete.body" /var/solana/data/arb/web/src/i18n/en.ts` returns 1
    - `cd /var/solana/data/arb/web && npx tsc -b --noEmit` exit code 0
    - The 23 new keys are inside the existing `pricegap.*` block (visual contiguity — `grep -n "pricegap" web/src/i18n/en.ts` shows the new keys grouped with existing pricegap keys, not in a random location)
  </acceptance_criteria>

  <done>
    23 keys added to en.ts, file typechecks, all keys live in the existing pricegap block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add identical key set to zh-TW.ts with Traditional Chinese translations</name>
  <files>web/src/i18n/zh-TW.ts</files>

  <read_first>
    - web/src/i18n/zh-TW.ts (locate existing `pricegap.*` block — should be at the same relative position as in en.ts)
    - web/src/i18n/en.ts (the keys you just added — must be mirrored 1:1)
    - .planning/phases/09-price-gap-dashboard-paper-live-operations/09-CONTEXT.md (existing pricegap zh-TW translation style for terminology consistency: "候選人" for candidate, "做多" for long, "做空" for short, "閾值" for threshold)
  </read_first>

  <action>
1. Open web/src/i18n/zh-TW.ts. Locate the existing `pricegap.*` block (mirror position from en.ts).

2. Append the following 23 keys at the END of the pricegap.* block. Key strings MUST be byte-identical to en.ts; only the right-hand-side values change:

```ts
'pricegap.candidates.add.button': '新增候選',
'pricegap.candidates.modal.add.title': '新增候選',
'pricegap.candidates.modal.edit.title': '編輯 {symbol} ({long}/{short})',
'pricegap.candidates.modal.symbol.label': '交易對',
'pricegap.candidates.modal.symbol.placeholder': 'BTCUSDT',
'pricegap.candidates.modal.longExch.label': '做多交易所',
'pricegap.candidates.modal.shortExch.label': '做空交易所',
'pricegap.candidates.modal.thresholdBps.label': '觸發閾值 (bps)',
'pricegap.candidates.modal.maxPositionUSDT.label': '單筆上限 (USDT)',
'pricegap.candidates.modal.modeledSlippageBps.label': '預估滑價 (bps)',
'pricegap.candidates.modal.save': '儲存',
'pricegap.candidates.modal.cancel': '取消',
'pricegap.candidates.errors.symbolFormat': '交易對必須為大寫,並以 USDT 結尾',
'pricegap.candidates.errors.exchangesEqual': '做多與做空交易所不可相同',
'pricegap.candidates.errors.thresholdRange': '閾值需介於 50–1000 bps',
'pricegap.candidates.errors.maxPositionRange': '單筆上限需介於 100–50000 USDT',
'pricegap.candidates.errors.slippageRange': '滑價需介於 0–100 bps',
'pricegap.candidates.errors.tupleCollision': '已存在相同的交易對+交易所組合',
'pricegap.candidates.errors.activePositionBlocksDelete': '無法刪除:此候選有未平倉的部位,請先平倉。',
'pricegap.candidates.confirmDelete.title': '刪除候選?',
'pricegap.candidates.confirmDelete.body': '{symbol}: {long} ↔ {short}, 閾值 {bps} bps。此操作無法復原。',
'pricegap.candidates.confirmDelete.confirm': '刪除',
'pricegap.candidates.confirmDelete.cancel': '取消',
```

3. Run lockstep parity verification:
   ```
   diff <(grep -oE "'pricegap\\.candidates\\.[a-zA-Z0-9_.]+'" /var/solana/data/arb/web/src/i18n/en.ts | sort -u) \
        <(grep -oE "'pricegap\\.candidates\\.[a-zA-Z0-9_.]+'" /var/solana/data/arb/web/src/i18n/zh-TW.ts | sort -u)
   ```
   Output MUST be empty.

4. Typecheck: `cd /var/solana/data/arb/web && npx tsc -b --noEmit`

5. Build: `cd /var/solana/data/arb/web && npm run build` (per CLAUDE.local.md build order — frontend before Go binary).
  </action>

  <verify>
    <automated>cd /var/solana/data/arb && diff <(grep -oE "'pricegap\\.candidates\\.[a-zA-Z0-9_.]+'" web/src/i18n/en.ts | sort -u) <(grep -oE "'pricegap\\.candidates\\.[a-zA-Z0-9_.]+'" web/src/i18n/zh-TW.ts | sort -u) > /tmp/p10-02-i18n-diff.log 2>&1; test ! -s /tmp/p10-02-i18n-diff.log</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "pricegap.candidates" /var/solana/data/arb/web/src/i18n/zh-TW.ts` returns 23 (matches en.ts count from Task 1)
    - `diff <(grep -oE "'pricegap\\.candidates\\.[a-zA-Z0-9_.]+'" /var/solana/data/arb/web/src/i18n/en.ts | sort -u) <(grep -oE "'pricegap\\.candidates\\.[a-zA-Z0-9_.]+'" /var/solana/data/arb/web/src/i18n/zh-TW.ts | sort -u)` produces empty output (zero drift)
    - `cd /var/solana/data/arb/web && npx tsc -b --noEmit` exit code 0
    - `cd /var/solana/data/arb/web && npm run build` exit code 0 (Vite build succeeds; emits to web/dist/ which is then go:embed'd by the Go binary)
    - Manual visual inspection: zh-TW values use Traditional Chinese characters (not Simplified — e.g., "儲存" not "保存", "無法" not "无法")
  </acceptance_criteria>

  <done>
    23 zh-TW keys added with Traditional Chinese translations. EN/zh-TW key sets identical (zero drift verified by diff). Frontend build green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Translation file → rendered DOM | Static strings; bundled at build time, no user input merged here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | I (Information disclosure) | Missing zh-TW key falls back to English at runtime | mitigate | Lockstep diff in acceptance_criteria; failing build if drift exists in this plan; Plan 05 adds an automated lint task to keep parity going forward |
| T-10-10 | T (Tampering: XSS via interpolation) | {symbol}/{long}/{short}/{bps} placeholders | mitigate | React renders text nodes (not innerHTML); placeholder substitution in t() helper escapes by default; symbol/long/short are validated server-side per Plan 01 (regex-bounded) so no untrusted strings reach the placeholder |
</threat_model>

<verification>
- `npx tsc -b --noEmit` exits 0 in web/ after Task 1 and Task 2
- `npm run build` exits 0 after Task 2
- `diff` between en.ts and zh-TW.ts pricegap.candidates.* keys produces zero output (lockstep verified)
- Visual inspection: zh-TW renders Traditional Chinese (Plan 05 includes manual UAT in zh-TW locale for layout/wrap)
</verification>

<success_criteria>
- 23 new keys in en.ts, 23 new keys in zh-TW.ts, key strings identical
- Both files typecheck and build
- Frontend build succeeds (web/dist/ regenerated for Plan 03 + Plan 05 to consume)
- All Plan 03 modal labels, validation errors, and confirm copy are reachable via t() with correct keys
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-candidate-crud/10-02-SUMMARY.md` documenting:
- Final 23-key list (with EN + zh-TW values side-by-side)
- Confirmation that diff returned empty
- Tested locale-switch behavior (manual or note for Plan 05)
- Note for Plan 03: complete key list available; modal must use t('pricegap.candidates.*') exclusively, no inline strings
</output>
</content>
</invoke>