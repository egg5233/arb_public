---
phase: 10-dashboard-candidate-crud
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified: []
autonomous: false
requirements: [PG-OPS-07]
must_haves:
  truths:
    - "Operator manually adds a candidate via dashboard, sees row appear in table after Save"
    - "Operator refreshes the page, candidate persists (config.json round-trip confirmed)"
    - "Operator edits candidate threshold, sees value updated after Save + refresh"
    - "Operator attempts to delete a candidate WITH an active position, sees 'Cannot delete: candidate has an active position' error"
    - "Operator deletes a candidate without active position, row disappears + persists across refresh"
    - "DevTools Network panel shows ZERO POST /api/config calls during: tab mount, Add modal open, Add modal cancel, Edit modal open, Edit modal cancel, Delete dialog dismiss (PG-OPS-08 carryover)"
    - "zh-TW locale renders all modal labels and errors without overflow / truncation"
    - "config.json.bak file exists with mtime older than current config.json after first Save (SaveJSON .bak invariant)"
    - "Existing per-candidate Disable/Re-enable buttons (Phase 9) still work — UAT regression check"
  artifacts: []
  key_links: []
---

<objective>
Final manual UAT pass before phase sign-off. Verifies what automated tests cannot: real browser DevTools Network observation, real config.json round-trip + .bak backup file presence, real zh-TW visual layout, real Phase-9 button regression check. Operator (the user) drives this; Claude pauses for verification.

Purpose: VALIDATION.md "Manual-Only Verifications" table mandates these checks; they catch the PG-OPS-08-class regressions that vitest fetch-spy cannot prove definitively in a real browser.
Output: Sign-off (or punch list of fixes if any UAT step fails).
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/var/solana/data/arb/.planning/phases/10-dashboard-candidate-crud/10-CONTEXT.md
@/var/solana/data/arb/.planning/phases/10-dashboard-candidate-crud/10-VALIDATION.md
@/var/solana/data/arb/.planning/phases/10-dashboard-candidate-crud/10-01-SUMMARY.md
@/var/solana/data/arb/.planning/phases/10-dashboard-candidate-crud/10-02-SUMMARY.md
@/var/solana/data/arb/.planning/phases/10-dashboard-candidate-crud/10-03-SUMMARY.md
@/var/solana/data/arb/.planning/phases/10-dashboard-candidate-crud/10-04-SUMMARY.md
@/var/solana/data/arb/CLAUDE.local.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and confirm full test suite green pre-UAT</name>
  <files>none — verification-only</files>

  <read_first>
    - .planning/phases/10-dashboard-candidate-crud/10-VALIDATION.md (sampling rate + full-suite command)
  </read_first>

  <action>
1. Run full Go suite: `cd /var/solana/data/arb && go test ./... -count=1 2>&1 | tail -30`
2. Run full vitest suite: `cd /var/solana/data/arb/web && npx vitest run 2>&1 | tail -30`
3. Build production binary in correct order (per CLAUDE.local.md):
   ```
   cd /var/solana/data/arb/web && npm run build
   cd /var/solana/data/arb && go build -o /tmp/arb-phase10 ./
   ```
4. Confirm `/tmp/arb-phase10` is fresh and the embedded `web/dist/` contains the new modal. Quick sanity: `strings /tmp/arb-phase10 | grep -c pricegap.candidates.modal.add.title` should return ≥1.
  </action>

  <verify>
    <automated>cd /var/solana/data/arb && go test ./... -count=1 > /tmp/p10-05-go.log 2>&1 && cd /var/solana/data/arb/web && npx vitest run > /tmp/p10-05-vitest.log 2>&1 && cd /var/solana/data/arb/web && npm run build > /tmp/p10-05-build.log 2>&1 && cd /var/solana/data/arb && go build -o /tmp/arb-phase10 ./ > /tmp/p10-05-gobuild.log 2>&1; test ${PIPESTATUS[0]} -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `cd /var/solana/data/arb && go test ./... -count=1` exit code 0
    - `cd /var/solana/data/arb/web && npx vitest run` exit code 0
    - `cd /var/solana/data/arb/web && npm run build` exit code 0 (web/dist/ regenerated)
    - `cd /var/solana/data/arb && go build -o /tmp/arb-phase10 ./` exit code 0
    - `strings /tmp/arb-phase10 | grep -c pricegap.candidates.modal.add.title` returns ≥1 (new i18n key embedded in binary via go:embed)
  </acceptance_criteria>

  <done>
    Full Go + Vitest suites green; production binary built with embedded frontend that includes the new modal keys.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — full Add/Edit/Delete round-trip</name>
  <files>none — operator runs the binary built in Task 1 and exercises the dashboard manually</files>

  <read_first>
    - .planning/phases/10-dashboard-candidate-crud/10-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/10-dashboard-candidate-crud/10-CONTEXT.md (D-02 dismiss paths, D-14 active-position 409 wording, D-15 single-step delete confirm, D-21 i18n lockstep)
    - CLAUDE.local.md (CRITICAL: Do not modify config.json — confirms the SaveJSON .bak invariant being checked in Step 8)
  </read_first>

  <action>
This task is manual-only — Claude cannot drive a real browser DevTools session against the live dashboard. Claude's role here is to (a) present the script, (b) wait for operator sign-off, (c) record the result.

Operator must execute the 16 UAT steps documented in `<how-to-verify>` below, in order, against the binary built in Task 1. Operator should run the binary in a SAFE configuration (sandbox copy of config.json OR explicit acceptance + .preuat backup) per Setup Step 1. Claude must NOT launch the binary unilaterally — wait for operator confirmation before any binary launch suggestion is acted on.

After UAT, operator types `approved` (success) or describes any divergence (failure → file follow-up plan or carry-forward to v2.2 backlog).
  </action>

  <verify>
    <automated>MISSING — manual checkpoint; operator confirmation gates phase sign-off. No automated command can validate real-browser DevTools Network observation, real .bak file mtime, or zh-TW visual layout.</automated>
  </verify>

  <done>
Operator types `approved` after all 16 UAT steps pass, OR documents divergences and Claude opens a follow-up gap-closure plan / v2.2 backlog item.
  </done>

  <acceptance_criteria>
    - All 16 UAT steps in `<how-to-verify>` execute with operator-recorded result (PASS or FAIL with specific divergence)
    - PG-OPS-08 carryover invariant confirmed (Steps 1-5 show zero POST /api/config in DevTools Network)
    - config.json round-trip confirmed (Step 7: candidate persists across page reload)
    - config.json.bak created with older mtime (Step 8)
    - 409 friendly error displayed (Step 13, if applicable)
    - zh-TW labels render without overflow (Step 14)
    - Phase-9 Disable/Re-enable buttons untouched (Step 15)
    - Operator sign-off captured (Step 16)
  </acceptance_criteria>

  <what-built>
- Backend `/api/config` accepts `price_gap.candidates` array, validates, guards active positions, persists via SaveJSON (Plan 01)
- 23 i18n keys in EN + zh-TW under `pricegap.candidates.*` (Plan 02)
- Add candidate button + Add/Edit modal + Delete confirm dialog in PriceGap.tsx (Plan 03)
- Vitest covers state/validation/PG-OPS-08 invariant; Go test pins tracker hot-reload (Plan 04)
- Production binary built at `/tmp/arb-phase10` with embedded frontend (Plan 05 Task 1)
  </what-built>

  <how-to-verify>
**SETUP (do not run binary yet — confirm with operator before launching anything against live config.json):**

1. Operator confirms test environment. The /tmp/arb-phase10 binary will read the SAME config.json that the live trading binary reads. Two safe paths:
   - **Option A (preferred):** stop the systemd-managed `arb` binary, copy config.json to a sandbox path, run /tmp/arb-phase10 against the sandbox copy via env override (or run on a different port + isolated config)
   - **Option B (riskier):** UAT against live binary. Only proceed if operator explicitly accepts the risk that test edits will hit the real candidates list. Operator must `cp config.json config.json.preuat` first.

2. Operator launches the test binary and opens the dashboard at the configured port.

**UAT STEPS — perform in this exact order:**

**Step 1 (PG-OPS-08 mount-time invariant):**
   - Open Chrome DevTools → Network tab → filter by "config"
   - Navigate to the Price-Gap tab
   - EXPECTED: Zero POST /api/config requests visible. Only GET /api/config and GET /api/pricegap/state.

**Step 2 (Add modal open invariant):**
   - Click "Add candidate" button (above the table, right-aligned)
   - EXPECTED: Modal opens; Network panel shows zero new POST /api/config

**Step 3 (Add modal cancel invariant):**
   - Click Cancel button
   - EXPECTED: Modal dismisses; zero new POST /api/config

**Step 4 (Backdrop dismiss):**
   - Click "Add candidate" again, then click outside the modal card (on the dark backdrop)
   - EXPECTED: Modal dismisses; zero POSTs

**Step 5 (ESC dismiss):**
   - Click "Add candidate" again, press ESC
   - EXPECTED: Modal dismisses; zero POSTs

**Step 6 (Add candidate — happy path):**
   - Click "Add candidate" → fill form: symbol=`TESTUATUSDT`, long=`gateio`, short=`bingx`, threshold=300, max_position=1000, slippage=10
   - Click Save
   - EXPECTED: Modal closes; new row appears in table with TESTUATUSDT/gateio/bingx; Network shows exactly 1 POST /api/config with status 200

**Step 7 (Persistence check):**
   - Reload the page (Ctrl-R)
   - EXPECTED: TESTUATUSDT row is still in the table after reload (config.json round-trip confirmed)

**Step 8 (.bak file):**
   - In a separate terminal: `ls -la <config.json path> <config.json.bak path>`
   - EXPECTED: Both files exist; mtime of .bak file is older than mtime of config.json

**Step 9 (Edit):**
   - Click Edit on TESTUATUSDT row → change threshold to 500 → Save
   - EXPECTED: Modal closes; row shows threshold=500; reload page; threshold still 500

**Step 10 (Delete confirm dialog):**
   - Click Delete on TESTUATUSDT row
   - EXPECTED: Confirm dialog appears showing "TESTUATUSDT: gateio ↔ bingx, threshold 500 bps"

**Step 11 (Delete cancel):**
   - Click Cancel on dialog
   - EXPECTED: Dialog dismisses; row still present; zero POSTs since dialog opened

**Step 12 (Delete confirm — no active position):**
   - Click Delete again → Confirm
   - EXPECTED: Dialog closes; TESTUATUSDT row gone; reload — still gone

**Step 13 (Delete blocked by active position) — OPTIONAL if any candidate has live position:**
   - Pick a candidate with an active position (or seed a paper position using existing flow)
   - Try Delete on it → Confirm
   - EXPECTED: Modal stays open with red error: "Cannot delete: candidate has an active position. Close it first."
   - Network shows 1 POST returning HTTP 409

**Step 14 (zh-TW locale visual check):**
   - Switch dashboard locale to 繁體中文 (zh-TW) via existing locale toggle
   - Click Add candidate
   - EXPECTED: All labels render in Traditional Chinese without overflow / truncation; field labels: 交易對, 做多交易所, 做空交易所, 觸發閾值 (bps), 單筆上限 (USDT), 預估滑價 (bps)
   - Type bad symbol "btc" → click Save → expect zh-TW error: "交易對必須為大寫,並以 USDT 結尾"

**Step 15 (Phase-9 regression — Disable/Re-enable buttons unchanged):**
   - On any candidate row, click the existing Disable button (Phase 9)
   - EXPECTED: Existing Phase-9 Disable modal opens (NOT the new Phase-10 modal); flow works exactly as before
   - Re-enable similarly

**Step 16 (Sign-off):**
   - If any step diverges from EXPECTED, list the divergence(s)
   - If all steps pass, type `approved`
  </how-to-verify>

  <resume-signal>Type "approved" if all 16 UAT steps pass, or describe any divergence.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test binary → live config.json | Operator-managed; explicit pre-UAT confirmation gates this risk per Task 2 SETUP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-20 | T (Tampering: UAT corrupts live config.json) | Test edits against live config | mitigate | UAT Setup Step 1 mandates sandbox copy or explicit operator acceptance + .preuat backup |
| T-10-21 | I (PG-OPS-08 regression slips through) | Vitest fetch-spy cannot fully prove zero real-browser POST | mitigate | Steps 1-5 use real DevTools Network in production-like environment |
</threat_model>

<verification>
- Full automated suite green (Task 1)
- All 16 manual UAT steps pass with operator sign-off (Task 2)
- Phase 9 Disable/Re-enable buttons untouched (Step 15 regression)
- PG-OPS-08 carryover invariant confirmed in real browser (Steps 1-5)
- zh-TW visual layout acceptable (Step 14)
- config.json.bak created with correct mtime (Step 8)
</verification>

<success_criteria>
- Operator types "approved" at the end of UAT
- All 16 steps either pass or are documented with a fix issued in a follow-up plan
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-candidate-crud/10-05-SUMMARY.md` documenting:
- Test suite green status
- UAT sign-off transcript (16 steps with PASS/FAIL)
- Any divergences observed and fix plan (or carry-forward to v2.2 backlog)
- Phase 10 complete — handoff to /gsd-verify-work for milestone-level audit
</output>
</content>
</invoke>