---
phase: 04-performance-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/package-lock.json
autonomous: false
requirements: [AN-04]
user_setup:
  - service: npm
    why: "Recharts charting library required for interactive PnL charts (D-07)"
    dashboard_config:
      - task: "Approve lockfile update after reviewing diff for axios contamination"
        location: "Terminal output of npm install --package-lock-only"

must_haves:
  truths:
    - "Recharts 3.8.x is in package.json dependencies"
    - "react-is ^19.0.0 is in package.json dependencies"
    - "npm ci succeeds without errors"
    - "Frontend builds successfully with Recharts available for import"
  artifacts:
    - path: "web/package.json"
      provides: "recharts and react-is declared as dependencies"
      contains: "recharts"
    - path: "web/package-lock.json"
      provides: "Locked dependency tree including recharts"
      contains: "recharts"
  key_links:
    - from: "web/package.json"
      to: "web/package-lock.json"
      via: "npm ci reads lockfile to install exact versions"
      pattern: "recharts"
---

<objective>
Add Recharts and react-is to the frontend dependency tree via a controlled, auditable npm lockfile update.

Purpose: The npm lockfile is security-locked due to a compromised axios dependency. Adding Recharts requires a deliberate one-time lockfile update with user review, separate from any code changes, so the diff can be audited for contamination.

Output: Updated package.json and package-lock.json with recharts@^3.8.1 and react-is@^19.0.0, frontend builds cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-performance-analytics/04-CONTEXT.md
@.planning/phases/04-performance-analytics/04-RESEARCH.md
@.planning/phases/04-performance-analytics/04-UI-SPEC.md

@web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update npm lockfile with Recharts and react-is dependencies</name>
  <files>web/package.json, web/package-lock.json</files>
  <read_first>web/package.json, CLAUDE.md</read_first>
  <action>
**CRITICAL: This task requires special npm handling due to the security lockdown (CLAUDE.md).**

Per D-07 and D-09, add Recharts to the frontend. Per the RESEARCH.md pitfall 1 and UI-SPEC dependency section:

1. Change to web/ directory: `cd /var/solana/data/arb/web`

2. Run lockfile-only update (does NOT install anything, only regenerates lockfile):
   ```bash
   npm install --package-lock-only recharts@^3.8.1 react-is@^19.0.0
   ```

3. Audit the lockfile diff for axios contamination:
   ```bash
   git diff web/package-lock.json | grep -i axios
   ```
   If "axios" appears ANYWHERE in the diff, STOP and report to user -- do NOT proceed.

4. Verify package.json shows the new dependencies in "dependencies":
   - `"recharts": "^3.8.1"`
   - `"react-is": "^19.0.0"`

5. Run `npm ci` to do a clean install from the updated lockfile.

6. Verify the frontend builds:
   ```bash
   cd /var/solana/data/arb/web && npm run build
   ```
   This runs `tsc -b && vite build`. Must exit 0.

**DO NOT run bare `npm install` without `--package-lock-only` flag. DO NOT run `npm update`. DO NOT modify any other dependencies.**
  </action>
  <verify>
    <automated>cd /var/solana/data/arb/web && npm ci && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - web/package.json contains `"recharts": "^3.8.1"` in dependencies
    - web/package.json contains `"react-is": "^19.0.0"` in dependencies
    - web/package-lock.json contains `"recharts"` (resolved version)
    - `git diff web/package-lock.json | grep -ic axios` returns 0 (no axios contamination)
    - `cd web && npm ci` exits 0
    - `cd web && npm run build` exits 0 (tsc + vite build succeeds)
  </acceptance_criteria>
  <done>Recharts and react-is are in package.json and lockfile. Frontend builds cleanly. No axios contamination in lockfile diff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User approval of npm lockfile changes</name>
  <files>web/package.json, web/package-lock.json</files>
  <action>Present the lockfile diff to the user for review. The user must verify that no axios contamination was introduced and approve the lockfile update before it is committed.</action>
  <what-built>Updated npm lockfile with Recharts 3.8.1 and react-is 19.x as new frontend dependencies. Audited lockfile diff for axios contamination (none found). Frontend builds successfully.</what-built>
  <how-to-verify>
    1. Review the lockfile diff: `cd /var/solana/data/arb && git diff web/package-lock.json | head -100`
    2. Confirm NO axios references in diff: `git diff web/package-lock.json | grep -i axios` should return empty
    3. Confirm recharts is present: `grep "recharts" web/package.json`
    4. Confirm build succeeds: `cd web && npm run build`
  </how-to-verify>
  <verify>
    <automated>cd /var/solana/data/arb && grep '"recharts"' web/package.json && grep '"react-is"' web/package.json</automated>
  </verify>
  <done>User has reviewed and approved the npm lockfile changes.</done>
  <resume-signal>Type "approved" to commit the lockfile changes, or describe concerns</resume-signal>
</task>

</tasks>

<verification>
- `grep '"recharts"' web/package.json` shows the dependency
- `grep '"react-is"' web/package.json` shows the dependency
- `cd web && npm ci && npm run build` succeeds
- `git diff web/package-lock.json | grep -ic axios` returns 0
</verification>

<success_criteria>
1. Recharts ^3.8.1 and react-is ^19.0.0 declared in package.json
2. Lockfile updated with no axios contamination
3. Frontend compiles and builds with new dependencies
4. User has reviewed and approved the lockfile change
</success_criteria>

<output>
After completion, create `.planning/phases/04-performance-analytics/04-02-SUMMARY.md`
</output>
