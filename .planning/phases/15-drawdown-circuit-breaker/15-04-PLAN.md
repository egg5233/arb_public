---
phase: 15
plan: 04
type: execute
wave: 3
depends_on: [15-03]
files_modified:
  - cmd/pg-admin/breaker.go
  - cmd/pg-admin/main.go
  - cmd/pg-admin/breaker_test.go
  - internal/api/pricegap_breaker_handlers.go
  - internal/api/pricegap_breaker_handlers_test.go
  - internal/api/server.go
  - internal/notify/pricegap_breaker.go
  - internal/notify/pricegap_breaker_test.go
  - internal/pricegaptrader/breaker_test_fire.go
  - internal/pricegaptrader/breaker_test_fire_test.go
  - internal/pricegaptrader/breaker_recovery.go
  - internal/database/pricegap_state.go
  - cmd/main.go
  - VERSION
  - CHANGELOG.md
autonomous: true
requirements: [PG-LIVE-02]
must_haves:
  truths:
    - "pg-admin breaker subcommand has 3 verbs: recover, test-fire [--dry-run], show; recover requires typed phrase RECOVER, test-fire requires TEST-FIRE per D-12"
    - "REST API exposes POST /api/pg/breaker/recover, POST /api/pg/breaker/test-fire, GET /api/pg/breaker/state — first two require bearer auth + typed-phrase body match"
    - "TelegramNotifier.NotifyPriceGapBreakerTrip + Recovery use critical bucket bypassing allowlist"
    - "Synthetic test-fire performs a real trip by default; --dry-run flag computes 24h PnL but writes NOTHING (no Redis state mutation, no trip log entry, no Telegram, no WS)"
    - "Recovery zeroes PaperModeStickyUntil + clears all Candidate.PausedByBreaker + LSets recovery_ts + recovery_operator on most-recent trip + dispatches Telegram + WS broadcast"
    - "TestSyntheticFireFullCycle exercises trip → paper-flip → operator recovery WITHOUT engine restart (success-criterion #5)"
    - "WS broadcast wires through Server.Hub() public accessor (Phase 12 D-15 boundary precedent)"
    - "cmd/main.go wires aggregator + breaker controller via setters; spawns daemon when PriceGapBreakerEnabled=true"
    - "VERSION bumped + CHANGELOG.md entry naming Phase 15 Plan 04"
  artifacts:
    - path: "cmd/pg-admin/breaker.go"
      provides: "pg-admin breaker {recover|test-fire|show} subcommands"
      contains: "TEST-FIRE"
    - path: "internal/api/pricegap_breaker_handlers.go"
      provides: "POST recover + POST test-fire + GET state handlers"
      contains: "confirmation_phrase"
    - path: "internal/notify/pricegap_breaker.go"
      provides: "TelegramNotifier critical-bucket dispatch for trip + recovery"
      contains: "sendCritical"
    - path: "internal/pricegaptrader/breaker_test_fire.go"
      provides: "BreakerController.TestFire(ctx, dryRun)"
      contains: "test_fire_dry_run"
    - path: "internal/pricegaptrader/breaker_recovery.go"
      provides: "BreakerController.Recover(ctx, operator)"
      contains: "ClearAllPausedByBreaker"
    - path: "cmd/main.go"
      provides: "wires aggregator + breaker controller; spawns daemon"
      contains: "SetBreakerController"
    - path: "VERSION"
      provides: "version bump for Phase 15"
    - path: "CHANGELOG.md"
      provides: "Phase 15 entry naming PG-LIVE-02"
  key_links:
    - from: "POST /api/pg/breaker/recover"
      to: "BreakerController.Recover(ctx, operator)"
      via: "auth check + confirmation_phrase=='RECOVER'"
      pattern: "RECOVER"
    - from: "POST /api/pg/breaker/test-fire"
      to: "BreakerController.TestFire(ctx, dryRun)"
      via: "auth check + confirmation_phrase=='TEST-FIRE' + dry_run body field"
      pattern: "TEST-FIRE"
    - from: "BreakerController.Recover"
      to: "Registry.ClearAllPausedByBreaker + UpdateBreakerTripRecovery + NotifyPriceGapBreakerRecovery + WS"
      via: "5-step inverse of trip"
      pattern: "ClearAllPausedByBreaker"
    - from: "cmd/main.go"
      to: "Tracker.SetBreakerController"
      via: "setter wiring after Tracker created; daemon spawned in tracker.Start"
      pattern: "SetBreakerController"
---

<objective>
Plan 15-04: Operations surfaces — pg-admin CLI, REST API, Telegram critical-bucket dispatch, synthetic test-fire path, recovery path, full bootstrap wiring in cmd/main.go.

Purpose: Plan 15-03 produces the daemon mechanics. Plan 15-04 ships the operator-facing surface so Plan 15-05 (frontend) has working endpoints, and the synthetic-fire integration test covers ROADMAP success-criterion #5 ("exercises full breaker → paper-flip → operator recovery without engine restart"). Version bump + CHANGELOG entry land here.
Output: 3 pg-admin subcommands, 3 REST endpoints, 2 Telegram methods, TestFire + Recover paths in BreakerController, full main.go wiring, end-to-end synthetic-cycle integration test, VERSION + CHANGELOG.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-drawdown-circuit-breaker/15-CONTEXT.md
@.planning/phases/15-drawdown-circuit-breaker/15-RESEARCH.md
@.planning/phases/15-drawdown-circuit-breaker/15-01-SUMMARY.md
@.planning/phases/15-drawdown-circuit-breaker/15-02-SUMMARY.md
@.planning/phases/15-drawdown-circuit-breaker/15-03-SUMMARY.md
@.planning/phases/14-daily-reconcile-live-ramp-controller/14-04-SUMMARY.md
@cmd/pg-admin/main.go
@cmd/main.go
@internal/api/server.go
@internal/notify/pricegap_reconcile.go
@internal/notify/telegram.go
@internal/pricegaptrader/tracker.go
@internal/pricegaptrader/breaker_controller.go
@VERSION
@CHANGELOG.md

<interfaces>

Critical-bucket pattern from internal/notify/pricegap_reconcile.go (Phase 14 — REUSE shape):

```
func (t *TelegramNotifier) NotifyPriceGapReconcileFailure(payload ReconcileFailurePayload) error {
    msg := buildReconcileFailureMsg(payload)
    return t.sendCritical(msg)  // bypasses allowlist
}
```

pg-admin subcommand registration pattern from cmd/pg-admin/main.go:

```
switch os.Args[1] {
case "ramp": rampCmd(os.Args[2:])
case "reconcile": reconcileCmd(os.Args[2:])
// Phase 15 ADDS:
case "breaker": breakerCmd(os.Args[2:])
default: usage()
}
```

API handlers pattern in internal/api/server.go:

```
mux.Handle("GET /api/pg/ramp", authMiddleware(s.handlePgRampGet))
// Phase 15 ADDS:
mux.Handle("POST /api/pg/breaker/recover", authMiddleware(s.handlePgBreakerRecover))
mux.Handle("POST /api/pg/breaker/test-fire", authMiddleware(s.handlePgBreakerTestFire))
mux.Handle("GET /api/pg/breaker/state", authMiddleware(s.handlePgBreakerStateGet))
```

Server.Hub() public accessor (Phase 12 Plan 12-03 D-15 boundary precedent — reuse for breaker WS):

```
func (s *Server) Hub() *Hub { return s.hub }
```

cmd/main.go setter pattern from Phase 14 — Phase 15 extends:

```
tracker := pricegaptrader.NewTracker(cfg, ...)
reconciler := pricegaptrader.NewReconciler(cfg, db, ...)
tracker.SetReconciler(reconciler)
ramp := pricegaptrader.NewRampController(cfg, db, ...)
tracker.SetRamp(ramp)
sizer := pricegaptrader.NewSizer(cfg, ramp)
tracker.SetSizer(sizer)
// Phase 15 ADDS:
aggregator := pricegaptrader.NewRealizedPnLAggregator(db)
tracker.SetBreakerStore(db)
breaker := pricegaptrader.NewBreakerController(cfg, db, aggregator, telegramNotifier, log)
breaker.SetWSBroadcaster(server.Hub())
breaker.SetRamp(ramp)
breaker.SetRegistry(registry)
breaker.SetPositions(db)
tracker.SetBreakerController(breaker)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Telegram critical-bucket dispatch + BreakerController.TestFire + Recover</name>
  <files>internal/notify/pricegap_breaker.go, internal/notify/pricegap_breaker_test.go, internal/pricegaptrader/breaker_test_fire.go, internal/pricegaptrader/breaker_recovery.go, internal/pricegaptrader/breaker_test_fire_test.go, internal/database/pricegap_state.go</files>
  <read_first>
    - internal/notify/pricegap_reconcile.go (full file — exact critical-bucket pattern Plan 15-04 mirrors)
    - internal/notify/telegram.go (locate sendCritical or equivalent allowlist-bypass)
    - .planning/phases/15-drawdown-circuit-breaker/15-CONTEXT.md (D-13 dual-surface, D-14 dry-run semantics, D-17 alert format, D-18 trip record fields)
    - .planning/phases/15-drawdown-circuit-breaker/15-RESEARCH.md §"Open Questions" Q4 (dry-run does NOT write to pg:breaker:trips per D-14 "skips all mutations")
    - internal/pricegaptrader/breaker_controller.go (Plan 15-03 trip method — model TestFire and Recover after it)
    - .planning/phases/15-drawdown-circuit-breaker/15-03-SUMMARY.md (BreakerNotifier interface, store interface signature)
  </read_first>
  <behavior>
    Telegram tests:
    - Test 1 — TestNotifyPriceGapBreakerTrip_CriticalBucket: trip record built; mock Telegram client records dispatched message; assert message uses critical bucket (allowlist bypass), contains 24h PnL, threshold, ramp stage, paused count, recovery instruction line, Asia/Taipei window timestamps, source label.
    - Test 2 — TestNotifyPriceGapBreakerRecovery_CriticalBucket: recovery dispatched with operator name; critical bucket; message contains operator name, recovery timestamp (Asia/Taipei), original trip 24h PnL.

    BreakerController.TestFire and Recover tests:
    - Test 3 — TestSyntheticFireFullCycle: state.PaperModeStickyUntil=0; call TestFire(ctx, dryRun=false); verify state has sticky=MaxInt64, AppendBreakerTrip called once with Source="test_fire", NotifyPriceGapBreakerTrip called once, WS broadcast called once, candidates with active positions paused. Then call Recover(ctx, "alice") — state.PaperModeStickyUntil=0, candidates cleared, NotifyPriceGapBreakerRecovery called with operator="alice", trip log LSet with recovery_ts + recovery_operator. Test passes WITHOUT engine restart (in-process state mutation only). This is the success-criterion #5 evidence.
    - Test 4 — TestSyntheticFireDryRun_NoMutations: state.PaperModeStickyUntil=0; call TestFire(ctx, dryRun=true); aggregator IS called (dry run computes PnL); state.PaperModeStickyUntil STILL 0; AppendBreakerTrip NOT called; NotifyPriceGapBreakerTrip NOT called; WS broadcast NOT called; PauseAllOpenCandidates NOT called. Returns dry-run record with would-be PnL but NOT persisted.
    - Test 5 — TestRecover_PreservesOperatorDisabled: candidate {Disabled=true, PausedByBreaker=true} pre-recovery; Recover(ctx, "alice"); candidate end state {Disabled=true, PausedByBreaker=false} (Disabled not touched, D-11).
    - Test 6 — TestBreaker_RecoveryReenablesCandidatesAndClearsSticky: full integration — pre-state has sticky=MaxInt64 + 5 paused candidates; Recover; post-state sticky=0, all 5 candidates have PausedByBreaker=false, Telegram dispatched, WS broadcast, trip log entry at index 0 LSet with recovery fields.
    - Test 7 — TestRecover_NotTrippedReturnsError: pre-state has sticky=0 (not tripped); Recover returns explicit error "breaker not tripped (sticky=0)" — operator cannot recover when nothing to recover from.
  </behavior>
  <action>
    Step A — Create internal/notify/pricegap_breaker.go with two methods that mirror NotifyPriceGapReconcileFailure shape (Phase 14):

    NotifyPriceGapBreakerTrip(record models.BreakerTripRecord) error builds a markdown message with:
    - source label (live | test_fire | test_fire_dry_run)
    - Realized 24h PnL (USDT, 2 decimals)
    - Threshold (USDT, 2 decimals)
    - Window start/end displayed in Asia/Taipei timezone using existing helper or time.LoadLocation("Asia/Taipei")
    - Ramp Stage
    - Paused Candidates count
    - Trip time (Asia/Taipei)
    - Recovery instruction line: literal "Recover via: pg-admin breaker recover --confirm or dashboard Recover button"

    Then dispatches via t.sendCritical(msg) — same allowlist-bypass path as NotifyPriceGapReconcileFailure (Phase 14 critical-bucket precedent at internal/notify/pricegap_reconcile.go:83).

    NotifyPriceGapBreakerRecovery(record models.BreakerTripRecord, operator string) error builds a markdown message with:
    - operator name (bold)
    - Recovery time (Asia/Taipei)
    - Original trip PnL + threshold
    - "Engine has resumed normal mode" closing line
    Dispatches via t.sendCritical(msg).

    Step B — Add LoadBreakerTripAt(idx int64) (models.BreakerTripRecord, bool, error) helper to internal/database/pricegap_state.go. Uses LIndex on pg:breaker:trips key + json.Unmarshal. Returns (record, false, nil) on redis.Nil (idx out of range — empty list). Mirrors LoadPriceGapPosition shape.

    Step C — Create internal/pricegaptrader/breaker_test_fire.go with method signature on *BreakerController:

    TestFire(ctx context.Context, dryRun bool) (models.BreakerTripRecord, error)

    Algorithm:
    1. Call bc.aggregator.Realized24h(ctx, time.Now()) to get pnl. On error, return zero record + error.
    2. Snapshot ramp stage (best-effort; default 0 if bc.ramp nil).
    3. Build BreakerTripRecord with Source="test_fire_dry_run" if dryRun else "test_fire", current TripTs, pnl, threshold from cfg, rampStage.
    4. If dryRun==true: log INFO "TEST-FIRE DRY RUN — would trip with 24h=X, threshold=Y, no mutations performed"; return (record, nil) — NO writes, NO Telegram, NO WS.
    5. If dryRun==false: load current state via bc.store.LoadBreakerState(); call bc.trip(ctx, state, pnl, "test_fire"). Return (record, trip-error-or-nil).

    Step D — Create internal/pricegaptrader/breaker_recovery.go with method signature on *BreakerController:

    Recover(ctx context.Context, operator string) error

    Algorithm — 5-step inverse of trip. Reverse safety property: clear sticky LAST (recovery transitions FROM safe-paper TO live-eligible; partial failure earlier means breaker stays tripped, which is the safer state).
    1. Load current state. If PaperModeStickyUntil==0, return error "breaker not tripped (sticky=0)" — cannot recover.
    2. STEP 1: bc.registry.ClearAllPausedByBreaker() (operator-set Disabled untouched, D-11). Log + continue on error (best-effort).
    3. STEP 2: bc.store.UpdateBreakerTripRecovery(0, recoveryTs=now, operator) — LSets idx 0 (most-recent trip). Log + continue on error.
    4. Best-effort load most-recent trip via bc.store.LoadBreakerTripAt(0) for alert payload.
    5. STEP 3 (sticky-clear): SaveBreakerState with PaperModeStickyUntil=0, PendingStrike=0, Strike1Ts=0. Critical — return error if SaveBreakerState fails.
    6. STEP 4: bc.notifier.NotifyPriceGapBreakerRecovery(lastTrip, operator). Log + continue on error.
    7. STEP 5: bc.ws.Broadcast("pg.breaker.recover", {operator, recovery_ts_ms, trip}) if ws wired.
    8. Log INFO "RECOVERED — sticky cleared, candidates re-enabled, operator=X".

    Step E — Update BreakerNotifier interface in breaker_controller.go (Plan 15-03 had only NotifyPriceGapBreakerTrip) to also include NotifyPriceGapBreakerRecovery. Update mocks in test files. *notify.TelegramNotifier already satisfies via Step A above.

    Step F — Replace stub tests with the 7 named tests above. Use a fake BreakerStateStore (in-memory map for state and list for trips with Save/Load/Append/UpdateBreakerTripRecovery/LoadBreakerTripAt mock impls), fake notifier (records all calls), fake ws (records broadcasts), fake registry (records pause/clear calls and tracks Disabled+PausedByBreaker per candidate), fake positions lister (returns preset list).
  </action>
  <verify>
    <automated>go test ./internal/notify -run "TestNotifyPriceGapBreaker(Trip|Recovery)_CriticalBucket" -count=1 -v && go test ./internal/pricegaptrader -run "TestSyntheticFireFullCycle|TestSyntheticFireDryRun_NoMutations|TestRecover_PreservesOperatorDisabled|TestBreaker_RecoveryReenablesCandidatesAndClearsSticky|TestRecover_NotTrippedReturnsError" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "func (t \*TelegramNotifier) NotifyPriceGapBreakerTrip" internal/notify/pricegap_breaker.go` succeeds
    - `grep -q "func (t \*TelegramNotifier) NotifyPriceGapBreakerRecovery" internal/notify/pricegap_breaker.go` succeeds
    - `grep -q "sendCritical" internal/notify/pricegap_breaker.go` succeeds (allowlist bypass — Phase 14 precedent)
    - `grep -q "Asia/Taipei" internal/notify/pricegap_breaker.go` succeeds (timezone display)
    - `grep -q "pg-admin breaker recover --confirm" internal/notify/pricegap_breaker.go` succeeds (recovery instruction)
    - `grep -q "func (bc \*BreakerController) TestFire" internal/pricegaptrader/breaker_test_fire.go` succeeds
    - `grep -q "test_fire_dry_run" internal/pricegaptrader/breaker_test_fire.go` succeeds
    - `grep -q "no mutations performed" internal/pricegaptrader/breaker_test_fire.go` succeeds (dry-run log line)
    - `grep -q "func (bc \*BreakerController) Recover" internal/pricegaptrader/breaker_recovery.go` succeeds
    - `grep -q "ClearAllPausedByBreaker" internal/pricegaptrader/breaker_recovery.go` succeeds
    - `grep -q "UpdateBreakerTripRecovery" internal/pricegaptrader/breaker_recovery.go` succeeds
    - `grep -q "breaker not tripped" internal/pricegaptrader/breaker_recovery.go` succeeds (recover-when-not-tripped error)
    - `grep -q "func (c \*Client) LoadBreakerTripAt" internal/database/pricegap_state.go` succeeds
    - `go test ./internal/notify -run "TestNotifyPriceGapBreaker" -count=1` exits 0
    - `go test ./internal/pricegaptrader -run "TestSyntheticFireFullCycle" -count=1` exits 0 (success-criterion #5 evidence)
    - `go test ./internal/pricegaptrader -run "TestSyntheticFireDryRun_NoMutations" -count=1` exits 0
    - `go test ./internal/pricegaptrader -run "TestBreaker_RecoveryReenablesCandidatesAndClearsSticky" -count=1` exits 0
    - `! grep -q "skip all mutations.*write" internal/pricegaptrader/breaker_test_fire.go` — dry-run does NOT write
  </acceptance_criteria>
  <done>Telegram critical-bucket trip+recovery dispatch, TestFire (real + dry-run with mutation skip), Recover (5-step inverse with operator-Disabled preservation), success-criterion #5 integration test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pg-admin breaker subcommands + REST API endpoints + auth + typed-phrase validation</name>
  <files>cmd/pg-admin/breaker.go, cmd/pg-admin/main.go, cmd/pg-admin/breaker_test.go, internal/api/pricegap_breaker_handlers.go, internal/api/pricegap_breaker_handlers_test.go, internal/api/server.go</files>
  <read_first>
    - cmd/pg-admin/main.go (full file — locate switch on os.Args[1]; existing `ramp`, `reconcile` subcommands as model)
    - cmd/pg-admin/ramp.go (Phase 14 P04 — typed-phrase prompt + Redis access pattern)
    - internal/api/server.go (locate authMiddleware + existing pg endpoints; identify how Server holds *BreakerController via setter)
    - internal/api/pricegap_handler.go (or pricegap_ramp_handler.go — Phase 14 P05 — request body parsing + JSON envelope)
    - .planning/phases/15-drawdown-circuit-breaker/15-CONTEXT.md (D-09 dual-surface, D-12 typed-phrase, D-13 test-fire dual-surface, D-14 dry-run flag)
    - .planning/phases/15-drawdown-circuit-breaker/15-03-SUMMARY.md (BreakerController.Snapshot signature)
  </read_first>
  <behavior>
    pg-admin tests:
    - Test 1 — TestPgAdminBreakerRecover_TypedPhraseRequired: stdin sends "wrong" → command exits with non-zero status + prints "expected 'RECOVER'". stdin sends "RECOVER" → calls BreakerController.Recover, exits 0 with "Recovered" output.
    - Test 2 — TestPgAdminBreakerTestFire_TypedPhraseRequired: stdin "wrong" → exit non-zero. stdin "TEST-FIRE" → calls TestFire(ctx, false) by default, exits 0. With --dry-run flag, calls TestFire(ctx, true).
    - Test 3 — TestPgAdminBreakerShow_PrintsState: no typed-phrase needed (read-only). Loads BreakerState via store and prints: pending_strike, strike1_ts (Asia/Taipei), last_eval_ts, last_eval_pnl_usdt, paper_mode_sticky_until human-formatted (Armed | Tripped | Disabled). Also prints last 10 entries from pg:breaker:trips.

    REST API tests:
    - Test 4 — TestPgBreakerHandlers_AuthRequired: POST /api/pg/breaker/recover without bearer → 401. With bad token → 401. With valid token + valid body → 200.
    - Test 5 — TestPgBreakerHandlers_TypedPhraseRequired: POST /recover with body {"confirmation_phrase": "recover"} (lowercase) → 400 with error containing "must equal 'RECOVER'". {"confirmation_phrase": "RECOVER"} → 200 + Recover called.
    - Test 6 — TestPgBreakerHandlers_RecoverEndpoint: POST /recover with valid auth + RECOVER phrase → 200 with envelope {ok:true, data:{recovery_ts_ms, operator}}. Operator name extracted from auth token claims (or "operator" default if anonymous bearer).
    - Test 7 — TestPgBreakerHandlers_TestFireEndpoint: POST /test-fire with auth + TEST-FIRE phrase + body {"dry_run": false} → 200, calls TestFire(ctx, false). With {"dry_run": true} → 200, calls TestFire(ctx, true).
    - Test 8 — TestPgBreakerHandlers_StateGetEndpoint: GET /state with auth → 200 with envelope {ok:true, data:{enabled, pending_strike, sticky_until_ms, last_eval_pnl_usdt, last_eval_ts_ms, threshold_usdt, armed, tripped, last_trip}} where last_trip is BreakerTripRecord at index 0 (or null if list empty).
    - Test 9 — TestPgBreakerHandlers_TestFireWhileTripped: GET /state shows tripped=true; POST /test-fire while tripped → 409 Conflict with error "breaker already tripped — recover first" (server-side guard prevents nesting trips).
    - Test 10 — TestPgBreakerHandlers_RecoverWhenNotTripped: state has sticky=0; POST /recover → 409 Conflict "breaker not tripped".
  </behavior>
  <action>
    Step A — Create cmd/pg-admin/breaker.go:

    breakerCmd(args []string) dispatches to recoverCmd / testFireCmd / showCmd based on args[0].

    recoverCmd:
    1. Parse --confirm flag (required; if missing, print usage and exit 1).
    2. Print: "Type 'RECOVER' to confirm operator-initiated recovery: "
    3. Read stdin line. If != "RECOVER", print "Aborted — phrase did not match." and exit 1.
    4. Load *database.Client + construct *BreakerController + dependencies (operator name from $USER env var or "pg-admin-cli" default).
    5. Call breaker.Recover(ctx, operator). On err: print + exit 1. On success: print "Recovered — sticky flag cleared, candidates re-enabled." and exit 0.

    testFireCmd:
    1. Parse --confirm flag (required) + optional --dry-run flag.
    2. Print "Type 'TEST-FIRE' to confirm synthetic breaker test-fire: " (if not --dry-run, also print: "WARNING: default behavior is REAL TRIP. Use --dry-run for simulation.")
    3. Read stdin. If != "TEST-FIRE", print "Aborted." and exit 1.
    4. Load deps. Call breaker.TestFire(ctx, dryRun).
    5. Print result summary (would-trip PnL, dry-run flag, trip ts if real). Exit 0.

    showCmd:
    1. Load *database.Client. Read pg:breaker:state HASH via LoadBreakerState. Format Armed/Tripped/Disabled status.
    2. Read last 10 from pg:breaker:trips via LRANGE 0 9 + json.Unmarshal each.
    3. Print formatted table with Asia/Taipei timestamps. Exit 0.

    Step B — Edit cmd/pg-admin/main.go: add `case "breaker": breakerCmd(os.Args[2:])` to the switch. Update usage() to list breaker {recover|test-fire|show}.

    Step C — Create internal/api/pricegap_breaker_handlers.go on *Server:

    handlePgBreakerStateGet(w, r):
    1. Load state via s.pgBreaker.Snapshot().
    2. Load most-recent trip via store LoadBreakerTripAt(0).
    3. Build response data {enabled, pending_strike, sticky_until_ms, last_eval_pnl_usdt, last_eval_ts_ms, threshold_usdt, armed, tripped, last_trip}.
       - armed = enabled && sticky_until_ms == 0
       - tripped = sticky_until_ms != 0 (and now < sticky_until_ms; MaxInt64 always tripped)
    4. writeJSON(w, 200, Response{ok: true, data}).

    handlePgBreakerRecover(w, r):
    1. Parse body {confirmation_phrase string}. If != "RECOVER" → 400 with error "confirmation_phrase must equal 'RECOVER'".
    2. Check current state: if sticky=0 → 409 with error "breaker not tripped".
    3. Extract operator from request context (bearer token claims; fallback to "operator" if anonymous).
    4. Call s.pgBreaker.Recover(ctx, operator). On err → 500. On success → 200 with {recovery_ts_ms, operator}.

    handlePgBreakerTestFire(w, r):
    1. Parse body {confirmation_phrase string, dry_run bool}. If phrase != "TEST-FIRE" → 400.
    2. Check state: if tripped → 409 "breaker already tripped — recover first".
    3. Call s.pgBreaker.TestFire(ctx, body.dry_run). On err → 500. On success → 200 with {trip_pnl_usdt, threshold, source, dry_run}.

    Step D — Edit internal/api/server.go:
    1. Add field `pgBreaker BreakerControllerAPI` on *Server (interface declared inline with the methods Server needs: Snapshot, Recover, TestFire). Phase 12 D-15 narrow-interface precedent.
    2. Add `SetPgBreaker(b BreakerControllerAPI)` setter.
    3. Register the 3 handlers in the existing pg routes block (POST /api/pg/breaker/recover, POST /api/pg/breaker/test-fire, GET /api/pg/breaker/state) — all under authMiddleware.

    Step E — Replace stub tests in cmd/pg-admin/breaker_test.go and internal/api/pricegap_breaker_handlers_test.go with the 10 tests in <behavior>. For pg-admin tests, use exec.Command and pipe to stdin; for API tests, use httptest.NewRecorder + the existing auth-middleware test helper (Phase 14 precedent).
  </action>
  <verify>
    <automated>go test ./cmd/pg-admin -run "TestPgAdminBreaker" -count=1 -v && go test ./internal/api -run "TestPgBreakerHandlers" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "func breakerCmd" cmd/pg-admin/breaker.go` succeeds
    - `grep -q "RECOVER" cmd/pg-admin/breaker.go` succeeds (literal phrase in CLI)
    - `grep -q "TEST-FIRE" cmd/pg-admin/breaker.go` succeeds
    - `grep -q "WARNING: default behavior is REAL TRIP" cmd/pg-admin/breaker.go` succeeds (Pitfall 7 mitigation)
    - `grep -q "case \"breaker\":" cmd/pg-admin/main.go` succeeds (subcommand registered)
    - `grep -q "func (s \*Server) handlePgBreakerRecover" internal/api/pricegap_breaker_handlers.go` succeeds
    - `grep -q "func (s \*Server) handlePgBreakerTestFire" internal/api/pricegap_breaker_handlers.go` succeeds
    - `grep -q "func (s \*Server) handlePgBreakerStateGet" internal/api/pricegap_breaker_handlers.go` succeeds
    - `grep -q "confirmation_phrase" internal/api/pricegap_breaker_handlers.go` succeeds (typed-phrase body field)
    - `grep -q "must equal 'RECOVER'" internal/api/pricegap_breaker_handlers.go` succeeds (clear error message)
    - `grep -q "must equal 'TEST-FIRE'" internal/api/pricegap_breaker_handlers.go` succeeds
    - `grep -q "/api/pg/breaker/recover" internal/api/server.go` succeeds (route registered)
    - `grep -q "/api/pg/breaker/test-fire" internal/api/server.go` succeeds
    - `grep -q "/api/pg/breaker/state" internal/api/server.go` succeeds
    - `grep -q "authMiddleware" internal/api/server.go | grep "breaker"` succeeds (auth-gated)
    - `go test ./cmd/pg-admin -run "TestPgAdminBreaker" -count=1` exits 0
    - `go test ./internal/api -run "TestPgBreakerHandlers" -count=1` exits 0
    - `go test ./internal/api -run "TestPgBreakerHandlers_AuthRequired" -count=1` exits 0 (security gate)
    - `go test ./internal/api -run "TestPgBreakerHandlers_TypedPhraseRequired" -count=1` exits 0 (D-12)
  </acceptance_criteria>
  <done>3 pg-admin subcommands with typed-phrase prompts, 3 REST endpoints with auth + body validation, 10 tests covering authentication and phrase enforcement.</done>
</task>

<task type="auto">
  <name>Task 3: cmd/main.go bootstrap wiring + VERSION + CHANGELOG</name>
  <files>cmd/main.go, VERSION, CHANGELOG.md</files>
  <read_first>
    - cmd/main.go (locate Phase 14 wiring block — NewReconciler / NewRampController / NewSizer + tracker.Set* setters)
    - VERSION (current version string)
    - CHANGELOG.md (top entry format from Phase 14 v0.37.0 — copy structure)
    - .planning/phases/15-drawdown-circuit-breaker/15-03-SUMMARY.md (BreakerController constructor signature, setter list)
  </read_first>
  <action>
    Step A — Edit cmd/main.go. Locate the Phase 14 wiring block (after `tracker.SetSizer(sizer)` line). Append the Phase 15 wiring:

    ```
    // ---- Phase 15: Drawdown Circuit Breaker (PG-LIVE-02) ----
    // Default OFF via cfg.PriceGapBreakerEnabled; daemon spawned conditionally inside tracker.Start.
    aggregator := pricegaptrader.NewRealizedPnLAggregator(db)
    tracker.SetBreakerStore(db) // narrow BreakerStateLoader interface; *database.Client implements via duck typing
    breaker := pricegaptrader.NewBreakerController(cfg, db, aggregator, telegramNotifier, log)
    breaker.SetWSBroadcaster(server.Hub()) // Plan 12-03 D-15 precedent — public Hub() accessor preserves module boundary
    breaker.SetRamp(ramp)
    breaker.SetRegistry(registry)
    breaker.SetPositions(db) // *database.Client implements ActivePositionLister
    tracker.SetBreakerController(breaker)
    server.SetPgBreaker(breaker) // expose Snapshot/Recover/TestFire to the API handlers
    ```

    Verify:
    - `aggregator` constructed BEFORE breaker (breaker takes aggregator as constructor arg).
    - `server.Hub()` is wired as WS broadcaster — same pattern as Phase 12 promote events.
    - `server.SetPgBreaker(breaker)` ties into the BreakerControllerAPI narrow interface added in Task 2.
    - All setter calls precede `tracker.Start(ctx)` so the daemon sees full wiring.

    Step B — Bump VERSION:
    Read current value. If it is v0.37.X (Phase 14 ship), bump to v0.38.0 (new feature, default OFF, additive). Single-line file replacement.

    Step C — Append to CHANGELOG.md a new top entry mirroring Phase 14 ship structure:

    ```
    ## v0.38.0 — 2026-05-XX

    ### Phase 15 — Drawdown Circuit Breaker (PG-LIVE-02)

    **New feature, default OFF.** Strategy 4 gains a realized-PnL drawdown breaker that auto-flips the engine to paper mode when rolling 24h realized PnL drops below `pricegap_drawdown_limit_usdt`. Bybit `:04-:05:30` blackout suppresses evaluation. Two-strike rule (≥5 min apart). Recovery requires explicit operator action via `pg-admin breaker recover --confirm` or dashboard Recover button.

    Added:
    - 3 new config fields: `enable_pricegap_breaker` (bool, default false), `pricegap_drawdown_limit_usdt` (float, default 0, validated ≤ 0), `pricegap_breaker_interval_sec` (int, default 300, validated [60, 3600])
    - New Redis keys: `pg:breaker:state` (HASH, 5 fields), `pg:breaker:trips` (capped LIST 500 entries)
    - 3 new pg-admin subcommands: `breaker recover --confirm`, `breaker test-fire [--dry-run] --confirm`, `breaker show`
    - 3 new REST endpoints (auth-gated, typed-phrase confirmation): POST /api/pg/breaker/recover, POST /api/pg/breaker/test-fire, GET /api/pg/breaker/state
    - 2 new Telegram critical-bucket alerts: NotifyPriceGapBreakerTrip, NotifyPriceGapBreakerRecovery
    - Dashboard Pricegap-tracker tab: new Breaker subsection (status badge + Recover button + Test-fire button) extends Phase 14 RampReconcileSection
    - Internal: single Tracker.IsPaperModeActive(ctx) chokepoint replaces 4 direct cfg.PriceGapPaperMode reads in execution.go (sticky flag becomes uncircumventable)
    - Static regression test prevents future direct paper-mode reads

    Changed:
    - internal/pricegaptrader/registry.go Candidate gains `paused_by_breaker bool` (distinct from operator-set Disabled); entry path rejects on `disabled || paused_by_breaker`

    Migration: existing operators set `enable_pricegap_breaker: true` + `pricegap_drawdown_limit_usdt: <negative threshold>` via dashboard config or API — config.json reload not required for runtime toggle.

    Tests: 30+ new unit + integration tests; full synthetic test-fire cycle covered by TestSyntheticFireFullCycle (success-criterion #5).
    ```

    Step D — Final compile + smoke check: `go build ./...` + run the breaker test-fire subcommand wired through `./arb` to confirm the binary actually serves the new endpoints (no actual breaker config flip). This is a sanity check, NOT a UAT — the dashboard human-verify lives in Plan 15-05.
  </action>
  <verify>
    <automated>go build ./... && grep -q "SetBreakerController" cmd/main.go && grep -q "SetPgBreaker" cmd/main.go && grep -q "v0.38.0" CHANGELOG.md && grep -q "v0.38.0" VERSION && go test ./internal/pricegaptrader/... ./internal/api/... ./internal/notify/... ./internal/database/... ./internal/config/... ./cmd/pg-admin/... -run "Breaker|TestPgBreaker|TestPgAdminBreaker|TestNotifyPriceGapBreaker|TestSyntheticFire|TestRecover|TestRegistry_Paused|TestStaticCheck_NoDirectPaperMode|TestAggregator|TestValidatePriceGapLive_BreakerFields|TestPriceGapState_(SaveLoadBreaker|LoadBreakerStateMissing|BreakerTripsCap|UpdateBreakerTripRecovery|StickyMaxInt64Roundtrip)" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "NewRealizedPnLAggregator" cmd/main.go` succeeds
    - `grep -q "NewBreakerController" cmd/main.go` succeeds
    - `grep -q "SetBreakerController" cmd/main.go` succeeds
    - `grep -q "SetBreakerStore" cmd/main.go` succeeds
    - `grep -q "SetPgBreaker" cmd/main.go` succeeds (server-side wiring)
    - `grep -q "server.Hub()" cmd/main.go` succeeds (WS broadcaster wiring per Phase 12 D-15 precedent)
    - VERSION file content matches `v0.38.0` exactly: `grep -Fxq "v0.38.0" VERSION`
    - `grep -q "v0.38.0 — " CHANGELOG.md` succeeds (top entry exists)
    - `grep -q "PG-LIVE-02" CHANGELOG.md` (requirement ID referenced)
    - `grep -q "Drawdown Circuit Breaker" CHANGELOG.md` succeeds
    - `go build ./...` exits 0
    - Full Phase 15 test suite green: `go test ./internal/pricegaptrader/... ./internal/api/... ./internal/notify/... ./internal/database/... ./internal/config/... ./cmd/pg-admin/... -run "Breaker|TestPgBreaker|TestPgAdminBreaker|TestNotifyPriceGapBreaker|TestSyntheticFire|TestRecover|TestRegistry_Paused|TestStaticCheck_NoDirectPaperMode|TestAggregator|TestValidatePriceGapLive_BreakerFields" -count=1` exits 0
    - Existing Strategy 4 tests untouched: `go test ./internal/pricegaptrader -count=1` exits 0 (no regression in Phase 8/9/10/11/12/14 tests)
    - perp-perp + spot-futures tests untouched (live trading safety): `go test ./internal/engine/... ./internal/spotengine/... -count=1` exits 0
    - `git diff config.json` empty (config.json untouched)
  </acceptance_criteria>
  <done>cmd/main.go wires aggregator + breaker controller + server pgBreaker; daemon spawns conditionally; VERSION bumped to v0.38.0; CHANGELOG.md documents Phase 15 ship; full test suite green; no regression in adjacent engines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/curl → POST /api/pg/breaker/recover | unauthenticated or replayed call could clear sticky flag |
| Browser/curl → POST /api/pg/breaker/test-fire | unauthenticated call could trip live engine |
| pg-admin CLI on operator workstation | shell-history replay could re-fire old commands |
| Telegram bot token | already-existing trust; no new tokens introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | E (Elevation) | Unauthenticated /api/pg/breaker/recover call | mitigate | All 3 endpoints behind authMiddleware (existing bearer-token middleware). Test `TestPgBreakerHandlers_AuthRequired` enforces 401 on missing token. |
| T-15-13 | T (Tampering) | Replayed POST /recover from shell history | mitigate | Typed-phrase body field `confirmation_phrase` MUST equal "RECOVER" — server checks exact-match (case-sensitive). Test `TestPgBreakerHandlers_TypedPhraseRequired` rejects `recover` (lowercase). |
| T-15-14 | T (Tampering) | Operator types `test-fire` lowercase by accident | accept | Typed-phrase requirement is intentionally case-sensitive — exact-match enforces deliberate operator intent (CONTEXT D-12 + Specifics). |
| T-15-15 | I (Information disclosure) | GET /api/pg/breaker/state leaks operator name | accept | Operator name only present on past trip records (already in pg:breaker:trips Redis log; same trust boundary as Phase 14 pg:ramp:events). |
| T-15-16 | D (DoS) | Repeated test-fire requests trip / recover loop | mitigate | Server-side guard: if tripped, /test-fire returns 409 (test 9). If not tripped, /recover returns 409 (test 10). Bounded recursion. |
| T-15-17 | E (Elevation) | Operator with read-only token recovers breaker | accept | Existing auth middleware does not differentiate role; bearer token grants full pg-admin equivalence. Future: role-based scopes are a v3.0 concern (CONTEXT Deferred). Logged via `recovery_operator` field in trip log for audit. |
</threat_model>

<verification>
1. `go build ./...` exits 0.
2. Full Phase 15 test suite (Plan 01 + 02 + 03 + 04 tests combined) passes.
3. Existing Strategy 4 tests + adjacent-engine tests unchanged.
4. config.json untouched (`git diff config.json` empty).
5. VERSION reads v0.38.0; CHANGELOG.md top entry references Phase 15 + PG-LIVE-02.
6. Synthetic test-fire integration test (success-criterion #5) green.
</verification>

<success_criteria>
- 3 pg-admin breaker subcommands with typed-phrase prompts (RECOVER + TEST-FIRE)
- 3 REST endpoints (POST recover, POST test-fire, GET state) with auth + typed-phrase enforcement
- 2 Telegram critical-bucket methods (trip + recovery) using sendCritical bypass
- BreakerController.TestFire(ctx, dryRun) — real trip default, dry-run skips ALL mutations
- BreakerController.Recover(ctx, operator) — 5-step inverse with operator-Disabled preservation
- TestSyntheticFireFullCycle integration test green (success-criterion #5)
- cmd/main.go fully wires aggregator + breaker + setters; daemon spawns conditionally
- VERSION → v0.38.0; CHANGELOG.md documents Phase 15 ship
- Adjacent engine tests untouched
</success_criteria>

<output>
After completion, create `.planning/phases/15-drawdown-circuit-breaker/15-04-SUMMARY.md` documenting: (a) Telegram critical-bucket method signatures + payload format, (b) TestFire + Recover algorithms with their respective ordering rationales, (c) pg-admin subcommand reference + typed-phrase flow, (d) REST endpoint contracts (request body + response envelope), (e) cmd/main.go wiring map (constructor → setter → daemon spawn), (f) version + changelog snippets. Cross-reference Plan 15-05 for the dashboard widget that consumes these endpoints.
</output>
