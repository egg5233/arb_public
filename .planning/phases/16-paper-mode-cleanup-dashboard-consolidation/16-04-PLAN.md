---
phase: 16
plan: 04
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - web/src/pages/PriceGap.tsx
  - web/src/pages/Config.tsx
  - web/src/components/PriceGap/ConfigCard.tsx
  - web/src/components/Ramp/BreakerConfirmModal.tsx
  - web/src/i18n/en.ts
  - web/src/i18n/zh-TW.ts
  - web/src/i18n/lockstep.test.ts
  - web/src/components/PriceGap/ConfigCard.test.tsx
  - .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-04-HUMAN-UAT.md
  - VERSION
  - CHANGELOG.md
autonomous: false
requirements: [PG-OPS-09]
must_haves:
  truths:
    - "A new 'Strategy 4 Configuration' card renders at the TOP of PriceGap.tsx, above existing sections"
    - "Card composes 4 subsections: Scanner, Breaker, Ramp (read-only), Live-Capital + Risk"
    - "Scanner subsection exposes 5 fields: PriceGapDiscoveryEnabled, PriceGapScanIntervalSec, PriceGapMaxUniverse, PriceGapAutoPromoteScore, PriceGapMaxCandidates"
    - "Breaker subsection exposes 3 fields: PriceGapBreakerEnabled (typed-phrase ENABLE-BREAKER), PriceGapDrawdownLimitUSDT, PriceGapBreakerIntervalSec"
    - "Live-Capital subsection exposes PriceGapLiveCapital (typed-phrase ENABLE-LIVE-CAPITAL) + PriceGapAnomalySlippageBps + 4 migrated gate fields"
    - "Ramp subsection is read-only (display only — edits go through pg-admin per D-17)"
    - "4 legacy controls deleted from Config.tsx and migrated to the new card (price_gap_free_bps, max_price_gap_bps, enable_price_gap_gate, max_price_gap_pct)"
    - "BreakerConfirmModal Phrase union extended to include 'ENABLE-LIVE-CAPITAL' and 'ENABLE-BREAKER'"
    - "Every new label, placeholder, modal text, validation message, error string exists in BOTH en.ts AND zh-TW.ts (i18n lockstep)"
    - "Magic strings ENABLE-LIVE-CAPITAL and ENABLE-BREAKER are NOT translated, NOT lowercased, NOT trimmed"
    - "Phase 14 RampReconcileSection and Phase 15 BreakerSubsection widgets remain in their current locations (D-19)"
    - "No new npm packages — package.json + package-lock.json untouched"
    - "Frontend build succeeds via `cd web && npm run build` (no go:embed regression)"
  artifacts:
    - path: "web/src/components/PriceGap/ConfigCard.tsx"
      provides: "Top-level collapsible Configuration card composing 4 subsections"
      contains: "ConfigCard"
    - path: "web/src/i18n/en.ts"
      provides: "All new pricegap.config.* keys per UI-SPEC"
      contains: "pricegap.config.cardTitle"
    - path: "web/src/i18n/zh-TW.ts"
      provides: "Lockstep zh-TW translations for every new EN key"
      contains: "pricegap.config.cardTitle"
    - path: "web/src/i18n/lockstep.test.ts"
      provides: "Vitest assertion that keyof en === keyof zhTW (no missing translations)"
      contains: "lockstep"
  key_links:
    - from: "web/src/pages/PriceGap.tsx top of layout"
      to: "<ConfigCard />"
      via: "import + render before Discovery/Candidates/Positions sections"
      pattern: "ConfigCard"
    - from: "ConfigCard ENABLE-LIVE-CAPITAL toggle"
      to: "BreakerConfirmModal phrase=\"ENABLE-LIVE-CAPITAL\""
      via: "modal opens on click, requires literal phrase, posts via postConfig with operator_action: true"
      pattern: "ENABLE-LIVE-CAPITAL"
    - from: "ConfigCard ENABLE-BREAKER toggle"
      to: "BreakerConfirmModal phrase=\"ENABLE-BREAKER\""
      via: "modal opens on click, requires literal phrase, posts via postConfig"
      pattern: "ENABLE-BREAKER"
    - from: "Config.tsx legacy controls (4 of them)"
      to: "DELETED — moved to ConfigCard subsections"
      via: "full cutover migration per D-16"
      pattern: "(price_gap_free_bps|max_price_gap_bps|enable_price_gap_gate|max_price_gap_pct)"
---

<objective>
PG-OPS-09: Consolidate ALL Strategy 4 configuration into a new collapsible "Strategy 4 Configuration" card at the top of `web/src/pages/PriceGap.tsx`. The card has 4 subsections (Scanner, Breaker, Ramp, Live-Capital + Risk) per UI-SPEC. New config sections expose previously config.json-only fields. 4 legacy controls in `Config.tsx` are MIGRATED (full cutover) into the new card and deleted from Config.tsx. Typed-phrase confirm modals (ENABLE-LIVE-CAPITAL, ENABLE-BREAKER) extend the Phase 15 `BreakerConfirmModal` Phrase union. EN + zh-TW i18n keys land in lockstep.

Purpose: Operator one-stop for Strategy 4. Before this plan, controls are scattered across Config.tsx tab + PriceGap.tsx tab + pg-admin CLI; after, all editable Strategy 4 config is in PriceGap.tsx. Phase 14 ramp-reconcile widget + Phase 15 breaker subsection (read-mostly status displays) stay where they are per D-19.

Output: New ConfigCard component + 4 subsections, BreakerConfirmModal extended, ~60 i18n keys added in BOTH locales, Config.tsx legacy controls deleted, Vitest lockstep test, frontend build green, operator UAT (both locales) attached, VERSION + CHANGELOG bump.

Wave 2 because: this plan modifies `web/src/pages/PriceGap.tsx` which Plan 02 also touches (togglePaper). Plan 02 must merge first; this plan rebases on the operator_action wire-up.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-CONTEXT.md
@.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-RESEARCH.md
@.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-VALIDATION.md
@.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-UI-SPEC.md
@web/src/pages/PriceGap.tsx
@web/src/pages/Config.tsx
@web/src/components/Ramp/BreakerConfirmModal.tsx
@web/src/components/Ramp/BreakerSubsection.tsx
@web/src/components/Ramp/RampReconcileSection.tsx
@web/src/i18n/en.ts
@web/src/i18n/zh-TW.ts
@internal/config/config.go

<interfaces>
<!-- Key contracts from existing code (read-first) -->

From web/src/components/Ramp/BreakerConfirmModal.tsx:24 (Phase 15 typed-phrase precedent — extend the Phrase union):
```typescript
// CURRENT
type Phrase = 'RECOVER' | 'TEST-FIRE';
// AFTER (this plan)
type Phrase = 'RECOVER' | 'TEST-FIRE' | 'ENABLE-LIVE-CAPITAL' | 'ENABLE-BREAKER';
```

From web/src/pages/PriceGap.tsx:340 (postConfig helper signature — reuse as-is):
```typescript
const postConfig = useCallback(async (body: Record<string, unknown>) => {
    // POSTs to /api/config with bearer token, returns parsed envelope
}, [...]);
```

From web/src/i18n/en.ts:973 (TranslationKey type — derives from English locale):
```typescript
export type TranslationKey = keyof typeof en;
// Compile catches code that references a key zh-TW lacks IF en has it.
// Lockstep direction (zh-TW key not in en) requires the new test in this plan.
```

From internal/config/config.go (PriceGap* fields — UI-SPEC D-17 fields ALL exist with validators per RESEARCH):
```go
PriceGapDiscoveryEnabled   bool
PriceGapScanIntervalSec    int
PriceGapMaxUniverse        int
PriceGapAutoPromoteScore   int
PriceGapMaxCandidates      int
PriceGapBreakerEnabled     bool
PriceGapDrawdownLimitUSDT  float64
PriceGapBreakerIntervalSec int
PriceGapLiveCapital        bool
PriceGapAnomalySlippageBps float64
PriceGapRampStageSizesUSDT []float64
// All read-only ramp fields per D-19
```

Migrated legacy controls (DELETE from Config.tsx, MOVE TO ConfigCard):
- `Config.tsx:746,753`  → `strategy.discovery.price_gap_free_bps`, `strategy.discovery.max_price_gap_bps`
- `Config.tsx:1393`     → `spot_futures.enable_price_gap_gate`
- `Config.tsx:1454-1466` → `spot_futures.max_price_gap_pct`

Server-side reject (PG-FIX-02 D-06 — already shipped in Plan 02):
- /api/config POST returns 409 on paper_mode write without operator_action
- Frontend togglePaper already sends operator_action: true (Plan 02 Task 3)
- ENABLE-LIVE-CAPITAL toggle in this plan ALSO sends operator_action: true (because it ultimately drives paper_mode=false)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all i18n keys (EN + zh-TW lockstep) + extend BreakerConfirmModal Phrase union + Wave-0 lockstep test</name>
  <read_first>
    - web/src/i18n/en.ts (full file — to find correct insertion point near existing pricegap.* keys; current line count = 974)
    - web/src/i18n/zh-TW.ts (full file — same structure, lockstep mirror; current line count = 974)
    - web/src/components/Ramp/BreakerConfirmModal.tsx (full file — Phrase union at line 24 + interaction contract verbatim)
    - .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-UI-SPEC.md (full file — copywriting contract has every key)
  </read_first>
  <action>
    Step 1: In `web/src/i18n/en.ts`, locate the existing `pricegap.*` key block (existing keys like `pricegap.ramp.*`, `pricegap.breaker.*`, `pricegap.discovery.*`). Add a new `pricegap.config.*` block immediately AFTER the existing pricegap keys and BEFORE the next top-level prefix. Use the EXACT keys + EN copy from UI-SPEC §"Copywriting Contract":

    Required keys (verbatim from UI-SPEC):
    - Card heading (2): `pricegap.config.cardTitle`, `pricegap.config.cardSubtitle`
    - Subsection headings (4): `pricegap.config.section.scanner`, `.breaker`, `.ramp`, `.liveCapital`
    - Primary CTA (2): `pricegap.config.saveButton` (interpolated `{section}`), `pricegap.config.saving`
    - Empty/null states (3): `pricegap.config.collapsed`, `pricegap.config.ramp.readOnlyNote`, `pricegap.config.loading`
    - Validation/error states (5): `pricegap.config.validation.range`, `.validation.drawdownNegative`, `.error.saveNetwork`, `.error.operatorActionRequired`, `.error.serverValidation`
    - Confirm modal copy (12 — 4 actions × 3 keys each): `pricegap.config.confirmTitle.{enableLiveCapital,disableLiveCapital,enableBreaker,disableBreaker}`, `pricegap.config.confirmPrompt.{...}`, `pricegap.config.confirmSubmit.{...}`
    - Field labels + help (28 — see UI-SPEC table for all 14 fields × 2 keys each = label + help):
      - Scanner (5 fields × 2 = 10): `pricegap.config.scanner.{discovery,interval,maxUniverse,promoteScore,maxCandidates}.{label,help}`
      - Breaker (3 × 2 = 6): `pricegap.config.breaker.{enabled,limit,interval}.{label,help}`
      - Live-Capital (2 × 2 = 4): `pricegap.config.liveCapital.{master,anomalyBps}.{label,help}`
      - Migrated gate (4 × 2 = 8): `pricegap.config.gate.{freeBps,maxBps,spotFuturesEnabled,maxPct}.{label,help}`
    - Paper-mode help (2): `pricegap.config.paperMode.helpText`, `pricegap.config.paperMode.serverReject`, `pricegap.config.paperMode.confirmHint`

    Total: ≈60 new keys per locale. EN copy comes from UI-SPEC verbatim.

    Step 2: In `web/src/i18n/zh-TW.ts`, mirror EVERY key added in Step 1, in the same position, with the zh-TW copy from UI-SPEC verbatim. The two files MUST end up with the SAME key set (lockstep).

    Step 3: Extend `web/src/components/Ramp/BreakerConfirmModal.tsx`:
      - Find `type Phrase = 'RECOVER' | 'TEST-FIRE';` (line ~24).
      - Change to `type Phrase = 'RECOVER' | 'TEST-FIRE' | 'ENABLE-LIVE-CAPITAL' | 'ENABLE-BREAKER';`.
      - DO NOT change any interaction logic (case-sensitive match, no trim, Esc closes, click-outside closes, Enter does not submit, real-trip warning banner). UI-SPEC §"Modal interaction contract" requires these stay verbatim.
      - DO NOT translate the magic strings.

    Step 4: Create `web/src/i18n/lockstep.test.ts` (NEW file) — Vitest test asserting key parity:

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { en } from './en';
    import { zhTW } from './zh-TW';

    function flatKeys(obj: Record<string, unknown>, prefix = ''): string[] {
      const keys: string[] = [];
      for (const [k, v] of Object.entries(obj)) {
        const path = prefix ? `${prefix}.${k}` : k;
        if (v && typeof v === 'object' && !Array.isArray(v)) {
          keys.push(...flatKeys(v as Record<string, unknown>, path));
        } else {
          keys.push(path);
        }
      }
      return keys.sort();
    }

    describe('i18n lockstep (Phase 16 PG-OPS-09 D-20)', () => {
      it('en and zh-TW have identical key sets', () => {
        const enKeys = flatKeys(en as unknown as Record<string, unknown>);
        const zhKeys = flatKeys(zhTW as unknown as Record<string, unknown>);
        const enOnly = enKeys.filter(k => !zhKeys.includes(k));
        const zhOnly = zhKeys.filter(k => !enKeys.includes(k));
        expect(enOnly, `keys present in en but missing in zh-TW: ${enOnly.join(', ')}`).toEqual([]);
        expect(zhOnly, `keys present in zh-TW but missing in en: ${zhOnly.join(', ')}`).toEqual([]);
      });
    });
    ```

    Adapt the import names (`en`, `zhTW`) to match the actual exports in those files (read-first to confirm the exact named export names).

    Step 5: Run vitest lockstep test (will FAIL if any new key is missing in either locale; PASS if lockstep):
    ```bash
    cd web && npm run test:run -- lockstep
    ```
    If it fails, fix the missing keys then rerun.
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && grep -c 'pricegap.config.cardTitle\|pricegap\.config\.cardTitle' web/src/i18n/en.ts | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c 'pricegap.config.cardTitle\|pricegap\.config\.cardTitle' web/src/i18n/zh-TW.ts | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "ENABLE-LIVE-CAPITAL" web/src/components/Ramp/BreakerConfirmModal.tsx | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "ENABLE-BREAKER" web/src/components/Ramp/BreakerConfirmModal.tsx | awk '$1 >= 1 {exit 0} {exit 1}' && test -f web/src/i18n/lockstep.test.ts && cd web && npm run test:run -- lockstep 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pricegap.config' web/src/i18n/en.ts` returns ≥ 50 (every new key reference)
    - `grep -c 'pricegap.config' web/src/i18n/zh-TW.ts` returns ≥ 50 (lockstep)
    - `grep -c 'cardTitle' web/src/i18n/en.ts` returns ≥ 1 AND `grep -c 'cardTitle' web/src/i18n/zh-TW.ts` returns ≥ 1
    - `grep -c 'ENABLE-LIVE-CAPITAL' web/src/components/Ramp/BreakerConfirmModal.tsx` returns ≥ 1
    - `grep -c 'ENABLE-BREAKER' web/src/components/Ramp/BreakerConfirmModal.tsx` returns ≥ 1
    - `grep -E "type Phrase = .*ENABLE-LIVE-CAPITAL.*ENABLE-BREAKER" web/src/components/Ramp/BreakerConfirmModal.tsx` matches (Phrase union extended)
    - File `web/src/i18n/lockstep.test.ts` exists
    - `cd web && npm run test:run -- lockstep` exits 0 (lockstep verified)
    - Magic-string greps: NO occurrence of `enable-live-capital` (lowercase) or `Enable-Live-Capital` (mixed) anywhere in en.ts or zh-TW.ts (literal-only check via case-sensitive grep)
    - `git diff web/package.json web/package-lock.json` is empty (no new deps)
  </acceptance_criteria>
  <done>All ~60 i18n keys added in BOTH locales; BreakerConfirmModal Phrase union extended (interaction logic untouched); lockstep test green.</done>
</task>

<task type="auto">
  <name>Task 2: Build ConfigCard component + 4 subsections + integrate into PriceGap.tsx + delete Config.tsx legacy controls</name>
  <read_first>
    - web/src/pages/PriceGap.tsx (full file — esp. layout shape, postConfig usage, paperMode/togglePaper)
    - web/src/pages/Config.tsx (lines 720-780, 1380-1480 — exact location of 4 legacy controls to delete)
    - web/src/components/Ramp/BreakerConfirmModal.tsx (full file — modal API to consume)
    - web/src/components/Ramp/BreakerSubsection.tsx (full file — UI-SPEC visual precedent)
    - web/src/components/Ramp/RampReconcileSection.tsx (full file — read-mostly subsection precedent for the read-only Ramp subsection)
    - .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-UI-SPEC.md (Component Inventory + Composition shape sections)
    - web/src/i18n/en.ts (just-extended — to confirm key names match what the component will reference)
  </read_first>
  <action>
    Step 1: Create `web/src/components/PriceGap/ConfigCard.tsx` (NEW directory + file). Component is a default export `ConfigCard`. UI-SPEC contract:
      - Outer wrapper uses `card-surface` class + `p-4 mt-4 mb-6` per UI-SPEC §"Spacing Scale"
      - Collapsible header (default collapsed); persist expanded state to `localStorage` key `arb_pg_config_expanded`
      - Header has `aria-expanded` + `aria-controls` per UI-SPEC §"Accessibility"
      - Renders 4 subsections in this order: Scanner, Breaker, Ramp (read-only), Live-Capital + Risk
      - Each subsection separated by `mt-6 pt-4 border-t` per UI-SPEC
      - Per-subsection "Save {section}" button enabled only when isDirty (UI-SPEC §"Per-subsection save flow")
      - Saving state shows `text` from `pricegap.config.saving` key
      - On 200: success toast + reset to clean
      - On 409: error toast `pricegap.config.error.operatorActionRequired`
      - On 422: inline field error `pricegap.config.error.serverValidation`
      - On 500/network: error toast `pricegap.config.error.saveNetwork`

    Step 2: Implement the 4 subsections inline in ConfigCard.tsx (or as sibling files under `web/src/components/PriceGap/` — your call, but ALL files must compile and ConfigCard must export the composition):

      **ConfigSectionScanner** — 5 fields per UI-SPEC table:
      - PriceGapDiscoveryEnabled (toggle)
      - PriceGapScanIntervalSec (number, range 60-3600, type="number" min/max/step)
      - PriceGapMaxUniverse (number, range 1-50)
      - PriceGapAutoPromoteScore (number, range 0-100)
      - PriceGapMaxCandidates (number, range 1-50)
      Save button posts via `postConfig` (no typed-phrase needed — none of these flip system to less-safe state alone).

      **ConfigSectionBreaker** — 3 fields:
      - PriceGapBreakerEnabled (toggle, ENABLE-BREAKER typed-phrase modal on toggle ON; on toggle OFF, opens BreakerConfirmModal but with no phrase requirement and red destructive button per UI-SPEC §"Destructive confirmation copy")
      - PriceGapDrawdownLimitUSDT (number, range -1e6 to 0; inline error from `pricegap.config.validation.drawdownNegative` if positive)
      - PriceGapBreakerIntervalSec (number, range 60-3600)

      **ConfigSectionRamp** — read-only display per D-19 + UI-SPEC §"Component Inventory":
      - Render PriceGapRampStageSizesUSDT, hard ceiling, clean-day target as plain text rows
      - Show banner with text `pricegap.config.ramp.readOnlyNote` ("Ramp parameters are read-only here. Edit through pg-admin CLI.")
      - NO save button

      **ConfigSectionLiveCapital** — toggle + 5 fields:
      - PriceGapLiveCapital (toggle, ENABLE-LIVE-CAPITAL typed-phrase modal on toggle ON; on toggle OFF, modal with no phrase requirement)
      - PriceGapAnomalySlippageBps (number)
      - 4 migrated gate fields:
        - price_gap_free_bps (number, label `pricegap.config.gate.freeBps.label`)
        - max_price_gap_bps (number, label `pricegap.config.gate.maxBps.label`)
        - enable_price_gap_gate (toggle, label `pricegap.config.gate.spotFuturesEnabled.label`)
        - max_price_gap_pct (number, label `pricegap.config.gate.maxPct.label`)

      Critical: ENABLE-LIVE-CAPITAL toggle's POST body MUST include `operator_action: true` (defense-in-depth — even though this field is `price_gap_live_capital`, not `price_gap_paper_mode`, treating both as engine-wide-flag toggles consistently reduces operator-error surface). ENABLE-BREAKER POST body does NOT need operator_action (server guard from Plan 02 is paper_mode-scoped only) but the typed-phrase modal still enforces operator intent.

    Step 3: Modify `web/src/pages/PriceGap.tsx`:
      - Import: `import ConfigCard from '../components/PriceGap/ConfigCard';`
      - In the layout return, render `<ConfigCard />` as the FIRST child element of the page content (above the existing PaperModeToggle, Discovery, Candidates, Positions, History sections — see UI-SPEC §"Composition shape")
      - Do NOT remove or relocate any existing section
      - Phase 14 RampReconcileSection + Phase 15 BreakerSubsection stay where they are (D-19)

    Step 4: Delete the 4 legacy controls from `web/src/pages/Config.tsx`. Use grep to find exact line ranges first, then remove the markup + any local handler/state slot that becomes unused. Search keys:
      - `price_gap_free_bps` (line ~746)
      - `max_price_gap_bps` (line ~753)
      - `enable_price_gap_gate` (line ~1393)
      - `max_price_gap_pct` (line ~1454-1466)
      Verify with grep BEFORE deleting that no other component reads them via state lifting (Pitfall 7):
      ```bash
      grep -rn "price_gap_free_bps\|max_price_gap_bps\|enable_price_gap_gate\|max_price_gap_pct" web/src/ | grep -v PriceGap | grep -v Config.tsx
      ```
      If references exist outside Config.tsx and PriceGap subtree, STOP and rewire them (UI-SPEC + RESEARCH Pitfall 7 explicit).

    Step 5: Create `web/src/components/PriceGap/ConfigCard.test.tsx` — basic Vitest render test:
      - Renders without crashing
      - Card title text exists in DOM (`pricegap.config.cardTitle`)
      - Click on header toggles expand/collapse (asserts via aria-expanded change)
      - Each subsection heading visible when expanded
      - ENABLE-LIVE-CAPITAL toggle opens BreakerConfirmModal with phrase="ENABLE-LIVE-CAPITAL"
      - ENABLE-BREAKER toggle opens BreakerConfirmModal with phrase="ENABLE-BREAKER"
      Use existing test setup pattern from `web/src/pages/PriceGap.candidates.test.ts`.

    Step 6: Run frontend tests + build:
    ```bash
    cd web && npm run test:run -- ConfigCard
    cd web && npm run test:run -- lockstep
    cd web && npm run build  # MUST succeed — go:embed depends on this output
    ```

    Step 7: Confirm npm lockdown:
    ```bash
    git diff web/package.json web/package-lock.json
    ```
    Must be empty.
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && test -f web/src/components/PriceGap/ConfigCard.tsx && grep -c "ConfigCard" web/src/pages/PriceGap.tsx | awk '$1 >= 2 {exit 0} {exit 1}' && ! grep -E '(price_gap_free_bps|max_price_gap_bps|enable_price_gap_gate|max_price_gap_pct)' web/src/pages/Config.tsx && cd web && npm run test:run -- ConfigCard 2>&1 | tail -10 && npm run test:run -- lockstep 2>&1 | tail -5 && npm run build 2>&1 | tail -10 && cd .. && test -z "$(git diff web/package.json web/package-lock.json 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - File `web/src/components/PriceGap/ConfigCard.tsx` exists
    - File `web/src/components/PriceGap/ConfigCard.test.tsx` exists
    - `grep -c 'ConfigCard' web/src/pages/PriceGap.tsx` returns ≥ 2 (1 import + 1 usage)
    - `grep -c "from '../components/PriceGap/ConfigCard'" web/src/pages/PriceGap.tsx` returns ≥ 1
    - Config.tsx legacy delete: `grep -E '(price_gap_free_bps|max_price_gap_bps|enable_price_gap_gate|max_price_gap_pct)' web/src/pages/Config.tsx` returns NO matches
    - All 4 migrated keys appear in ConfigCard.tsx (or its sibling subsection files): `grep -rE '(price_gap_free_bps|max_price_gap_bps|enable_price_gap_gate|max_price_gap_pct)' web/src/components/PriceGap/` returns ≥ 4
    - Phase 14 RampReconcileSection still imported by PriceGap.tsx: `grep -c 'RampReconcileSection' web/src/pages/PriceGap.tsx` returns ≥ 1 (D-19 — not removed)
    - `cd web && npm run test:run -- ConfigCard` exits 0
    - `cd web && npm run test:run -- lockstep` exits 0
    - `cd web && npm run build` exits 0 (frontend bundle rebuilt)
    - `git diff web/package.json` empty
    - `git diff web/package-lock.json` empty
    - `git diff config.json` empty
    - No orphan references: `grep -rE '(price_gap_free_bps|max_price_gap_bps|enable_price_gap_gate|max_price_gap_pct)' web/src/ | grep -v PriceGap | grep -v node_modules | grep -v dist` returns NO matches
  </acceptance_criteria>
  <done>ConfigCard composes 4 subsections, integrated at top of PriceGap.tsx, Config.tsx legacy controls deleted, no orphan references, vitest green, build green, no dependency drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Operator UAT — render Configuration card in BOTH locales + exercise typed-phrase modals</name>
  <files>.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-04-HUMAN-UAT.md, .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/uat-evidence/configcard/</files>
  <action>Operator-led UAT in BOTH locales (EN + zh-TW). Operator runs `make build`, restarts engine binary, exercises full Configuration card render + ENABLE-LIVE-CAPITAL + ENABLE-BREAKER typed-phrase modal flows per the steps in &lt;how-to-verify&gt;. Captures screenshots in both locales (≥6 total: card-EN, card-zh-TW, 4 modal states), saves under uat-evidence/configcard/, writes 16-04-HUMAN-UAT.md with the checklist template. Verdict must be `pass` before Task 4 (VERSION/CHANGELOG bump) proceeds.</action>
  <verify><automated>test -f .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-04-HUMAN-UAT.md &amp;&amp; grep -q 'Verdict' .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-04-HUMAN-UAT.md &amp;&amp; ls .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/uat-evidence/configcard/ 2>/dev/null | head -1</automated></verify>
  <done>16-04-HUMAN-UAT.md exists with all checklist items addressed; ≥6 screenshots saved under uat-evidence/configcard/; verdict is `pass`.</done>
  <what-built>New Configuration card with 4 subsections + ENABLE-LIVE-CAPITAL / ENABLE-BREAKER typed-phrase modals + Config.tsx legacy controls migrated.</what-built>
  <how-to-verify>
    1. From repo root: `make build` (full rebuild — frontend FIRST then Go binary, per project pitfall 9).
    2. Restart the engine binary.
    3. Open the dashboard. Switch locale to EN.
    4. Navigate to Price-Gap tab. Confirm:
       - "Strategy 4 Configuration" card appears at TOP (above Discovery / Candidates / Positions / History).
       - Card defaults to collapsed; click expands.
       - Card has 4 subsections in order: Scanner, Drawdown Circuit Breaker, Live-Capital Ramp (read-only banner shown), Live-Capital & Risk.
       - Phase 14 RampReconcileSection + Phase 15 BreakerSubsection still appear in their original locations (D-19).
    5. Click ENABLE-LIVE-CAPITAL toggle (currently OFF):
       - Modal opens.
       - Real-trip warning banner visible (red).
       - Confirm button disabled.
       - Type `ENABLE-LIVE-CAPITAL` exactly. Confirm button enables.
       - Type lowercase `enable-live-capital` first to verify case-sensitivity rejects it.
       - Esc closes modal.
    6. Click ENABLE-BREAKER toggle (currently OFF). Same flow, phrase `ENABLE-BREAKER`.
    7. Switch locale to zh-TW. Repeat steps 4-6.
       - Verify all labels appear in Chinese.
       - Verify magic strings ENABLE-LIVE-CAPITAL / ENABLE-BREAKER stay LITERAL (untranslated, visible in modal prompt).
       - Verify NO raw key strings (`pricegap.config.X`) are visible anywhere.
    8. Open Config tab. Confirm the 4 deleted controls (price_gap_free_bps, max_price_gap_bps, enable_price_gap_gate, max_price_gap_pct) are GONE.
    9. Capture screenshots:
       - PriceGap tab EN, card expanded
       - PriceGap tab zh-TW, card expanded
       - Each modal in both locales (4 screenshots)
    10. Save screenshots under `.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/uat-evidence/configcard/`.
    11. Write `16-04-HUMAN-UAT.md` with this template:
        ```
        # Phase 16 Plan 04 — PG-OPS-09 Human UAT
        ## Build
        - VERSION: <value>
        - make build: <pass/fail>
        ## Render checks
        - [ ] Card visible at top of PriceGap tab (EN)
        - [ ] Card visible at top of PriceGap tab (zh-TW)
        - [ ] All 4 subsections render
        - [ ] Phase 14/15 widgets still in original location
        - [ ] No raw i18n keys visible in either locale
        ## Typed-phrase modals
        - [ ] ENABLE-LIVE-CAPITAL: case-sensitive accept
        - [ ] ENABLE-LIVE-CAPITAL: lowercase rejects
        - [ ] ENABLE-BREAKER: case-sensitive accept
        - [ ] Magic strings untranslated in zh-TW
        ## Config.tsx migration
        - [ ] 4 legacy controls absent from Config tab
        ## Screenshots
        - <list saved file paths>
        ## Verdict
        - <pass / fail>
        ```
  </how-to-verify>
  <resume-signal>Type "uat green" with the verdict, or "uat issues" with details to address</resume-signal>
</task>

<task type="auto">
  <name>Task 4: VERSION + CHANGELOG bump for PG-OPS-09</name>
  <read_first>
    - VERSION (current value — likely already 0.38.x from Plans 01/02/03)
    - CHANGELOG.md (head 30 lines)
    - 16-04-HUMAN-UAT.md (verdict must be pass)
  </read_first>
  <action>
    Step 1: Confirm UAT verdict is "pass". If fail, STOP.

    Step 2: Bump VERSION. Plan 04 closes Phase 16 — bump to MINOR (e.g. 0.38.x → 0.39.0).

    Step 3: Prepend CHANGELOG section:

    ```
    ## 0.39.0 — 2026-05-02

    ### Phase 16 Plan 04 — Strategy 4 Configuration card consolidation (PG-OPS-09)

    **UI consolidation.** All Strategy 4 editable configuration now lives in a single "Strategy 4 Configuration" card at the top of the Price-Gap dashboard tab. Operator one-stop: scanner, breaker, ramp display, live-capital + risk, plus 4 controls migrated from the legacy Config tab. Phase 14 ramp-reconcile widget and Phase 15 breaker subsection stay in their existing locations (D-19 — read-mostly status displays separated from editable inputs).

    #### Added
    - `web/src/components/PriceGap/ConfigCard.tsx` — collapsible top-of-page Configuration card with 4 subsections (Scanner / Breaker / Ramp / Live-Capital).
    - Scanner subsection: `PriceGapDiscoveryEnabled`, `PriceGapScanIntervalSec`, `PriceGapMaxUniverse`, `PriceGapAutoPromoteScore`, `PriceGapMaxCandidates`.
    - Breaker subsection: `PriceGapBreakerEnabled` (typed-phrase ENABLE-BREAKER), `PriceGapDrawdownLimitUSDT`, `PriceGapBreakerIntervalSec`.
    - Ramp subsection: read-only display of stage sizes + hard ceiling + clean-day target (D-19 — edits through pg-admin CLI).
    - Live-Capital + Risk subsection: `PriceGapLiveCapital` (typed-phrase ENABLE-LIVE-CAPITAL + operator_action: true on POST), `PriceGapAnomalySlippageBps`, plus migrated gate fields.
    - `web/src/components/PriceGap/ConfigCard.test.tsx` — Vitest render + interaction tests.
    - `web/src/i18n/lockstep.test.ts` — Vitest assertion that en.ts and zh-TW.ts have identical key sets (Pitfall 6 closure).
    - ~60 new i18n keys per locale (en + zh-TW lockstep).
    - Operator UAT evidence at `.planning/phases/16-.../16-04-HUMAN-UAT.md` + `uat-evidence/configcard/` screenshots in both locales.

    #### Changed
    - `web/src/pages/PriceGap.tsx` renders ConfigCard at top of layout (above existing sections).
    - `web/src/components/Ramp/BreakerConfirmModal.tsx` `Phrase` union extended to include `'ENABLE-LIVE-CAPITAL' | 'ENABLE-BREAKER'`. Interaction logic unchanged (case-sensitive, no trim, Esc closes, Enter does not submit) — magic strings NOT translated per CONTEXT D-18.

    #### Removed
    - `web/src/pages/Config.tsx` — 4 legacy Strategy 4 controls deleted: `price_gap_free_bps`, `max_price_gap_bps`, `enable_price_gap_gate`, `max_price_gap_pct`. All migrated to ConfigCard. Pre-delete grep verified no orphan references in other components.

    #### Preserved
    - Phase 14 `RampReconcileSection` widget — kept in original location (D-19).
    - Phase 15 `BreakerSubsection` widget — kept in original location (D-19).
    - CandidateRegistry chokepoint (Phase 11) — candidate CRUD writes unchanged.
    - PG-FIX-02 server guard (Plan 02) — ENABLE-LIVE-CAPITAL toggle posts `operator_action: true`; integration tests pass through Plan 02's reject + accept assertions.
    - npm lockdown — `package.json` and `package-lock.json` unchanged in this plan.
    ```

    Step 4: Confirm config.json untouched.
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && grep -q 'pass' .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-04-HUMAN-UAT.md && grep -q '^0.39.0$' VERSION && head -5 CHANGELOG.md | grep -q '## 0.39.0 — 2026-05-02' && grep -q 'PG-OPS-09' CHANGELOG.md && test -z "$(git diff config.json web/package.json web/package-lock.json 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - `cat VERSION` outputs `0.39.0`
    - `head -5 CHANGELOG.md` contains `## 0.39.0 — 2026-05-02`
    - `grep -c 'PG-OPS-09' CHANGELOG.md` returns ≥ 1
    - `grep -c 'ConfigCard' CHANGELOG.md` returns ≥ 1
    - `grep -c 'ENABLE-LIVE-CAPITAL' CHANGELOG.md` returns ≥ 1
    - `grep -c 'lockstep' CHANGELOG.md` returns ≥ 1
    - `git diff config.json` empty
    - `git diff web/package.json web/package-lock.json` empty
    - 16-04-HUMAN-UAT.md verdict line contains "pass"
  </acceptance_criteria>
  <done>VERSION bumped to 0.39.0 (Phase 16 ship); CHANGELOG documents UI consolidation + magic-string extension + i18n lockstep + Config.tsx migration; npm lockdown preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ /api/config (POST) | Same as Plan 02 — operator_action marker required for paper_mode; Plan 04's ENABLE-LIVE-CAPITAL toggle adheres |
| Operator click ↔ destructive action (live capital ON, breaker OFF) | Typed-phrase modal interposes intent verification |
| Browser ↔ embedded JS bundle | npm lockdown preserved — no new attack surface from third-party deps |
| Frontend i18n strings ↔ DOM | `useLocale().t()` enforces TranslationKey lookup; missing key = compile error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering | Operator mis-click on ENABLE-LIVE-CAPITAL toggle | mitigate | Typed-phrase modal requires literal `ENABLE-LIVE-CAPITAL` (case-sensitive, no trim, Enter does not submit). Phase 15 D-12 pattern verbatim. |
| T-16-04-02 | Tampering | Operator mis-click on ENABLE-BREAKER toggle | mitigate | Typed-phrase modal requires `ENABLE-BREAKER`. Same modal interaction contract. |
| T-16-04-03 | Tampering | ENABLE-LIVE-CAPITAL POST flips paper_mode without operator_action marker (regression of Plan 02 guard) | mitigate | ConfigCard ENABLE-LIVE-CAPITAL toggle's POST body explicitly includes `operator_action: true`. Vitest interaction test covers. Plan 02 server guard catches if it's ever omitted. |
| T-16-04-04 | Information Disclosure | Raw i18n keys (`pricegap.config.X`) visible in zh-TW because translator missed a key | mitigate | New `web/src/i18n/lockstep.test.ts` Vitest test asserts key-set equality (closes Pitfall 6 from RESEARCH). Operator UAT explicitly checks both locales. |
| T-16-04-05 | Tampering | Magic strings get translated and break the safety guard | mitigate | UI-SPEC §"Magic strings (NEVER translated...)" + acceptance criterion grep checks. CHANGELOG documents the rule. BreakerConfirmModal interaction logic unchanged from Phase 15 (case-sensitive match preserved). |
| T-16-04-06 | Tampering | Config.tsx legacy delete leaves orphan references in other components (Pitfall 7) | mitigate | Pre-delete grep across web/src/ explicit in Task 2 Step 4; acceptance criterion repeats the grep as a hard gate. |
| T-16-04-07 | Tampering (build) | go:embed serves stale JS because frontend not rebuilt before binary (Pitfall 9) | mitigate | UAT checkpoint Step 1 explicitly runs `make build` (which chains frontend then Go); operator UAT verifies the new card renders before signing off. |
| T-16-04-08 | Tampering (dependency) | Engineer adds new npm package for fancy input under deadline pressure (Pitfall 8) | mitigate | Acceptance criterion: `git diff web/package.json web/package-lock.json` is empty. CHANGELOG explicitly documents npm lockdown preserved. |
| T-16-04-09 | DoS | Operator spams toggle clicks during typed-phrase modal | accept | Modal busy state disables submit during in-flight POST (BreakerConfirmModal precedent); existing rate-limit / bearer-auth gate. |
| T-16-04-10 | Repudiation | Operator denies enabling live capital | mitigate | Typed-phrase magic string gives strong intent signal in audit logs; Plan 02 server logs the operator_action marker on every paper_mode-related POST. |
| T-16-04-11 | Authorization (V4 ASVS) | Authorization on Configuration card writes | mitigate | All POSTs ride existing bearer-token auth; no new authorization surface. CandidateRegistry chokepoint (Phase 11) handles candidate CRUD authorization separately — unchanged. |
| T-16-04-12 | Input Validation (V5 ASVS) | Number inputs out of range (e.g. negative scan interval) | mitigate | HTML5 `min`/`max`/`step` per UI-SPEC. Inline error from `pricegap.config.validation.range` on blur. Server-side `validatePriceGapLive` is authoritative (CONTEXT — no new validation needed at boot). |
| T-16-04-13 | Race condition | Two operators flip live-capital simultaneously | accept | Existing /api/config handler holds write lock; per-section save reduces blast radius; rare in single-operator setup. |
</threat_model>

<verification>
- `cd web && npm run test:run` exits 0 (full vitest suite including new lockstep + ConfigCard tests)
- `cd web && npm run build` exits 0
- `make build` exits 0 (full rebuild including go:embed)
- `go test ./... -count=1` exits 0 (regression — handlers tests from Plan 02 still pass)
- Operator UAT verdict = pass with screenshots in both locales
- 4 legacy Config.tsx controls deleted; no orphan references via grep
- BreakerConfirmModal Phrase union extended; interaction contract unchanged
- ~60 i18n keys added in BOTH locales; lockstep test green
- Magic strings ENABLE-LIVE-CAPITAL / ENABLE-BREAKER appear LITERAL in source (no translation, no lowercase)
- Phase 14 RampReconcileSection + Phase 15 BreakerSubsection still in original location (D-19)
- npm lockdown preserved (package.json + lock untouched)
- VERSION bumped to 0.39.0; CHANGELOG documents the consolidation + magic-string extension
- config.json untouched
</verification>

<success_criteria>
- ROADMAP success-criterion #4 closed: new top-level Price-Gap dashboard tab consolidates paper toggle, ramp display, breaker threshold, scanner config, PriceGapMaxCandidates, PriceGapAutoPromoteScore, candidate CRUD (preserved via Phase 11 chokepoint), bidirectional mode (preserved from Phase 999.1) — and 4 legacy controls in other tabs migrated.
- ROADMAP success-criterion #5 closed: EN + zh-TW i18n keys for the new tab in lockstep — verified by Vitest lockstep test.
- All 4 phase requirements (PG-FIX-01, PG-FIX-02, DEV-01, PG-OPS-09) addressed across Plans 01-04.
</success_criteria>

<output>
After completion, create `.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-04-SUMMARY.md`.
</output>
