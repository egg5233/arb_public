---
phase: 16
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/bingxprobe/main.go
  - Makefile
  - .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-RESTORATION-NOTES.md
  - .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-HUMAN-UAT.md
  - VERSION
  - CHANGELOG.md
autonomous: false
requirements: [DEV-01]
must_haves:
  truths:
    - "cmd/bingxprobe/main.go exists in the working tree, restored from commit 21cb60b^"
    - "Restored source compiles against current go.mod (no compile-blocking adapter drift)"
    - "make probe-bingx target is defined in the Makefile"
    - "make probe-bingx runs end-to-end and prints a successful BingX probe response"
    - "Restoration notes document any retrofit needed to match D-11 probe scope (preflight TestOrder + ticker)"
  artifacts:
    - path: "cmd/bingxprobe/main.go"
      provides: "Restored BingX probe utility (preflight TestOrder + ticker per D-11)"
      contains: "package main"
    - path: "Makefile"
      provides: "probe-bingx .PHONY target running go run ./cmd/bingxprobe/"
      contains: "probe-bingx:"
    - path: ".planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-RESTORATION-NOTES.md"
      provides: "Gap assessment between restored source (21cb60b^) and D-11 probe scope contract"
      contains: "Restoration source"
  key_links:
    - from: "Makefile probe-bingx target"
      to: "cmd/bingxprobe/main.go"
      via: "go run ./cmd/bingxprobe/"
      pattern: "go run \\./cmd/bingxprobe"
    - from: "cmd/bingxprobe/main.go"
      to: "BingX adapter OrderPreflight.TestOrder"
      via: "production preflight path (per D-11) — same path priceGapBingXProbePrice uses"
      pattern: "OrderPreflight|TestOrder"
---

<objective>
DEV-01: Restore the deleted `cmd/bingxprobe/` debug utility from commit `21cb60b^` (parent of the Phase 13 deletion commit) and add a `make probe-bingx` Makefile target. Probe scope per D-11: preflight `OrderPreflight.TestOrder` + ticker fetch — exercises the production path that `priceGapBingXProbePrice` uses, validating the path Phase 9 paper-mode and Phase 11/12 scanner depend on.

Purpose: Restore reproducible BingX probe for ops. Phase 13 deletion ("debug utility purpose served") is reversed per ROADMAP success-criterion #3.

Output: Restored `cmd/bingxprobe/main.go` (with retrofit if D-11 scope mismatch), `make probe-bingx` target, restoration notes documenting any drift, human-UAT capturing successful probe output, VERSION + CHANGELOG bump.
</objective>

<execution_context>
@/var/solana/data/arb/.claude/get-shit-done/workflows/execute-plan.md
@/var/solana/data/arb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-CONTEXT.md
@.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-RESEARCH.md
@Makefile
@pkg/exchange/types.go

<interfaces>
<!-- Key contracts the executor needs without grepping -->

From pkg/exchange/types.go:79-83 (OrderPreflight interface — production path the probe must exercise):
```go
type OrderPreflight interface {
    TestOrder(ctx context.Context, req PlaceOrderRequest) error
}
```

Adapter location: `pkg/exchange/bingx/` — the BingX adapter implements OrderPreflight. Confirm exact constructor (likely `bingx.NewClient` or similar — read first).

From internal/pricegaptrader/execution.go:296 (priceGapBingXProbePrice usage — call shape to mirror in the probe):
```go
// preflight.TestOrder(ctx, PlaceOrderRequest{...non-marketable...})
```

Restored source (recoverable):
```bash
git show 21cb60b^:cmd/bingxprobe/main.go
```
RESEARCH confirmed this is a 53-line gorilla/websocket WS bookTicker subscriber. PER D-11 + RESEARCH Pitfall 5: the original WS-only scope is DIFFERENT from what D-11 specifies (preflight TestOrder + ticker). The restored source likely needs a retrofit. Plan to read it first, assess gap, and either trim/extend OR replace WS-only scope with REST preflight + ticker.

Existing Makefile structure (RESEARCH verified):
```makefile
.PHONY: build-frontend build run dev clean
# 5 existing targets; clean insertion point for probe-bingx
```

Project rules (CLAUDE.local.md / CLAUDE.md):
- Build order does NOT apply (no frontend embed in this plan)
- Single Go binary; gorilla/websocket already in go.mod
- No new dependencies allowed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect restored source from 21cb60b^, assess D-11 scope gap, document retrofit plan</name>
  <read_first>
    - .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-CONTEXT.md (D-10, D-11 — restoration scope)
    - .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-RESEARCH.md (Pitfalls 4 + 5 — drift + scope-mismatch risk)
    - pkg/exchange/types.go (lines 70-100 — OrderPreflight + PlaceOrderRequest signatures)
    - internal/pricegaptrader/execution.go (lines 280-320 — priceGapBingXProbePrice call shape to mirror)
  </read_first>
  <action>
    Step 1: Print restored source to a tmp file (no commit yet):
    ```bash
    cd /var/solana/data/arb
    mkdir -p /tmp/16-03-restore
    git show 21cb60b^:cmd/bingxprobe/main.go > /tmp/16-03-restore/main.go.original
    wc -l /tmp/16-03-restore/main.go.original
    ```

    Step 2: Read `/tmp/16-03-restore/main.go.original` end-to-end. Identify:
      - Imports (esp. gorilla/websocket, bingx adapter)
      - Top-level operations (WS dial? REST call? TestOrder? ticker?)
      - Credential source (env vars? hardcoded?)
      - Output format

    Step 3: Discover current BingX adapter constructor + OrderPreflight implementation:
    ```bash
    # Locate adapter package + constructor
    grep -rn 'package bingx' pkg/exchange/bingx/ | head -5
    grep -rn 'func New' pkg/exchange/bingx/ | head -10
    # Confirm it implements OrderPreflight
    grep -rn 'TestOrder' pkg/exchange/bingx/ | head -5
    # Confirm production probe call shape
    grep -n 'priceGapBingXProbePrice\|TestOrder' internal/pricegaptrader/execution.go | head -10
    ```

    Step 4: Write `.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-RESTORATION-NOTES.md` with EXACTLY these sections:

    ```markdown
    # Phase 16 Plan 03 — bingxprobe Restoration Notes

    ## Restoration source
    - Commit: `21cb60b^` (parent of `21cb60b chore(13): remove cmd/bingxprobe — debug utility purpose served`)
    - File: `cmd/bingxprobe/main.go`
    - Original line count: <N>
    - Original scope: <one-paragraph summary of what original utility did>

    ## D-11 contract
    - Probe scope: preflight `OrderPreflight.TestOrder` + ticker fetch.
    - Production path mirrored: `internal/pricegaptrader/execution.go:296` (priceGapBingXProbePrice).

    ## Gap analysis
    | Aspect | Original (21cb60b^) | D-11 contract | Action |
    |--------|---------------------|---------------|--------|
    | Operation | <WS bookTicker / REST call / etc.> | preflight TestOrder + ticker | <keep / retrofit / replace> |
    | Imports | <gorilla/websocket etc.> | bingx adapter + ticker call | <add / remove> |
    | Credentials | <env var path> | match adapter env vars | <align> |
    | Output | <stdout fmt> | non-error stdout per D-14 | <keep / extend> |

    ## Retrofit plan (Task 2)
    <If gap is small (≤20 lines): patch in place. If large (full rewrite): replace WS-only scope with adapter-based REST probe + ticker per D-11 contract. Justify choice here.>

    ## Compile risk (Pitfall 4)
    - gorilla/websocket: present in go.mod? <yes/no>
    - bingx adapter API stable since 21cb60b^? <yes/no — note any rename>
    - Estimated retrofit size: <S/M/L>
    ```
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && test -s /tmp/16-03-restore/main.go.original && test -f .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-RESTORATION-NOTES.md && grep -q '## Gap analysis' .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-RESTORATION-NOTES.md && grep -q '## Retrofit plan' .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-RESTORATION-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - Tmp file `/tmp/16-03-restore/main.go.original` exists and is non-empty
    - File `.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-RESTORATION-NOTES.md` exists
    - Notes file contains all 4 H2 sections (## Restoration source, ## D-11 contract, ## Gap analysis, ## Retrofit plan, ## Compile risk)
    - Gap-analysis table is filled in (no `<placeholder>` remaining)
    - Retrofit plan section explicitly chooses "keep / retrofit / replace" with justification
  </acceptance_criteria>
  <done>Restoration notes committed; retrofit decision documented before any code restoration touches the working tree.</done>
</task>

<task type="auto">
  <name>Task 2: Restore cmd/bingxprobe/main.go with D-11 retrofit + add Makefile target</name>
  <read_first>
    - 16-03-RESTORATION-NOTES.md (just-created — gap analysis + retrofit plan)
    - /tmp/16-03-restore/main.go.original (the source to restore from)
    - Makefile (full file — current .PHONY block + target style)
    - pkg/exchange/types.go (OrderPreflight + PlaceOrderRequest signatures)
  </read_first>
  <action>
    Step 1: Restore the file. Two paths depending on Task 1 retrofit decision:

    Path A (gap is small — patch in place):
    ```bash
    git checkout 21cb60b^ -- cmd/bingxprobe/
    # then edit cmd/bingxprobe/main.go to align with D-11 (add TestOrder + ticker if missing)
    ```

    Path B (full rewrite per D-11):
    ```bash
    mkdir -p cmd/bingxprobe
    # write a new main.go that:
    #   - reads BingX credentials from env vars (match adapter expectations — read pkg/exchange/bingx/ to confirm)
    #   - constructs the adapter
    #   - calls TestOrder() with a non-marketable PlaceOrderRequest (mirror execution.go:296 call shape)
    #   - calls a ticker fetch (use the adapter's exposed ticker method — read adapter source first)
    #   - prints output to stdout in plain text
    #   - exits 0 on success, non-zero with stderr on failure
    ```

    Either path: the file MUST start with `package main`, define `func main()`, and reference the BingX adapter from `pkg/exchange/bingx/`. Do NOT import `internal/engine` or `internal/spotengine` (module-boundary rule).

    Step 2: Confirm it builds:
    ```bash
    go build ./cmd/bingxprobe/
    go vet ./cmd/bingxprobe/
    ```
    If `go build` fails due to adapter drift (Pitfall 4), fix the call signature(s) or adapter type names — do NOT introduce new dependencies. If you cannot fix without adding a new package, STOP and report to user (do not run `go get`).

    Step 3: Add the Makefile target. Edit `Makefile`:
      - In the existing `.PHONY:` line (currently `.PHONY: build-frontend build run dev clean`), append ` probe-bingx`.
      - At the end of the file (after the existing `clean:` target), append a new target block:
    ```makefile

    probe-bingx:
    	go run ./cmd/bingxprobe/
    ```
    NOTE: The body line MUST start with a literal TAB character (Make requires tabs). Do not use spaces.

    Step 4: Confirm the target works at the syntax level:
    ```bash
    make -n probe-bingx       # dry-run; should print "go run ./cmd/bingxprobe/"
    ```

    Step 5: Confirm `git diff config.json` is empty and no new dependencies in `go.mod`:
    ```bash
    git diff config.json
    git diff go.mod go.sum
    ```
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && test -f cmd/bingxprobe/main.go && go build ./cmd/bingxprobe/ && go vet ./cmd/bingxprobe/ && grep -c 'probe-bingx' Makefile | awk '$1 >= 2 {exit 0} {exit 1}' && make -n probe-bingx 2>&1 | grep -q 'go run ./cmd/bingxprobe' && test -z "$(git diff go.mod go.sum config.json 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - File `cmd/bingxprobe/main.go` exists
    - `head -1 cmd/bingxprobe/main.go` outputs `package main`
    - `go build ./cmd/bingxprobe/` exits 0
    - `go vet ./cmd/bingxprobe/` exits 0
    - `grep -c 'probe-bingx' Makefile` returns ≥ 2 (one in .PHONY line + one target)
    - `grep -E '^probe-bingx:' Makefile` matches (target defined at line start)
    - `make -n probe-bingx` outputs `go run ./cmd/bingxprobe/`
    - `git diff go.mod go.sum config.json` is empty (no new deps; config untouched)
    - cmd/bingxprobe/main.go contains a reference to OrderPreflight.TestOrder OR equivalent BingX adapter call (per D-11 retrofit decision in Task 1)
  </acceptance_criteria>
  <done>Probe utility restored + retrofitted per Task 1 plan; Makefile target defined; compiles + vets clean; no dependency drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Operator runs `make probe-bingx` and captures successful output</name>
  <files>.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-HUMAN-UAT.md</files>
  <action>Operator-led UAT. Operator runs `make probe-bingx` from repo root, captures stdout, writes 16-03-HUMAN-UAT.md per the template in &lt;how-to-verify&gt;. ROADMAP success-criterion #3 requires non-error stdout response. Verdict must be `pass` before Task 4 (VERSION/CHANGELOG bump) proceeds. If probe fails (BingX endpoint unreachable, credentials invalid), operator STOPS and either escalates or returns to Task 2 to fix the retrofit.</action>
  <verify><automated>test -f .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-HUMAN-UAT.md &amp;&amp; grep -q 'Verdict' .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-HUMAN-UAT.md</automated></verify>
  <done>16-03-HUMAN-UAT.md exists with stdout capture and Verdict line; verdict is `pass` before Task 4 begins.</done>
  <what-built>Restored cmd/bingxprobe/main.go + make probe-bingx target.</what-built>
  <how-to-verify>
    1. From repo root, run: `make probe-bingx`
    2. Expected: stdout shows successful BingX response (preflight TestOrder result + ticker output, per D-11). Exit code 0.
    3. Capture the output and write it to `.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-HUMAN-UAT.md` using this template:

    ```markdown
    # Phase 16 Plan 03 — DEV-01 Human UAT

    ## Test
    - Command: `make probe-bingx`
    - Date: <YYYY-MM-DD>
    - Operator: <name>

    ## Output
    ```
    <paste stdout here>
    ```

    ## Result
    - Exit code: <0 / non-zero>
    - Verdict: <pass / fail>
    - Notes: <any errors, retries, or observations>
    ```

    4. ROADMAP success-criterion #3 is satisfied when this UAT shows a non-error stdout response.
    5. If the probe fails (e.g. BingX endpoint unreachable, credentials invalid), STOP. Do NOT bump VERSION/CHANGELOG. Investigate and either fix in Task 2 or escalate.
  </how-to-verify>
  <resume-signal>Type "uat green" with a one-line summary of the probe output, or "uat failed" with the error to investigate</resume-signal>
</task>

<task type="auto">
  <name>Task 4: VERSION + CHANGELOG bump for DEV-01</name>
  <read_first>
    - VERSION (current; may have been bumped by Plans 01/02)
    - CHANGELOG.md (head 20 lines)
    - 16-03-HUMAN-UAT.md (just-captured — verdict must be pass)
  </read_first>
  <action>
    Step 1: Confirm UAT verdict is "pass" by re-reading `16-03-HUMAN-UAT.md`. If the verdict is fail, STOP — do not bump.

    Step 2: Bump `VERSION`. Read current value first; bump by patch (e.g. 0.38.x → 0.38.(x+1)).

    Step 3: Prepend a new CHANGELOG section (after top-of-file header, before most recent entry):

    ```
    ## <VERSION> — 2026-05-02

    ### Phase 16 Plan 03 — Restored cmd/bingxprobe + make probe-bingx (DEV-01)

    **Restoration.** The `cmd/bingxprobe/` debug utility (deleted in `21cb60b chore(13): remove cmd/bingxprobe — debug utility purpose served`) is restored from `21cb60b^` and made reproducible via a new `make probe-bingx` Makefile target. Probe scope per CONTEXT D-11: preflight `OrderPreflight.TestOrder` + ticker fetch — exercises the same production path that `priceGapBingXProbePrice` (`internal/pricegaptrader/execution.go:296`) uses, validating the BingX integration that Phase 9 paper mode and Phase 11/12 scanner depend on.

    #### Added
    - `cmd/bingxprobe/main.go` (restored + retrofitted per `.planning/phases/16-.../16-03-RESTORATION-NOTES.md`).
    - `make probe-bingx` Makefile target.
    - Operator UAT evidence at `.planning/phases/16-.../16-03-HUMAN-UAT.md`.

    #### Reversed
    - Phase 13 deletion (`21cb60b`) — Strategy 4 ops still wants reproducible probe per DEV-01.

    #### Notes
    - No new Go dependencies (gorilla/websocket already in go.mod via existing usage).
    - No `config.json` change.
    ```
  </action>
  <verify>
    <automated>cd /var/solana/data/arb && grep -q 'pass' .planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-HUMAN-UAT.md && head -5 CHANGELOG.md | grep -qE '## 0.38.[1-9] — 2026-05-02' && grep -q 'DEV-01' CHANGELOG.md && grep -q 'probe-bingx' CHANGELOG.md && test -z "$(git diff config.json 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - `cat VERSION` outputs a value > 0.38.0 (bumped)
    - `head -5 CHANGELOG.md` contains `## 0.38.x — 2026-05-02`
    - `grep -c 'DEV-01' CHANGELOG.md` returns ≥ 1
    - `grep -c 'probe-bingx' CHANGELOG.md` returns ≥ 1
    - `grep -c '21cb60b' CHANGELOG.md` returns ≥ 1 (audit-trail to original deletion commit per D-13)
    - `git diff config.json` is empty
  </acceptance_criteria>
  <done>VERSION + CHANGELOG bumped; CHANGELOG entry references original deletion commit + UAT evidence path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell ↔ BingX REST/WS endpoint | Outbound network call from operator's machine to public BingX API; existing pkg/exchange/bingx HMAC auth |
| cmd/bingxprobe ↔ BingX adapter (pkg/exchange/bingx) | Same internal trust boundary as the production engine |
| Operator shell ↔ env var credentials | Local-only; no new credential surface introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering | `OrderPreflight.TestOrder` accidentally placing a real order | mitigate | TestOrder is BingX's `/dry-run` equivalent — non-marketable. Plan 16-03 Task 1 verifies in adapter docs/source that TestOrder does NOT submit live order. If verification fails, retrofit reduces probe to ticker-only (read path). |
| T-16-03-02 | Information Disclosure | Restored bingxprobe leaks API keys to stdout | mitigate | Restored utility reads credentials from existing env path (matches adapter expectations); does NOT print credentials in any output. Output limited to ticker + TestOrder result. |
| T-16-03-03 | Denial of Service | Probe spams BingX endpoint, hits rate limits, affects production engine | accept | Single one-shot probe per `make probe-bingx` invocation; operator-controlled cadence. Same rate-limit budget as production single TestOrder call. |
| T-16-03-04 | Tampering (build-system) | Adding `probe-bingx` to .PHONY breaks adjacent targets | mitigate | Acceptance criteria: `make -n` for adjacent targets (build, dev, clean) still works. Tab vs space in target body verified. |
| T-16-03-05 | Tampering (dependency drift) | Restored source pulls in a removed package, requires `go get` | mitigate | Pitfall 4 explicit; acceptance criterion checks `git diff go.mod go.sum` is empty. If new dep needed, executor STOPS and escalates. |
| T-16-03-06 | Repudiation | UAT pass claim without evidence | mitigate | Checkpoint 3 requires operator paste of stdout into 16-03-HUMAN-UAT.md; Task 4 grep-checks UAT verdict before bumping VERSION. |
</threat_model>

<verification>
- `go build ./cmd/bingxprobe/` exits 0
- `go vet ./cmd/bingxprobe/` exits 0
- `make -n probe-bingx` outputs `go run ./cmd/bingxprobe/`
- `make probe-bingx` (operator-run via Checkpoint 3) prints successful BingX response + exits 0
- 16-03-RESTORATION-NOTES.md documents source + gap + retrofit decision
- 16-03-HUMAN-UAT.md captures operator verification with verdict=pass
- VERSION + CHANGELOG bumped
- config.json untouched; go.mod / go.sum untouched
</verification>

<success_criteria>
- ROADMAP success-criterion #3 closed: `make probe-bingx` runs end-to-end and prints a successful BingX probe response (verified by operator UAT).
- Restoration audit trail complete: notes doc references original deletion commit `21cb60b`; CHANGELOG documents the reversal.
- No dependency drift; no `npm install` / `go get` invoked.
</success_criteria>

<output>
After completion, create `.planning/phases/16-paper-mode-cleanup-dashboard-consolidation/16-03-SUMMARY.md`.
</output>
