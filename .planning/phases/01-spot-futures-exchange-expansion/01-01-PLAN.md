---
phase: 01-spot-futures-exchange-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/livetest/main.go
  - pkg/exchange/bybit/margin.go
  - internal/spotengine/execution.go
autonomous: false
requirements: [SF-01, SF-02, SF-03]

must_haves:
  truths:
    - "Livetest harness has PlaceSpotMarginOrder test (Dir A sell with auto-borrow, Dir B buy with QuoteSize)"
    - "Livetest harness has GetSpotMarginOrder test (fill reconciliation)"
    - "Bybit passes all 26+ livetest tests including new margin trade tests"
    - "Bybit Dir A (borrow-sell-long) manual open and close works end-to-end"
    - "Bybit Dir B (buy-spot-short) manual open and close works end-to-end"
    - "After Bybit exit, GetMarginBalance shows zero residual borrows"
  artifacts:
    - path: "cmd/livetest/main.go"
      provides: "Enhanced margin trade tests (27-28)"
      contains: "PlaceSpotMarginOrder"
  key_links:
    - from: "cmd/livetest/main.go"
      to: "pkg/exchange/types.go"
      via: "SpotMarginExchange interface methods"
      pattern: "PlaceSpotMarginOrder|GetSpotMarginOrder"
    - from: "internal/spotengine/execution.go"
      to: "pkg/exchange/bybit/margin.go"
      via: "SpotMarginExchange.PlaceSpotMarginOrder(AutoBorrow: true)"
      pattern: "AutoBorrow.*true"
---

<objective>
Enhance the livetest harness with spot margin trade tests and fully verify Bybit as the reference implementation for both Dir A and Dir B lifecycle.

Purpose: Bybit is the most-tested exchange but has not been fully verified for edge cases (per D-02). The enhanced livetest harness is a prerequisite for all subsequent exchange verification. Establishing Bybit as a known-good reference makes debugging other exchanges faster.

Output: Enhanced livetest with margin trade tests (27-28), Bybit fully passing all tests, Bybit Dir A + Dir B manually verified with live trades.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spot-futures-exchange-expansion/01-CONTEXT.md
@.planning/phases/01-spot-futures-exchange-expansion/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From pkg/exchange/types.go (lines 278-351):
```go
type SpotMarginOrderParams struct {
    Symbol     string // e.g. "BTCUSDT"
    Side       Side   // buy or sell
    OrderType  string // "limit" or "market"
    Price      string // required for limit orders
    Size       string // quantity in base coin (required for limit + market sell)
    QuoteSize  string // quantity in quote coin / USDT (required for market buy on some exchanges)
    Force      string // "gtc", "ioc", "fok"
    AutoBorrow bool   // if true, exchange auto-borrows on sell
    AutoRepay  bool   // if true, exchange auto-repays on buy
    ClientOid  string
}

type SpotMarginExchange interface {
    MarginBorrow(params MarginBorrowParams) error
    MarginRepay(params MarginRepayParams) error
    PlaceSpotMarginOrder(params SpotMarginOrderParams) (orderID string, err error)
    GetMarginInterestRate(coin string) (*MarginInterestRate, error)
    GetMarginBalance(coin string) (*MarginBalance, error)
    TransferToMargin(coin string, amount string) error
    TransferFromMargin(coin string, amount string) error
}

type SpotMarginOrderQuerier interface {
    GetSpotMarginOrder(orderID, symbol string) (*SpotMarginOrderStatus, error)
}
```

From cmd/livetest/main.go: Tests 23-26 exist (GetMarginInterestRate, GetMarginBalance, MarginBorrow+Repay, Transfer). Tests 27-28 are missing and must be added for PlaceSpotMarginOrder and GetSpotMarginOrder.

From internal/spotengine/execution.go (line 309-391): executeBorrowSellLong() calls PlaceSpotMarginOrder with AutoBorrow:true, then PlaceOrder for futures long. executeBuySpotShort() calls PlaceSpotMarginOrder with QuoteSize for market buy, then PlaceOrder for futures short.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PlaceSpotMarginOrder and GetSpotMarginOrder tests to livetest harness</name>
  <files>cmd/livetest/main.go</files>
  <read_first>
    - cmd/livetest/main.go (full file -- understand existing test structure, runTest helper, testMargin flag gating, how tests 23-26 work)
    - pkg/exchange/types.go (SpotMarginOrderParams, SpotMarginOrderStatus, SpotMarginExchange, SpotMarginOrderQuerier interfaces)
    - pkg/exchange/bybit/margin.go (reference implementation of PlaceSpotMarginOrder and GetSpotMarginOrder)
  </read_first>
  <action>
Add two new tests to the livetest harness, gated behind the --test-margin flag (same pattern as tests 25-26). Insert them after test 26. Update the totalTests constant from 26 to 28.

**Test 27: PlaceSpotMarginOrder (Dir A sell with auto-borrow + Dir B buy)**

This test exercises the critical path for both directions:
1. Pick a cheap, liquid symbol for testing. Use SEIUSDT (low price, available on all exchanges). If SEI is not in contracts list, fall back to DOGEUSDT.
2. Dir A test: Place a market SELL with AutoBorrow:true for a small quantity (e.g., "1" SEI or "10" DOGE -- enough to be above minimum order size). Call `PlaceSpotMarginOrder(SpotMarginOrderParams{Symbol: testSymbol, Side: SideSell, OrderType: "market", Size: smallQty, AutoBorrow: true})`. Verify: orderID is non-empty, no error. Then check GetMarginBalance(baseCoin) to confirm Borrowed > 0 (auto-borrow worked). Then immediately buy back: `PlaceSpotMarginOrder(SpotMarginOrderParams{Symbol: testSymbol, Side: SideBuy, OrderType: "market", QuoteSize: buybackQuote, AutoRepay: true})` where buybackQuote = quantity * lastPrice * 1.05 (5% slippage buffer). Verify: Borrowed drops back to ~0 after auto-repay.
3. Dir B test: Place a market BUY using QuoteSize (e.g., "5" USDT worth). Call `PlaceSpotMarginOrder(SpotMarginOrderParams{Symbol: testSymbol, Side: SideBuy, OrderType: "market", QuoteSize: "5"})`. Verify: orderID is non-empty, no error. Then sell back: `PlaceSpotMarginOrder(SpotMarginOrderParams{Symbol: testSymbol, Side: SideSell, OrderType: "market", Size: filledQty})`.
4. Use time.Sleep(2*time.Second) between operations for order settlement.
5. Print detailed output: order IDs, filled quantities, prices, margin balance before/after.
6. On any error, print the error but attempt cleanup (repay/sell back) to avoid leaving open borrows.

**Test 28: GetSpotMarginOrder (fill reconciliation)**

This test verifies the SpotMarginOrderQuerier interface:
1. Check if the exchange implements SpotMarginOrderQuerier via type assertion: `querier, ok := exc.(exchange.SpotMarginOrderQuerier)`. If not, print "SKIPPED (exchange does not implement SpotMarginOrderQuerier)" and pass.
2. If supported, use the order ID from Test 27's Dir A sell order (store it in a variable accessible to Test 28). If Test 27 didn't run or failed, skip with message.
3. Call `querier.GetSpotMarginOrder(orderID, testSymbol)` and verify: Status is "filled" or "closed", FilledQty > 0, AvgPrice > 0.
4. Print: OrderID, Status, FilledQty, AvgPrice.

**Important implementation details:**
- Both tests must be inside the `if testMargin { ... }` block, after the existing tests 25-26.
- Use the same `runTest()` helper pattern as existing tests.
- Store Test 27's order ID and symbol in variables declared before the test block so Test 28 can access them.
- Increment `totalTests` constant at the top of main.go from 26 to 28.
- For the test symbol, get the last price from the exchange via `exc.GetLastPrice(testSymbol)` or from the BBO data. Use this to calculate appropriate quantities.
- Handle the case where the exchange's minimum order size is larger than our test amount -- log and skip gracefully.

Per D-04: This is the "Livetest CLI first" phase of verification. These tests validate the adapter's PlaceSpotMarginOrder behavior before attempting full lifecycle trades.

**Skill requirement:** Agent team MUST load `/local-api-docs` skill when implementing, as this involves exchange API code.
  </action>
  <verify>
    <automated>go build ./cmd/livetest/ && echo "livetest builds"</automated>
  </verify>
  <acceptance_criteria>
    - cmd/livetest/main.go contains `const totalTests = 28`
    - cmd/livetest/main.go contains string `"27. PlaceSpotMarginOrder`
    - cmd/livetest/main.go contains string `"28. GetSpotMarginOrder`
    - cmd/livetest/main.go contains `PlaceSpotMarginOrder(exchange.SpotMarginOrderParams{` (at least 2 calls -- sell and buy)
    - cmd/livetest/main.go contains `AutoBorrow: true` inside a PlaceSpotMarginOrder call
    - cmd/livetest/main.go contains `AutoRepay: true` inside a PlaceSpotMarginOrder call
    - cmd/livetest/main.go contains `GetSpotMarginOrder(` call
    - cmd/livetest/main.go contains type assertion `exchange.SpotMarginOrderQuerier`
    - `go build ./cmd/livetest/` exits 0
    - `go vet ./cmd/livetest/` exits 0
  </acceptance_criteria>
  <done>Livetest harness has tests 27-28 covering PlaceSpotMarginOrder (Dir A auto-borrow sell + buyback, Dir B QuoteSize buy + sell) and GetSpotMarginOrder (fill reconciliation). Tests compile and are gated behind --test-margin flag.</done>
</task>

<task type="auto">
  <name>Task 2: Run Bybit livetest and fix any adapter bugs found</name>
  <files>pkg/exchange/bybit/margin.go, internal/spotengine/execution.go</files>
  <read_first>
    - pkg/exchange/bybit/margin.go (full file -- understand current implementation of all SpotMarginExchange methods)
    - pkg/exchange/bybit/client.go (understand signing, HTTP methods, error handling)
    - pkg/exchange/bybit/adapter.go (understand symbol mapping, LoadAllContracts, GetOrderbook)
    - internal/spotengine/execution.go (executeBorrowSellLong lines 309-391, executeBuySpotShort lines 395+, closeDirectionA lines 1145+, closeDirectionB lines 1325+)
    - internal/spotengine/exit_manager.go (exit triggers and repay retry logic)
    - cmd/livetest/main.go (test 27-28 just added -- understand what they test)
  </read_first>
  <action>
Run the Bybit livetest with margin tests and fix any issues found. Per D-02, Bybit is NOT fully verified despite v0.22.44-49 work.

**Step 1: Run livetest**
Execute: `go run ./cmd/livetest/ --exchange bybit --test-margin`
Examine all 28 test results. For tests 23-28 (margin tests), pay close attention to:
- Test 23: GetMarginInterestRate returns valid HourlyRate > 0
- Test 24: GetMarginBalance returns sensible values (TotalBalance, Available, MaxBorrowable)
- Test 25: MarginBorrow + MarginRepay succeeds (1 USDT)
- Test 26: TransferToMargin returns nil (Bybit is unified, should be no-op)
- Test 27: PlaceSpotMarginOrder auto-borrow sell works, GetMarginBalance confirms Borrowed > 0, auto-repay buyback works
- Test 28: GetSpotMarginOrder returns filled status with quantity and price

**Step 2: Fix any failures**
Based on v0.22.44-49 precedent, expect 0-2 bugs on Bybit (most issues already fixed). Common patterns to watch for:
- Integer precision on borrow amounts (Bybit uses math.Floor for whole numbers)
- `isLeverage=1` must be present on margin orders
- GetSpotMarginOrder may need fallback from realtime to history endpoint (existing pattern in bybit/margin.go)
- PlaceSpotMarginOrder with QuoteSize -- verify Bybit passes `marketUnit=quoteCoin` for market BUY with QuoteSize

For each bug found:
1. Identify root cause (per /sdebug methodology)
2. Fix in the adapter (pkg/exchange/bybit/margin.go) or engine (internal/spotengine/execution.go) as appropriate
3. Re-run the failing test to confirm fix

**Step 3: Verify no regressions**
Run: `go test ./pkg/exchange/bybit/ -count=1 -v` (existing unit tests)
Run: `go test ./internal/spotengine/ -count=1 -v` (existing unit tests)
Run: `go build ./...` (full project builds)

**Skill requirements:**
- Agent team MUST load `/local-api-docs` skill for exchange API code
- Agent team MUST load `/sdebug` skill if debugging adapter bugs
  </action>
  <verify>
    <automated>go test ./pkg/exchange/bybit/ -count=1 -v && go test ./internal/spotengine/ -count=1 -v && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go run ./cmd/livetest/ --exchange bybit --test-margin` shows all 28 tests PASS (run manually to verify)
    - `go test ./pkg/exchange/bybit/ -count=1` exits 0
    - `go test ./internal/spotengine/ -count=1` exits 0
    - `go build ./...` exits 0
    - If any bugs were fixed in bybit/margin.go, each fix has error wrapping with `fmt.Errorf("MethodName: %w", err)` pattern
  </acceptance_criteria>
  <done>Bybit passes all 28 livetest tests including new margin trade tests. Any adapter bugs found are fixed. Unit tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Bybit Dir A and Dir B with live trades</name>
  <files>N/A</files>
  <action>
Human verifies Bybit full lifecycle by running small live trades for both Dir A (borrow-sell-long) and Dir B (buy-spot-short), then closing each position and confirming no residual borrows remain.
  </action>
  <verify>User confirms both directions work end-to-end on Bybit</verify>
  <done>Bybit Dir A and Dir B manually verified with live trades, no residual borrows after exit</done>
  <what-built>Enhanced livetest harness (tests 27-28) and Bybit full margin verification. Bybit should now be confirmed working for both Dir A (borrow-sell-long) and Dir B (buy-spot-short) at the API level.</what-built>
  <how-to-verify>
    1. Review the livetest output from Task 2 -- all 28 tests should show PASS for Bybit
    2. Run a small Dir A live trade on Bybit via dashboard API:
       ```
       curl -X POST http://localhost:8080/api/spot/open \
         -H "Authorization: Bearer $TOKEN" \
         -H "Content-Type: application/json" \
         -d '{"symbol":"SEIUSDT","exchange":"bybit","direction":"borrow_sell_long"}'
       ```
    3. Verify position appears in dashboard with correct margin/borrow info
    4. Close the position via dashboard and verify:
       - Both legs close cleanly
       - GetMarginBalance shows no residual borrows
       - Position shows in history with PnL
    5. Repeat steps 2-4 for Dir B:
       ```
       curl -X POST http://localhost:8080/api/spot/open \
         -H "Authorization: Bearer $TOKEN" \
         -H "Content-Type: application/json" \
         -d '{"symbol":"SEIUSDT","exchange":"bybit","direction":"buy_spot_short"}'
       ```
    6. Verify edge case: partial fill handling (if applicable with small sizes)
  </how-to-verify>
  <resume-signal>Type "approved" if Bybit Dir A and Dir B lifecycle verified, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Livetest builds and runs: `go build ./cmd/livetest/`
- Bybit passes all tests: `go run ./cmd/livetest/ --exchange bybit --test-margin`
- Unit tests pass: `go test ./pkg/exchange/bybit/ -count=1 -v && go test ./internal/spotengine/ -count=1 -v`
- Full build: `go build ./...`
</verification>

<success_criteria>
1. Livetest has 28 tests (up from 26) with PlaceSpotMarginOrder and GetSpotMarginOrder coverage
2. Bybit passes all 28 livetest tests with --test-margin
3. Bybit Dir A and Dir B manually verified with live trades (checkpoint approved)
4. No residual borrows after exit on Bybit
5. No regressions in existing unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-spot-futures-exchange-expansion/01-01-SUMMARY.md`
</output>
